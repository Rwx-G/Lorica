[package]
name = "lorica-waf"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }
tracing = "0.1"
chrono = "0.4"
parking_lot = "0.12"

[dev-dependencies]
serde_json = "1"
