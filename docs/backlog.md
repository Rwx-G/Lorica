# Technical Backlog

Bug fixes, improvements, and maintenance tasks. For new features, see the Roadmap section in [README.md](/README.md).

## Open

| # | Item | Type | Notes |
|---|------|------|-------|
| 7 | Split `lorica/src/proxy_wiring.rs` (5195 LOC) into sub-modules + extract `request_filter` (1805 LOC) into per-check helpers + dedupe 18 error-response sites | Refactor | Single file carries routing (exact + wildcard host tables, path-prefix sort), LB wiring, cache integration, WAF, rate-limit, forward-auth, bot-protection, RequestCtx construction, response rewrite, error pages, and metrics. Target layout: `proxy_wiring/{mod, routing, backends, filters/{cache, waf, rate_limit, forward_auth, bot}, context, response, helpers}.rs`. Zero behaviour change ; use `cargo expand` before / after each extraction step. Split in 5 commits so each keeps `cargo test -p lorica` + `cargo clippy --tests -- -D warnings` green. **Bundled v1.5.2 audit H-8 (function-level)** : `request_filter` itself is 1805 LOC with ~30 nested early-returns (cyclomatic ~70+) ; before / alongside the file split, extract each early-return block into `fn check_<name>(self, session, ctx) -> Result<Option<Decision>>` returning `Some(Decision::Reject(status, reason, body_hint))` or `None`. Top-level becomes a sequence of `if let Some(d) = self.check_X(...)? { return self.write_decision(session, ctx, d).await; }`. ~12 such extractions. **Bundled v1.5.2 audit H-10** : `write_decision` collapses the 18 duplicate `extract_host + render_error_body + ResponseHeader::build + insert Content-Type/Length + write_response_header + write_response_body + return` blocks scattered through `request_filter`. Two of them already drift (`:3741` adds `Retry-After: 1` + `X-RateLimit-Reset` ; nothing else does) - the helper signature should accept `extra_headers: &[(&str, String)]` so those drift sites become first-class. |
| 8 | Split `lorica/src/main.rs` (4329 LOC) into `cli/` + `startup/` + extract `BackgroundTasks::start(role)` to dedupe `run_supervisor` / `run_worker` / `run_single_process` | Refactor | Mixes CLI subcommand parsing (clap), encryption-key rotation + generation, signal handling, tokio runtime init, worker spawn coordination. Target: `lorica/src/main.rs` shrinks to ~150 LOC of dispatch ; extract `cli/{mod, serve, rotate_key, reset_admin}.rs` and `startup/{mod, encryption_key, signals, workers, runtime}.rs`. 4 commits, each green. **Bundled v1.5.2 audit H-9** : `run_supervisor` (1363 LOC), `run_worker` (836 LOC), `run_single_process` (555 LOC) duplicate ~80% of wiring (notify dispatcher, persisted alert dispatcher, SLA collector flush task, probe scheduler reload, load-test engine, ACME challenge store, ACME renewal task spawn, cert-expiry check task spawn, basic-auth-cache prune, rate-limit prune, bot-stash prune, GeoIP / ASN set_handle, rDNS resolver init, blocklist refresh, OTel try-init - 15+ shared spawns). **The v1.5.2 worker-mode-cert-hotswap bug existed because of this duplication** (worker branch was missed when supervisor + single were already wired). Extract `BackgroundTasks::start(&store, &task_tracker, &alert_sender, role: Role) -> BackgroundTaskHandles` owning the 200-LOC of common spawns ; each `run_*` shrinks to ~150 LOC of role-specific wiring + one call. Do this before the file split so the `cli/` + `startup/` shape lands on the deduplicated code. |
| 14 | Upstream-sync `rustls-pemfile` removal in `lorica-tls` | Track | RUSTSEC-2025-0134 (unmaintained) is visible transitively through our Pingora fork `lorica-tls`. Native Lorica code migrated to `rustls-pki-types` in v1.5.0 ; the transitive dep will clear once Pingora upstream migrates. Monitor their tracker ; pull the change into the fork when it lands. |
| 15 | Upstream-sync `rand 0.8` removal in forked crates | Track | RUSTSEC-2026-0097 (unsound with custom logger) was visible transitively via Pingora forks (`lorica-runtime`, `lorica-limits`), the `tungstenite 0.24` / `axum 0.7` chain, and the `captcha` crate. Native Lorica code bumped to `rand 0.9` in v1.5.0. **v1.5.8: the advisory itself is cleared** - the `0.8` line was bumped `0.8.5 -> 0.8.6` via `cargo update`, so `cargo audit` no longer flags RUSTSEC-2026-0097 (down to 2 allowed warnings: `derivative`, `rustls-pemfile`). Removing the `0.8` line entirely still depends on the upstream forks bumping to `rand 0.9` and on the `axum` / `tungstenite` majors; keep tracking. |
| 17 | Story 7.3 : wire `header_timeout_s` and `flood_strict_rps` global settings | Feature | v1.5.1 audit H-5. Story 7.3 acceptance criteria checked `[x]` for "Add configurable `header_timeout_s` field to global proxy settings (default 10s)" and "Add configurable `flood_threshold_rps` and `flood_strict_rps` fields to global settings", but workspace-wide grep finds neither field outside the story md. `flood_threshold_rps` made it ; `header_timeout_s` and `flood_strict_rps` did not. Add to `lorica-config/src/models/settings.rs::GlobalSettings`, plumb to per-worker config + proxy hot path (slowloris read-timeout on header phase, halve-factor on flood-strict mode). Defer to v1.6.0 because adding new settings = `feat`, not patch. |
| 18 | OCSP staple runtime refresh loop + move OCSP fetch off the cert-resolver-reload critical path + hoist `OCSP_CLIENT: Lazy<reqwest::Client>` | Feature | v1.5.1 audit M-2. `lorica-tls/src/ocsp.rs:147-159`'s `try_fetch_ocsp` is invoked only from `reload_cert_resolver` (config reload / cert install) - a Lorica process running 30+ days happily continues to staple a 3-week-old "good" response. Spawn a `task_tracker.spawn` loop that walks the cert resolver's entries every `min(nextUpdate - now, 6h)`, re-fetches via `try_fetch_ocsp`, and `arc-swap`s a fresh `Inner` via a new `CertResolver::refresh_staples(...)`. Surface `lorica_ocsp_refresh_total{result="ok|fail"}` Prometheus counter. New behaviour = `feat`. **Bundled v1.5.2 audit H-5** : the cert-resolver reload sequence today swaps the `ProxyConfig` (new route live) BEFORE running `reload_cert_resolver`, which spends up to 10 s on parallel OCSP fetches (`try_fetch_ocsp` 10 s per-responder timeout). During that window, TLS handshakes to the new route's hostname call `CertResolver.resolve()` and get `None` - handshake fails. The same refresh-loop refactor closes this gap : OCSP fetching moves to a background task, the resolver hot-swaps the staple bytes when each response lands, and `reload_cert_resolver` itself becomes synchronous (cert bodies only, no OCSP I/O). Add a `lorica_cert_resolver_pending_ocsp_seconds` per-domain gauge while in there. **Bundled v1.5.2 audit L-21** : `fetch_ocsp_response` builds a fresh `reqwest::Client` per fetch (`ocsp.rs:89-92`) ; this is dormant today (only called on reload) but the refresh loop will make it N clients per cycle. Hoist to `static OCSP_CLIENT: Lazy<reqwest::Client>` mirroring the `HEALTH_HTTP_CLIENT` precedent BEFORE landing the refresh loop. **Pair with #33** (H-6 cert-resolver metrics + partial-tolerant reload) : both rework the same ~40 LOC of `reload_cert_resolver` ; ship together as one coherent cert-resolver pass instead of two refactors of the same surface. |
| 19 | Structured admin audit log for management mutations | Feature | v1.5.1 audit M-3. ~30 mutating handlers in `lorica-api/src/` ; only TWO emit a `tracing::warn!` with operator context (cert download in `certificates.rs:563-569`). Forensics gap : an operator cannot answer "who deleted route X at 03:14?". `Session.username` is in `req.extensions()` (per `middleware/auth.rs:343-346`) but handlers ignore it. Macro `audit_log!(state, action = "route.delete", target_id = id, before = ..., session = &session)` feeding a dedicated structured-log target. Apply uniformly to every state-mutating handler. Tamper-resistance via hash chain is a v1.7.0 candidate. New facility = `feat`. |
| 20 | TLS on management API port (`localhost:9443`) + add `/metrics` auth knob + fix `lorica unban` CLI URL scheme | Feature | v1.5.1 audit M-5. Session cookie is flagged `Secure` (`middleware/auth.rs:315`) but the API is served over plaintext HTTP (`server.rs:598-615`). Browsers' `localhost` "potentially trustworthy" exemption hides the issue ; the moment the operator forwards via a corporate reverse proxy without TLS termination in front, the cookie silently stops being sent. Ship a self-signed cert at startup and serve TLS on the management port (axum + `axum-server` with rustls config, certificate auto-rotated with the cert resolver). Alternative `feat` (Host-header check + 421) is simpler but less correct. **Bundled v1.5.2 audit L-1** : `/metrics` is unauthenticated even on localhost (`server.rs:221-226`, mounted under `metrics_routes` with no `require_auth`). Payload includes `lorica_certificate_expiry_days{domain}` (every cert SAN), `lorica_backend_health{backend_id, address="10.0.0.1:8080"}` (every upstream address), `lorica_http_requests_total{route_id, status_code}` (full route catalog), system CPU/memory. Any local user on a multi-tenant box (containers sharing loopback, Synology-style host) can curl the entire backend topology + cert inventory without a session. Add a `metrics_require_auth` global setting (default off for back-compat) ; when on, attach `require_auth` to the `metrics_routes` router. Alternative : static bearer token via `prometheus_scrape_token` env var, checked in a tiny middleware on `/metrics` only. **Bundled v1.5.2 audit L-22 (RESOLVED in v1.5.8: scheme switched to `http://`)** : the `lorica unban` CLI subcommand used `https://127.0.0.1:{port}` (`main.rs:361, 380`, with `danger_accept_invalid_certs(true)`) but the management API is HTTP today - immediate connect error. Once TLS lands the CLI works ; in the interim, either fix the scheme or improve the error message ("Cannot connect to management API on port {port}: {e}" is generic enough to make operators paste their password and get a connect error without understanding why). |
| 21 | Per-source-IP TCP connection cap | Feature | v1.5.1 audit M-6. A single source IP can open `max_global_connections` worth of connections to one route ; the per-route cap is shared across all clients. Add `connection_limits_per_ip: Option<u32>` global setting. In the existing connect-time CIDR filter (`lorica/src/proxy_wiring.rs:3410-3430` neighbourhood), increment / decrement a `DashMap<IpAddr, AtomicU32>` and refuse the accept past the cap. Defense in depth - the IP blocklist + WAF auto-ban already deflect most volumetric attacks. New setting = `feat`. |
| 22 | `BanReason` enum surfaced in `/api/v1/bans` JSON | Feature | v1.5.1 audit L-12. Story 7.3 designs `BanEntry` with a `BanReason` enum (RateLimit, Flood) ; the implemented backend uses the tuple `(banned_at: Instant, duration_s: u64)` (`cache.rs:84-103`) so the dashboard cannot show *why* an IP was banned. Add a `BanReason` enum to the backend, change the ban-list value type to carry it, and surface `reason` in the `/api/v1/bans` JSON. Updates the dashboard Security view alongside (4 fields per row : IP / reason / expiry / unban). New API field = `feat`. |
| 26 | CSP3 nonces for `style-src` (drop `'unsafe-inline'`) | Future | v1.5.1 audit L-1. Current CSP at `lorica-dashboard/src/lib.rs:74` includes `style-src 'self' 'unsafe-inline'` because Svelte injects scoped styles inline at runtime. Migrating to CSP3 nonces requires Svelte build-time emission of a per-build nonce + the dashboard backend reading and injecting that nonce into the served HTML + the CSP header. Non-trivial Svelte build hook ; defer to v2.0 alongside any other CSP overhaul. |
| 27 | Vendor `captcha 1.0` into `lorica-challenge` (or move to browser-side) | Chore | v1.5.1 audit M-16 (supply chain). `captcha 1.0` is maintained by `daniel-e` (single-maintainer, 113 GH stars, last push 2025-03-19 - ~13 months inactive at audit time), 800K crates.io downloads. Used in `lorica-challenge/src/captcha.rs` for the image-captcha fallback when PoW is bypassed - so on a security-sensitive path, though the crate is pure Rust with no native deps so takeover blast radius is "weakened bot challenge", not RCE. No `SECURITY.md` on the repo. Two options : (a) **Vendor in-tree** - inline ~600 LOC of pure Rust (PNG gen via `lodepng` + embedded font + filter code) under `lorica-challenge/src/captcha/` following the v1.3.0 `no_debug` precedent. Drops the dep and the takeover surface collapses onto what we review at inline time. (b) **Move image-captcha to the browser** - render the captcha client-side via Canvas + JS, remove server-side image gen entirely. Bigger UX change but removes the whole dep path AND reduces server CPU. Both options are feat-shaped (600 LOC rewrite with font handling + image deps, OR Svelte component + challenge flow refactor), out of v1.5.2 patch scope. Risk today is latent : no exploitable path exists, operators who don't enable the image-captcha fallback (PoW-only config) are unaffected. Decision for v1.6.0 : pick (a) unless the UX story for (b) is already planned. |
| 29 | v1.5.2 audit C-3 : dual rate-limit code paths fire on the same request (legacy `rate_limit_rps` + new `RateLimit` struct) | Refactor | `lorica/src/proxy_wiring.rs:2647-2700` (new `Route.rate_limit: Option<RateLimit>` engine via `LocalBucket::try_consume`) AND `lorica/src/proxy_wiring.rs:3653-3749` (legacy `Route.rate_limit_rps` / `rate_limit_burst` via `self.rate_limiter.observe`). Model-side at `lorica-config/src/models/route.rs:574-577` (legacy) + `:712` (new). A route with both legacy fields AND the structured `rate_limit` set goes through TWO independent token-bucket computations and gets blocked by whichever fires first. The two paths use different keys (`{id}|{ip}` vs `{id}:{ip}`) so cross-process state never converges. Auto-ban only wires off the legacy path - an operator migrating to the new `rate_limit` silently loses the auto-ban escalation. Path-rule overrides only know about the legacy fields, so per-path overrides have no effect on routes using the new struct. Fix : pick a winner. (a) Make legacy fields a thin compatibility shim that synthesises a `RateLimit { capacity: burst.unwrap_or(rps), refill_per_sec: rps, scope: PerIp }` and routes through the new path only, or (b) hard-deprecate the legacy fields with a one-shot DB migration. Then delete the legacy block. Auto-ban needs to be lifted to the unified path. Behaviour change + DB migration = `refactor`. |
| 33 | v1.5.2 audit H-6 : cert-resolver reload outcome has no Prometheus counter ; one bad cert poisons the whole batch silently | Feature | `lorica/src/reload.rs:803-809` + `lorica-tls/src/cert_resolver.rs:109-141`. `reload_cert_resolver` returns `()` ; the `Err` arm is a single `warn!` and that's it. No `lorica_cert_resolver_reload_total{result}`, no `lorica_cert_resolver_active_domains` gauge. A single malformed cert in the DB makes `cert_resolver.reload()` return `Err` for the whole batch (one bad cert short-circuits, leaves the previous map in place). Operator firing a config-save sees "OK" while every TLS handshake to the new cert silently fails. Fix : add `lorica_cert_resolver_reload_total{worker_id, result="ok\|fail"}` + `lorica_cert_resolver_active_domains{worker_id}`. Make `reload()` partial-tolerant : build cert-by-cert, log + skip the bad ones, swap with the rest. Behaviour change (partial-tolerant reload) + new metric = `feat`. **Overlaps with #18** (OCSP staple refresh loop) - both rework the same ~40 LOC of `reload_cert_resolver` ; ship together as one coherent cert-resolver pass. #18 moves OCSP I/O off the critical path, #33 makes the cert-body reload itself fault-tolerant + observable. |
| 34 | v1.5.2 audit H-11 : `update_settings` is 335 LOC of repeated `if let Some(x) = ... { check; settings.x = x }` | Refactor | `lorica-api/src/settings.rs:120-454`. ~25 fields each repeat the same shape. Bound-checking is inconsistent (some "must be >= 1", some "must be > 0", some "must be in 1..=N", some no upper bound). Adding a setting means hand-rolling 4-7 lines that look like the previous one ; copy-paste drift is the norm and audit history shows several bound-check escapes. Fix : `update_field!(body.x, settings.x, RangeI32(1..=N), "label")` macro, OR a builder `SettingsPatch::new(&body).numeric_range(...)?.string_choice(...)?.apply(&mut settings)`. Centralises bound-checking. Range / choice metadata becomes machine-readable, extending the v1.5.2 L-23 const-lift work (which exposed body / rate-limit caps for OpenAPI consumption) to settings field bounds as well. Behaviour-preserving = `refactor`. |
| 37 | v1.5.2 audit M-27 + M-28 : observability gap - SLA breach + active-probe + notification dispatch outcomes silent on Prometheus | Feature | **Both findings need three bounded-cardinality counters - architecturally these are NOT a quick patch.** **M-27 (SLA + probes)** : `lorica-bench/src/passive_sla/persistence.rs`, `lorica-bench/src/active_probes.rs`. Operator can't alert on "SLA threshold X breached for route Y" without parsing notifications out-of-band. A probe that flaps looks identical in `/metrics` to one that never ran. Want `lorica_sla_breach_total{route_id, threshold_kind}` + `lorica_active_probe_outcome_total{probe_id, outcome="ok\|fail\|timeout"}`. **M-28 (notification dispatch)** : `lorica-notify/src/channels/{email,slack}.rs`, `lorica/src/main.rs:4071-4101`. `lorica_notifier_events_dropped_total` exists for channel-lag drops only. No signal for actual delivery success / failure per channel. Want `lorica_notification_dispatch_total{channel="email\|webhook\|slack", outcome="ok\|http_4xx\|http_5xx\|timeout\|connect_failed"}` (3 channels x 5 outcomes = bounded cardinality, safe for Prometheus). **Architectural blocker (the v1.5.2 deferral reason - originally I described this as `patch`-shaped, then discovered the dep cycle while writing the v1.5.2 CHANGELOG)** : `lorica-api` owns the custom Prometheus `REGISTRY` (`lorica-api/src/metrics.rs`) and already depends on `lorica-bench` + `lorica-notify`. Bench / notify cannot depend on `lorica-api` to bump a counter (cycle). Three options to unblock, all `feat`-shaped : (a) Extract REGISTRY into a new `lorica-metrics` crate that api / bench / notify all depend on (~4-6h, mechanical extraction, lowest risk - **recommended**). (b) Callback hooks : bench / notify expose an `EventObserver` trait, the `lorica` binary registers an impl that writes to prometheus (~3-4h, requires API design). (c) `mpsc::Sender<Event>` channels : bench / notify emit events, `lorica/src/main.rs` consumes and increments (~2-3h, mirrors the existing notif-dispatcher pattern). v1.6.0 candidate. Pick (a) by default unless there's a reason the dep boundary should stay where it is. |
| 38 | v1.5.2 audit L-3 : captcha PNG stash unbounded per-IP / per-route | Feature | `lorica/src/bot.rs` captcha-image stash. ~10-30 KiB per PNG x 5 min TTL x unique IPs = unbounded RSS pressure under sustained challenge issuance. No per-IP cap, no global cap. Operator under captcha-flood scenario sees memory grow until process OOM-kills. Fix : add a global cap (e.g. 10000 entries) backed by an LRU on issuance + a per-IP-prefix counter (e.g. /24 for IPv4, /48 for IPv6) capped at e.g. 100 entries. Reject with HTTP 503 when caps tripped (operator already rate-limited via outer flood guard). New caps = `feat` (operator-tunable globals + LRU plumbing). |
| 39 | v1.5.2 audit L-15 : `MIRROR_SEMAPHORE` global cap of 256 ; one slow shadow starves all routes' mirrors | Feature | `lorica/src/proxy_wiring/mirror_rewrite.rs::MIRROR_SEMAPHORE`. Single global semaphore caps total in-flight mirror requests at 256. One slow mirror endpoint (e.g. an analytics shadow that stops responding) holds permits indefinitely until the per-request 5 s timeout fires ; meanwhile every other route's mirror waits in queue. Cross-route blast radius from a single misbehaving shadow. Fix : per-route semaphore (`DashMap<RouteId, Arc<Semaphore>>`) sized from a new `mirror_max_concurrent_per_route` global setting (default 32). Drop the global cap or keep it as a coarse safety net at 4096+. New setting + per-route plumbing = `feat`. |
| 41 | v1.5.9 audit (perf-auditor) item (b) : `CircuitBreaker` key allocations - residual from the v1.5.10 hot-path pass | Refactor | **(a), (c), (d), (e), (f), (g) resolved in v1.5.10** (see Resolved table). **(b) remains** : `CircuitBreaker::is_available/record_*` builds two owned `String` keys `(route_id.to_string(), addr.to_string())` per upstream request for a `DashMap` key (~100k alloc/s at 50k rps); pre-intern `(Arc<str>, Arc<str>)` in `RouteEntry`. Deferred from v1.5.10 because the interning has to thread `Arc<str>` through `RequestCtx` (`route_id` / `backend_addr` are `Option<String>` consumed by logging, SLA, metrics) AND through both `BreakerEngine` variants (Local + worker-RPC). Ship it with the #7 `proxy_wiring.rs` split, where `RouteEntry` / `RequestCtx` get restructured anyway. Bench before/after (perf-auditor marked "measure first"). |
| 43 | Upstream-sync : `lorica-proxy` fails clippy 1.95 `collapsible_match` lints (4 sites in `proxy_cache.rs:2391-2450`) | Track | Surfaced during the v1.5.10 cycle: `cargo clippy -p lorica -- -D warnings` under rust 1.95 lints the path-dep fork `lorica-proxy` and fails on 4 pre-existing `if` blocks that the new clippy wants collapsed into their `match` arms. Zero CI impact today (CI's lint job only clippies the product crates, which do not depend on `lorica-proxy`), and zero runtime impact. Revisit when pulling the next upstream Pingora sync, or if the CI clippy scope ever widens to the binary crate. Local workaround for full-crate linting: `cargo clippy -p lorica --no-deps`. |
| 42 | v1.5.9 audit (architect-reviewer) : structural refactors - the items NOT already tracked by #7 / #8 / #14 / #15 | Refactor | **Deferred from the v1.5.9 cycle (lot 6). Multi-day, needs dedicated QA per slice; NOT a patch.** **(a) `lorica-api` god-crate**: depends on `lorica-dashboard` + `lorica-bench` + `lorica-cache` + `lorica-waf` + `lorica-notify` + `lorica-tls` (`lorica-api/Cargo.toml:11-26`), 49 files incl. a 14-file ACME subsystem whose stated job is "management plane only". Extract ACME (issuance/renewal/challenge state machine) into a `lorica-acme` crate `lorica-api` calls through a thin trait - highest-value first slice. **(b) `AppState` mode enum** (`lorica-api/src/server.rs:107`): ~30 fields, ~half `Option<Arc<...>>` encoding deployment mode (single-process vs supervisor vs test) as nullable-field combinatorics; split into a mandatory `CoreState` + a `Mode` enum holding mode-specific handles so invalid combinations are unrepresentable. This is the precondition that makes the crate split reviewable - do it first. **(c) API contract drift gate**: Rust handlers, `lorica-api/openapi.yaml`, and `lorica-dashboard/frontend/src/lib/api.ts` (100 hand-written interfaces) are triple-maintained by hand across 104 operations with no CI gate; either generate `openapi.yaml` from the Rust DTOs (`utoipa` derive) then the TS types from the spec, or add a CI job that fails when the committed spec diverges from a freshly generated one. **(d) two-tier version leak**: `lorica-waf` and `lorica-notify` are first-party but sit at `0.1.0` (`*/Cargo.toml:3`), muddying the "0.1.0 == forked == rebaseable" signal; bump them onto the product version and reserve 0.1.0 for actual Pingora forks. **(e) `run_migrations` versioned table**: `lorica-config/src/store/mod.rs:183` tracks versions 1-21 correctly but later columns land via unconditional idempotent `ALTER TABLE ... ` with `let _ =` swallowing errors (e.g. `sticky_session` :415); convert to a `(i64, fn(&Connection))` table the runner iterates, every schema change getting a tracked row; add the missing `MIGRATION_V18` const for idiom consistency. **(f) `source-tree.md` doc-vs-reality**: `docs/architecture/source-tree.md` self-declares stale; replace the per-file enumeration with a crate-responsibility table. **Already tracked, do not duplicate**: split `proxy_wiring.rs` = #7; split `main.rs` = #8; forked-crate dep realignment (rand/x509-parser/etc.) = #14, #15. |

## Resolved

| # | Item | Type | Resolution |
|---|------|------|------------|
| 31 | v1.5.2 audit H-3 + M-16 : ConfigStore async-handler migration via `db_blocking` helper | Refactor | Resolved in v1.5.10. New `lorica-api/src/db.rs` with `db_blocking` (takes the `Arc<tokio::sync::Mutex<ConfigStore>>` directly so background tasks qualify too; acquires `lock_owned` then `spawn_blocking`, generic error `Into<ApiError>`) and `log_db_blocking` (LogStore, `Result<T, String>` closures). ~100 sites migrated: the 96 inventoried handler sites across certificates / backends / routes / settings / sla / probes / loadtest / dns_providers / waf / config / status / auth / cert_export / acme (incl. the renewal background loop and expiry check), plus `SessionStore` (login persistence, per-request DB fallback, logout, renew, GC, password-change purge) and the `clear_waf_events` site both missed by the original inventory. The six hand-rolled M-7 `spawn_blocking` sites collapsed onto the helpers. Error mapping fidelity preserved per site (handlers that returned 500 on NotFound still do). Locking granularity unchanged: one `db_blocking` per former guard acquisition, none merged or split. 457 lorica-api tests green. The N+1 `LEFT JOIN` folding idea was NOT done (no site is request-critical; revisit if Overview polling ever shows up in profiles). |
| 30 | v1.5.2 audit H-2 : bot-stash hot path holds `tokio::sync::Mutex<ConfigStore>` across sync SQLite I/O | Refactor | Resolved in v1.5.10 via option (a): `lorica/src/bot.rs` gained a local `stash_blocking` helper (owned guard + `spawn_blocking`, returns `Option<T>`, join failure reads as stash miss); `insert` / `take` / `captcha_image` / `prune_expired` / `len` SQLite arms all route through it. Queueing semantics on the shared store mutex unchanged. Option (b) (dedicated `synchronous=OFF` connection for the ephemeral stash) stays a candidate if bot-flood profiling ever shows the shared-store WAL as the next bottleneck. |
| 24 | Refactor : access-log + WAF event persistence via bounded queue + batched `INSERT` | Refactor | Resolved in v1.5.10. New `lorica-api/src/log_writer.rs`: bounded `tokio::sync::mpsc` queue (8192) + a dedicated OS writer thread (`blocking_recv` + opportunistic `try_recv` drain, so it works identically in supervisor / worker / single-process modes with no runtime-context dependency). Hot path (`LoricaProxy.log_writer` replaces the direct `log_store` handle) does a non-blocking enqueue; the writer batches up to 256 rows per transaction via new `LogStore::insert_batch` / `insert_waf_events_batch` (one WAL fsync per batch). Overflow drops are counted in `lorica_log_write_dropped_total{kind="access\|waf"}`; batch failures keep warning + bumping `lorica_waf_event_persist_failed_total` (v1.5.1 L-6 guarantee). Supersedes v1.5.2 audit M-8 (multi-rule WAF matches now cost one queue push each). Writer integration test included (`log_writer::tests`). |
| 40 | v1.5.2 audit L-17 + L-18 : `EwmaTracker` global `RwLock` contention + cold-backend thundering herd | Feature | L-18 resolved in v1.5.8 (bounded explore/exploit `select_best`). L-17 resolved in v1.5.10: `EwmaTracker.scores` switched from `Arc<parking_lot::RwLock<HashMap<String, f64>>>` to `Arc<dashmap::DashMap<String, f64>>` - per-sample `record` writes contend per shard instead of serialising on one global write lock. The simpler `DashMap<String, f64>` was preferred over the suggested `DashMap<String, Mutex<f64>>` since the read-modify-write already happens under the shard guard via `get_mut`. Type propagated through `WorkerMetricsCtx`, the legacy command-channel metrics handler, `AppState.ewma_scores`, and `backends.rs::get_ewma_score_async`. Explore/exploit selection semantics unchanged (7 EwmaTracker unit tests green). |
| 41a-g | v1.5.9 audit (perf-auditor) lot 5 : hot-path allocations + lock scope, items (a) (c) (d) (e) (f) (g) | Refactor | Resolved in v1.5.10 (item (b) remains open, see #41). (a) `SmoothWrrState::next` does zero `String` allocations in steady state (`contains_key`/`get_mut` by `&str`; only first-seen backends pay for the owned key) - simpler than the suggested `Arc<str>` interning, same win. (c) WAF event ring buffer: events cloned BEFORE the buffer lock, pushed as owned values under a short lock; eviction semantics byte-identical. (d) `WafEngine::evaluate` acquires the `custom_rules` read lock once and threads `&[(CustomRule, Regex)]` through `scan_custom_rules` (signature change, private fn, no external callers). (e) `should_rewrite_response` fully allocation-free via an ASCII case-insensitive prefix compare (drops both the per-response `to_ascii_lowercase` of the Content-Type and the per-prefix lowercase of the configured list). (f) client IP parsed once per request into `RequestCtx.client_ip_addr`; WAF whitelist, IP blocklist (`is_blocked(&IpAddr)`), GeoIP, and bot-protection reuse it. (g) `apply_worker_generic_counters` restructured to prepare label/key work outside the supervisor snapshot lock and hold it only for the delta math (merge semantics preserved to avoid the double-count a snapshot swap would cause); the `snapshot_per_worker_counters` 7-Vec suggestion was NOT actionable (the allocations are inside `prometheus::Collector::collect()` by API contract; documented at the call site). No benchmark figures captured: alloc-count / lock-scope reductions verified by review + regression suite, per-change benching deferred to a future perf rig. |
| 32 | v1.5.2 audit H-4 : `health_check_loop` probed backends sequentially | Feature | Resolved in v1.5.8. The loop is now three phases: drain monitoring (sequential), concurrent probing via `futures_util::stream` `buffer_unordered` bounded by the new operator-tunable `health_max_concurrent_probes` global setting (default 32, range 1..=512, wired end-to-end through the config model, KV store, settings API, and the dashboard Global Configuration tab), then a single sequential flip-hysteresis pass whose confirmed status changes are persisted under one store lock. Round time drops from `interval + sum(probes)` to `interval + max(probe)`; lock acquisitions per round drop from N to one. The "batch DB writes via one `spawn_blocking` transaction" idea folds into the still-open #31. |
| 28 | v1.5.2 audit C-1 + M-26 : worker-restart path - re-wire RPC fd on respawn + move 30 s exponential-backoff sleep off the supervisor tokio thread | Refactor | Resolved in v1.5.2. **C-1** : `restart_worker` now returns `Result<Option<(OwnedFd, OwnedFd)>, _>` (was cmd-fd only) ; the crash branch in `lorica/src/main.rs::monitor_workers` re-spawns the supervisor RPC handler via the new `spawn_supervisor_rpc_handler(...)` helper (extracted from the inline initial-spawn block, single source of truth for "supervisor RPC loop for worker N"). The respawned worker's RPC endpoint inserts back into `worker_rpc_endpoints` ; the next two-phase reload fans out the commit. **M-26** : `restart_worker` split into `restart_backoff(id) -> Duration` (read-only) + `restart_worker(id)` (no internal sleep) ; the `monitor_workers` caller drops the `std::sync::Mutex<WorkerManager>` lock between the two and does `tokio::time::sleep(backoff).await` in between (re-checking the `shutting_down` flag on either side so SIGTERM mid-backoff doesn't trigger a respawn that races shutdown). Verified end-to-end : new section in `tests-e2e-docker/run-tests-workers.sh` does `kill -9 worker --id 1`, polls `/api/v1/workers` for the new PID, then triggers `maintenance_mode=true` and fans out 30 parallel requests. Pre-fix : 12 x 503 + 18 x 200 (the 18 stale 200s = the respawned worker still on boot-time config). Post-fix : 30 x 503 + 0 x 200. Test runner gets `pid: "service:lorica-workers"` + `user: root` in `docker-compose.yml` to signal worker child processes across the shared PID namespace. 268 lorica + 44 lorica-command + 14 lorica-worker tests green. |
| 35 | v1.5.2 audit M-22 : worker-mode cert hot-reload test gap | Test debt | **Cert-hot-reload surface : Resolved in v1.5.2** via three white-box regression tests against `proxy_wiring::handle_config_reload_commit` (the function whose missing `reload_cert_resolver` call was the v1.5.2 bug shape). New `lorica/src/proxy_wiring/cert_reload_commit_tests.rs` module : (1) the regression pin (seed cert + route + commit, assert `cert_resolver.domain_count() == 1` post-commit) ; (2) active-cert filter pin (orphan cert stays unloaded) ; (3) gate short-circuit pin (stale generation skips reload + replies error). Required exposing `IncomingCommand::for_test()` in `lorica-command` (marked `#[doc(hidden)]`). 268 lorica + 44 lorica-command tests green. **SMTP encryption modes** : already covered by `lorica-notify::channels::email::tests` (default-port-per-mode + plaintext / TLS / STARTTLS builder regression tests, 12 cases) ; mockito doesn't do SMTP so the audit's mockito suggestion was incorrect. **ACME-renewal-in-worker-mode** : intentionally skipped - shares the same RPC path the cert-hot-reload regression test now covers, so marginal value is low ; a Pebble-fixture e2e test stays in v1.6.0+ scope as a separate item if a renewal-specific bug ever shows up. |
| 36 | v1.5.2 audit M-23 : hostname validators in `routes/crud.rs` repeat 6-step shape across helpers | Refactor | Resolved in v1.5.2. Folded `validate_redirect_hostname` (canonical-host redirect target) and `validate_hostname_alias` (SNI alias, accepts `*.example.com`) into one `validate_dns_hostname(raw, HostnameOpts { allow_wildcard, allow_empty, field_label })` helper in `lorica-api/src/routes/crud.rs`. Both surviving functions become 9-line wrappers that delegate. The `HostnameOpts` knobs cover the only real differences (alias accepts wildcards, redirect doesn't ; redirect treats empty-after-trim as "clear field", alias rejects). Future change to RFC-1123 acceptance (IPv6 literals in brackets, label cap, alphabet) is now a single edit instead of two synchronised edits. While in there, an orphaned doc-comment block (the redirect_hostname preamble that had been silently stuck to `validate_group_name` since a previous refactor) was deleted. 453 lorica-api tests green ; the existing `redirect_hostname_validation_tests` + `hostname_alias_tests` modules pin byte-for-byte error messages so the dashboard surface is unchanged. |
| 23 | Refactor : SQLite calls in async handlers via `spawn_blocking` | Refactor | Resolved in v1.5.2. Six `LogStore` call sites (`logs.rs::get_logs / export_logs / clear_logs`, `waf.rs::get_waf_events / get_waf_stats`, `settings.rs::notification_history`) wrapped in `tokio::task::spawn_blocking({ let store = Arc::clone(store); move || store.<method>(...) }).await` so a contended SQLite WAL no longer stalls the tokio reactor. `LogsQuery` gained `#[derive(Clone)]` so query params can move into the closure while the in-memory fallback branch still has its binding. Two-tier `?` (JoinError + SQLite error) keeps the runtime-shutdown abort distinguishable from a SQLite failure. 442 lorica-api tests + lorica suite green. |
| 25 | Refactor : move `hex_to_16_bytes` conversion into `fresh_nonce` typing | Refactor | Resolved in v1.5.2. `BotEngine::fresh_nonce` now returns `([u8; 16], String)`. The PoW branch consumes the raw bytes directly ; the captcha branch destructures with `(_, nonce)`. Dead `hex_to_16_bytes` helper + its 3 tests removed (shared `hex_digit` kept, still used by the url-decode helper). Replaces the `.expect("fresh_nonce returns a 32-char hex string")` runtime contract on the bot-protection hot path with a compile-time invariant. 265 unit tests green. |
| 13 | Exported-cert orphan cleanup | Feature | Resolved in v1.5.0. New `GET /api/v1/cert-export/orphans` + `DELETE /api/v1/cert-export/orphans/:name` endpoints walk the export root and surface every per-hostname subdirectory whose name does not map to a live certificate (neither the sanitised hostname nor the `cert-{id}` wildcard fallback), with size + mtime per entry. The dashboard Settings → Cert export tab gains an "Orphan directories" section with a rescan button, per-row Delete button, and a `ConfirmDialog` explaining the consequences (privkey.pem removed, any consumer still reading from the path starts failing). Triple safety on the delete path : hostname sanitiser on the name (rejects `..`, `/`, empty, leading dot, non-ASCII), re-check against the live-cert set (defense in depth against scan/click race), parent == export root assertion (catches pathological `cert_export_dir` edits). 9 new unit tests (empty-when-disabled, dangling-subdir detection, non-sanitisable-entry skipping, missing-root graceful, delete happy, idempotence, path-traversal rejection, live-cert refusal, disabled-feature rejection). |
| 10 | `#![warn(missing_docs)]` pass on native crates | Doc coverage | Resolved in v1.5.0. `#![warn(missing_docs)]` is enabled on `lorica-challenge`, `lorica-config`, and `lorica-api` ; `///` comments added on every previously-undocumented public item (~780 items across 28 files), covering model fields (`Route`, `Backend`, `Certificate`, `GlobalSettings`, ...), enum variants, request/response DTOs in `routes/crud.rs` + per-feature sub-DTOs (path rules, header rules, traffic splits, forward-auth, mirror, response rewrite, mTLS, cert export, DNS providers, load tests, probes, SLA, settings, backends, status, system, workers, auth, logs, waf, config, metrics), and the challenge crate's error + mode + IP-prefix surface. `cargo clippy --tests --no-deps -- -D warnings` stays green so future changes break CI if they introduce undocumented public surface. |
| 9 | ACME module unit tests | Test debt | Resolved in v1.5.0. `acme::tests` went from 28 → 50 cases : `is_valid_dns_server` shell-injection filter (8 tests), pure `should_auto_renew` renewal predicate (7 tests), and HTTP-mocked coverage of the Cloudflare + OVH DNS challengers via `wiremock = "0.6"` (7 tests : happy paths + 4xx error body surfacing + missing-record handling + OVH's signed-request round `/auth/time` + `/record` + `/refresh`). Route53 is intentionally out of scope : it routes through `aws-sdk-route53`, which carries its own request/response stack + upstream unit coverage, so our ~30 LOC of builder calls would just re-validate the SDK at runtime. HTTP-01 flows + renewal-scheduler re-queue are covered via the E2E Docker path (Pebble), not unit tests ; mocking `instant-acme`'s full protocol end-to-end in a unit test would duplicate Pebble's responsibility. |
| 16 | `verdict_cache_fifo_evicts_oldest_when_full` test race under parallel execution | Test infra | Resolved in v1.5.0. The three tests mutating the process-global `VERDICT_CACHE` + `VERDICT_CACHE_ORDER` pair (`verdict_cache_hit_skips_hmac_verify`, `verdict_cache_expired_entry_is_miss`, `verdict_cache_fifo_evicts_oldest_when_full`) now carry `#[serial(verdict_cache)]` from the new `serial_test = "3"` dev-dependency, so cargo test runs them sequentially without having to drop the whole binary to `--test-threads=1`. Tests that only read the cache via `evaluate()` stay parallel ; they never fill the 16384-entry cap, so FIFO collision with them is impossible. |
| 6 | `return_status` path renders an empty body | Bug | Fixed in v1.4.0. The `proxy_wiring::request_filter` branch for `return_status` (without `redirect_to`) used to write `ResponseHeader::build(status, None)` and no body, so clients received the status code with a blank page - inconsistent with every other terminal path that funnels through `render_error_body()`. The operator's optional `error_page_html` template was also ignored. Now routed through `render_error_body(status, error_page_html)` with `Content-Type: text/html; charset=utf-8` and explicit `Content-Length`. The per-path rule's `error_page_html` overrides the route-level default when set (picked up from `ctx.route_snapshot`). |
| 1 | Global IP whitelist for WAF bypass | Fix | Resolved in v1.1.0 - Global WAF whitelist IPs in Settings |
| 2 | WAF pattern tuning (body scan false positives) | Fix | Resolved in v1.1.0 - Path traversal and protocol violation rules excluded from body scanning |
| 3 | SLA accuracy in worker mode | Fix | Resolved in v1.1.0 - Supervisor checks thresholds on every flush cycle |
| 4 | CLI unban command | Fix | Resolved in v1.1.0 - `lorica unban <IP> --password <PASSWORD>` |
| 5 | Worker drain timeout too short for long-poll clients | Improvement | Observed in prod during v1.1.0 -> v1.2.0 upgrade: all 6 workers got SIGKILL after the 30 s drain timeout because GitLab runner long-polls (`POST /api/v4/jobs/request`) held connections open past the deadline. Not a bug but hurts graceful upgrades. Options: (a) make drain timeout configurable, (b) close idle keepalives aggressively on SIGTERM, (c) force-close long-polls after a grace period. Impacts zero-downtime deploys. Fixed in 1.4.0 |
