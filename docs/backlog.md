# Technical Backlog

Bug fixes, improvements, and maintenance tasks. For new features, see the Roadmap section in [README.md](/README.md).

## Resolved in v1.6.0 (Epic 8)

These items shipped in the v1.6.0 cycle and have been pruned from the Open
table. See `docs/qa/epic-8-report.md`, the `[1.6.0]` section of
`CHANGELOG.md`, and the commit log:
**#17** (header_timeout_s + flood_strict_rps, Story 8.10), **#18** +
**#33** (OCSP background refresh + OCSP_CLIENT + partial-tolerant
cert-resolver metrics, Story 8.5), **#19** (admin audit log, Story 8.9),
**#20** (management-plane TLS listener + `/metrics` auth + `unban` scheme,
Story 8.8), **#21** (per-source-IP connection cap, Story 8.9), **#22**
(BanReason, Story 8.11), **#26** (CSP3 style-src nonce, Story 8.8),
**#27** (vendor `captcha`, Story 8.12), **#29** (dual rate-limit path
unification, Story 8.10), **#37** (SLA / probe / notification counters +
`lorica-metrics`, Story 8.11), **#38** (bot-stash caps, Story 8.9),
**#39** (per-route mirror semaphores, Story 8.9), **#43** + **#45**
(clippy-1.95 workspace cleanup), **#44** (WAF retention + 24h count),
**#46** (`settings/schema` endpoint, Story 8.10 AC #7), **#47** (Story 8.8
completion - the management TLS listener and CSP3 nonce shipped, so the
partial-status pointer is retired).

**#42** (v1.5.9 architect-reviewer structural refactors) - all five slices
landed in the v1.6.0 audit-fix follow-up: **(d)** `lorica-waf` /
`lorica-notify` versioned onto the product line; **(e)** `run_migrations`
converted to a tracked `(version, fn)` table with an idempotent
`add_column_if_absent` guard (old-field-DB upgrade path tested); **(b)**
`AppState`'s mode-specific `Option<Arc<...>>` fields collapsed into a `Mode`
enum so invalid single-process/supervisor combinations are unconstructable;
**(a)** the pure ACME core (DNS challengers + instant-acme driver) extracted
into a new first-party `lorica-acme` crate; **(c)** an `openapi.yaml` <->
axum-route-table drift-gate test (which caught and reconciled three
undocumented endpoints). Sub-items (f) source-tree and the split of
`proxy_wiring.rs` / `main.rs` were already resolved in v1.5.10.

**#48** (settings cross-field validation) - `GlobalSettings::validate_cross_fields`
added in `lorica-config`, called from `lorica-api::settings::update_settings`
on the merged result before persist. Enforces `cert_warning_days >
cert_critical_days` and `flood_strict_rps < flood_threshold_rps` (when both
set; `0` strict = auto). Per-field bounds stay at the API boundary; the
uid/gid and zero-duration-ban NOTE drifts are documented intentional choices,
not invariants. Five unit tests in `lorica-config`.

## Open

| # | Item | Type | Notes |
|---|------|------|-------|
| 14 | Upstream-sync `rustls-pemfile` removal in `lorica-tls` | Track | RUSTSEC-2025-0134 (unmaintained) is visible transitively through our Pingora fork `lorica-tls`. Native Lorica code migrated to `rustls-pki-types` in v1.5.0 ; the transitive dep will clear once Pingora upstream migrates. Upstream tracking issue [cloudflare/pingora#772](https://github.com/cloudflare/pingora/issues/772) is still open (re-checked during the v1.6.0 dependency pass). Monitor their tracker ; pull the change into the fork when it lands. |
| 15 | Upstream-sync `rand 0.8` removal in forked crates | Track | RUSTSEC-2026-0097 (unsound with custom logger) was visible transitively via Pingora forks (`lorica-runtime`, `lorica-limits`), the `tungstenite` / `axum` chain, and the `captcha` crate. Native Lorica code is on `rand 0.9` (v1.5.0) and the advisory itself was cleared in v1.5.8 (bumped `0.8.5 -> 0.8.6`). Removing the `0.8` line entirely still depends on the upstream forks bumping to `rand 0.9`. NOTE (v1.6.0 dep pass): the product-crate `rand 0.9 -> 0.10` bump was deliberately NOT taken - it is pervasive across product AND the pinned-at-0.8 forked crates, so bumping would fragment the workspace into three `rand` majors without removing the old line from the tree. `reqwest 0.12 -> 0.13` deferred for the same reason (forked `lorica-tls` keeps 0.12). Both revisit when the fork moves. |
| 49 | Arc-wrap `SinkEvent` for multi-lane publish | Perf | Story 9.8 QA (performance-auditor, Medium, measure-first): with syslog + OTLP both enabled, each exported event pays one deep clone per lane plus the initial clone (~3 string-heavy clones per access row at request volume). Wrapping the payload in `Arc` collapses that to refcount bumps. Structural change to the `SinkEvent`/`SinkPayload` surface touching both consumers ; confirm allocator pressure under a load test with both sinks on before doing it. |
| 50 | OTLP logs per-kind toggles | Feature | Story 9.8 QA: syslog has access/waf/audit toggles, the OTLP lane is all-on (`SinkLane` already models the filter ; it is a settings gap, not a design one). Add `otlp_logs_{access,waf,audit}_enabled` when an operator asks for e.g. audit-only OTLP. |
| 51 | Invert sink lane creation: `install() -> Receiver` handoff to consumer-side registration | Refactor | Story 9.8 QA (architect-reviewer): the OTLP lane's receiver must be wired by the caller, an obligation stated only in prose ; a dropped receiver or failed `init_logs` leaves a lane whose events all drop (mitigated in 9.8 by lane self-removal for syslog and cfg gating for OTLP, but not compiler-enforced). Revisit when Story 9.2 adds the next consumer of this seam - a `register_lane(name, filter) -> Receiver` API called by the consumer owner makes "installed lane, dead consumer" unrepresentable. |
| 52 | Crate boundary for the fleet runtime (`lorica-fleet` first, `lorica-obs` second) | Arch | **Re-scoped at the Epic 9 close (architecture review).** The extraction that unlocks anything is `lorica-fleet`, not `lorica-obs`: `lorica-api/src/cluster/runtime.rs` (whose own doc opens "No HTTP here"), `cluster_telemetry_store.rs`, and the ingest and replication POLICY currently inlined in `lorica/src/startup/cluster_plane.rs` (the audit-charged-but-exempt rule, take-before-measure, the watermark override, the drop reasons) are consumed by the binary's startup path and live in the HTTP crate only because `AppState` needed them; the binary imports seven `lorica-api` modules for cluster work alone. `lorica-obs` as scoped below covers none of them, so taking it first pays a crate split and leaves the binary linked to `lorica-api` for the fleet runtime. `lorica-fleet` depends on `lorica-cluster` + `lorica-config`, is consumed by `lorica-api` (handlers) and `lorica` (composition), and gives the ingest policy a home where it is a pure `admit(batch, node, stats) -> outcome` with unit tests, the seam `evaluate_hello` already proved. The extraction that would be a MISTAKE is folding the runtime into `lorica-cluster`: it would drag `lorica-config` into the transport crate and destroy the one property that kept nine stories from tangling (the wire layer treats the blob as opaque bytes). Then `lorica-obs`, scope unchanged. Earlier scope note kept for the record: `lorica-obs` = `metrics`, `log_writer`, `log_sinks`, `system`; `log_store` / `audit` are explicitly EXCLUDED (Story 9.9 QA, architect-reviewer). Those four are mechanisms; `LogStore` carries a domain invariant (one hash chain per node, local tail, verbatim fan-in) and filing an unkeyed hash chain with a compliance claim under a crate named for observability puts the invariant further from the API that owns it. If the audit store ever needs to be reachable without the management API, that is a separate, smaller extraction with a name that says what it is. **The stated trigger has fired** (Story 9.7 QA, architect-reviewer): `lorica-api::system` (`SystemCache`, `disk_usage_statvfs`) is now consumed by the cluster follower in `lorica`, which is the second shared runtime service the entry says to wait for. Reusing it rather than reimplementing the reserved-blocks correction was the right call and the dependency triangle is acyclic, but `lorica-api` is now growing a second identity as the workspace utility crate, so a binary wanting the follower without the management API cannot have one. Take the extraction in ONE move at the top of the next cycle, with `system.rs` as its third member alongside `log_writer` and `log_sinks`; doing it mid-story is the per-story accretion this entry exists to prevent. Original note, Story 9.8: the sink hub + syslog transport live in `lorica-api` beside `log_writer.rs` (whose bounded/try_send/drop contract they reuse) even though their consumers and lifecycle owners live in `lorica`. Acceptable while there is ONE shared runtime service ; when the cluster stories (9.2-9.6) add another, extract `lorica-obs` (metrics + log_writer + log_sinks) in one move instead of per-story accretion. |
| 53 | Log-sink observability follow-ups | Observability | Story 9.8 QA leftovers, none blocking: (a) `lorica_log_sink_up{sink}` gauge from the consumer loop's connection state (needs per-worker gauge aggregation, which only exists for counters today) ; (b) unit tests around `apply_log_sinks_from_store` transitions (dedup no-op, enable/disable) - global-state heavy, needs a test seam ; (c) a `--workers` variant of the `log-sinks` e2e phase to exercise the per-worker counter aggregation end to end (the counters ARE in `PER_WORKER_COUNTERS` and covered by its sync test, but no e2e asserts the aggregate). |
| 54 | Viewer role can read sink topology | Policy | **RESOLVED 2026-09-09 (Epic 9 close): `GET /settings` withholds `syslog_endpoint`, the syslog CA and client certificate, `syslog_extra_sd` and `otlp_endpoint` from a Viewer; the endpoint's floor stays Viewer because the dashboard reads it for every role.** Original entry: Story 9.8 QA (security-auditor, Info): `GET /settings` is Viewer-accessible and now discloses `syslog_endpoint`, the CA / client-cert public halves and `syslog_extra_sd` to the least-trusted authenticated role. Pre-existing pattern (same as `otlp_endpoint`, GeoIP paths). Decide deliberately whether sink topology should be SuperAdmin-read-only ; today it follows the house convention. |
| 55 | Small dedup follow-ups from Story 9.8 reviews | Refactor | (a) `SettingsWatcher<T>` helper to collapse the repeated `OnceLock<Mutex<Option<Snapshot>>>` scaffolds in `lorica/src/reload.rs` (now 3+ instances) ; (b) `normalizeSettingsForm()` extraction in `Settings.svelte` (load and save-response mapping duplicated, pre-existing hot spot) ; (c) shared `ensure_otlp_signal_path()` for the 4 copies of the `/v1/<signal>` suffix-append closure across `lorica/src/otel.rs` and `lorica-api/src/settings.rs` (2 collapsed into `probe_otlp_endpoint` already ; the two exporter-side copies remain). |
| 56 | Per-recipient replication payload for need-to-know distribution | Arch | Story 9.4 QA (all four auditors, ranked Medium to High). The blob is fleet-wide and `node_selector` is evaluated by the RECIPIENT, so every follower holds every other node's routes, upstream addresses, IP lists, mTLS configuration and Basic-auth hashes ; compromise of the least-trusted edge discloses the fleet's routing topology. Harmless in 9.4 (no secret material in the blob, only `sha256:` digests) and now stated plainly in `docs/cluster.md`. NOT harmless in Story 9.5: a predicate the recipient applies to a payload it already holds is not need-to-know, and Phase 1 decision D11 assumed it was. 9.5 must filter on the control plane, per recipient, matching on `node_id` rather than the display name (`cluster_nodes.name` has no UNIQUE constraint and is chosen by the joining node). Each node then converges on its own payload, which changes the single fleet-wide hash the convergence design rests on - take the decision at the start of 9.5, not inside it. |
| 57 | Replication tuning is compiled in | Config | Story 9.4 QA (architect-reviewer): `Replicator::per_node_deadline` (10 s) and `quarantine_threshold` (3) are `pub` fields but `ControlPlane::new` is the only construction site, so no operator can reach them - the `pub` advertises configurability the architecture does not deliver. These two decide whether a high-latency WAN link is a slow node or a quarantined one. Either make them private constants and say so, or thread a small `ClusterPlaneTuning` struct from the CLI. |
| 58 | Replicator policy state does not survive a restart | Track | Story 9.4 QA (architect-reviewer): `evictions`, `quarantined` and the last report live in process memory, while everything else authoritative on the control plane is persisted (roster, CRL, generation, break-glass, applied markers). A hot upgrade therefore releases every quarantined node silently, and `GET /cluster/replication` reports "no round yet" for a fleet converged for weeks. Probably correct to keep quarantine as a within-process circuit breaker - a restart is a legitimate reason to re-probe - but log the reset at WARN so it is not silent, and decide whether the last report should be persisted for the dashboard. |
| 59 | `DriftTracker`'s two-method contract is enforced by prose | Refactor | **Story 9.7 shipped without triggering this** (verified in the 9.7 QA): `Dashboard.svelte`'s poll calls `getClusterStatus` only, `/cluster/drift` has no frontend caller at all, and the drift pill is computed client-side. The read path also uses `record_first_seen` rather than `observe`. The entry stays open on its own merits, since `observe` is still reachable from any caller, but its stated trigger has passed without firing. Original note, Story 9.4 QA (architect-reviewer): `observe` advances the alert backoff and returns what is due ; `record_first_seen` does not. The read path must call the second and the alert path the first, and the rule lives in a comment. The exact bug the design avoids (an operator refreshing the dashboard consuming a node's suppression budget) is one accidental call away, and Story 9.7 will add dashboard polling on that endpoint. Make `observe` reachable only through a handle the periodic task owns. |
| 60 | Certificate key retention is byte-exact PEM equality | Track | Story 9.4 QA (architect-reviewer): `apply_replica_certificates` keeps a follower's private key when `secret_digest(held.key_pem)` matches the blob's digest, computed over raw PEM text. Once Story 9.5 delivers keys on its own path, any line-ending or trailing-newline difference between the two writers silently wipes the key and drops the route to the default certificate, on a path that runs on every configuration change. Digest the parsed DER instead, or assert byte-identity in a test that crosses both stores. |
| 61 | Story 9.4 small follow-ups | Refactor | QA leftovers, none blocking: (a) `Replicator::run_round` (~105 lines) and `spawn_cluster_plane` (~245 lines) are past one screen and have obvious seams (partition/prepare/commit ; load_pki/adopt_or_bind/spawn_tasks) ; (b) `ReplicationReport`'s three `Vec<(String, String)>` fields want the named pair `lorica-api` already defines as `NodeOutcome` ; (c) `Route::route_applies_to_node(route, name)` stutters and should be a method ; (d) `write_key` in `cluster_replica.rs` is inlined twice more because it needs a transaction - pass the executor ; (e) `get_status` reads both the control-plane generation and the applied marker then discards one, and can pair a generation with a hash belonging to the other quantity ; (f) RESOLVED at the Epic 9 close: every `/cluster/*` read (`status`, `nodes`, `replication`, `drift`, `logs`, `waf-events`) now carries the `cluster` limiter ; (g) a staged generation has no expiry, so a Prepare that is never committed holds a decoded blob for the life of the process ; (h) unbounded Prepare fan-out clones the blob once per target - bound with a semaphore and carry it as `Arc` if a large fleet ever meets a large configuration ; (i) `prepare_replica` runs a pure-CPU decode inside `db_blocking`, holding the follower's store lock for no reason. |
| 62 | Decide `lorica-cluster`'s version line | Chore | **RESOLVED 2026-09-09: it follows the product version** (`docs/BUMP-CHECKLIST.md` lists it as first-party since v1.7.0; bumped to 1.7.0 with the release). The compatibility contract it carries is now stated in three places rather than implied: `docs/cluster.md` "Running a Mixed-Version Fleet" (protocol range, schema ordering, followers first), the `version.rs` module doc (schema is the binding gate; `INCOMPATIBLE_VERSION` is unreachable until `PROTOCOL_VERSION` moves), and the frozen wire corpus `lorica-cluster/tests/wire_corpus.rs` + `tests/fixtures/wire-corpus-v1.txt` (encoding and decoding stability of every message against the bytes v1.7.0 ships). Original entry: the crate was pinned at 1.6.0 and appeared in neither the product-version list nor the forked-at-0.1.0 list. |
| 63 | `cargo fmt --check` does not pass on `lorica/src/ai_bot.rs` | Chore | Noticed during the Story 9.5 audit while gating the remediation: `cargo fmt --all -- --check` reports drift in `ai_bot.rs` only (one stray blank line plus four call sites rustfmt would rewrap). Pre-existing and unrelated to Epic 9, so it was NOT folded into a 9.5 commit. It means `cargo fmt --check` cannot currently be made a CI gate. Fix in its own `style` commit, then decide whether to add the gate. |
| 64 | Measure the access-log fan-in ceiling on real hardware | Perf | **Measured once at the Epic 9 close, on the dev workstation** (the load phase now in the `cluster` profile): two followers at 300 rps each for 60 s, 97 % of each node's rows ingested by the time the load stopped, zero quota drops; `docs/cluster.md` quotes it as one data point. Still open for the run on production-class hardware and for the ceiling itself (the phase runs at the envelope's per-node rate, not above it). Original entry: Story 9.6 AC #4. `docs/cluster.md` states a supported envelope (up to five nodes at a sustained few hundred rps each) DERIVED from the write ceiling `log_writer.rs` documents for SQLite, halved for retention and dashboard contention on the same connection. It has not been measured, and the doc says so rather than presenting a number that looks measured. Measure it with the `cluster` e2e profile plus `lorica-bench`, then replace the derivation with the measurement and the hardware it was taken on. **Concrete instrument** (Epic 9 close, performance audit): the profile is a correctness smoke with no load phase, so the ceiling stays undiscovered by construction. Add a phase that drives `lorica-bench` at the documented envelope (a few hundred rps per follower, both followers, a few minutes), polls the control plane's `lorica_cluster_telemetry_ingested_total{node_id}` and `lorica_cluster_telemetry_dropped_total{node_id,reason}` at a fixed interval, and asserts (a) `reason="node_quota"` stays at zero at the claimed rate and (b) the gap between a follower's local access-log row count and what the control plane reports ingested for it does not grow monotonically over the window (the drain keeps up). One run on representative hardware replaces "derived" in `docs/cluster.md` with a number and the machine it came from. |
| 65 | `cargo clippy -p lorica-api --all-targets` does not pass | Chore | Two `await_holding_lock` errors in Story 9.8's log-sink TEST code (`log_sinks/syslog.rs` and `log_sinks/mod.rs` both take a std `Mutex` test-hub guard across an await). Pre-existing, and the reason the project's gate 2 runs those crates WITHOUT `--all-targets`. Not a runtime bug: it is test-only serialisation of a global. Fix with an async-aware mutex or by dropping the guard before the await, then consider adding `--all-targets` to the gate. |
| 66 | The `cluster` e2e profile has never existed | Test | **RESOLVED 2026-09-09: the profile now exists** (`tests-e2e-docker`: `entrypoint-cluster-cp.sh`, `entrypoint-cluster-follower.sh`, `test-runner/run-cluster-smoke.sh`, three compose services, `--profile cluster` in `ALL_PROFILES`, a `run.sh` phase). One control plane and two followers, one in workers mode, joined through a real token redeemed from a file. Its first run found four defects before reaching an assertion, one of them in the product: `--cluster-advertise` silently accepted `host:port`, which became the control-plane leaf's SAN and made every join fail with a TLS error that named neither the cause nor the flag. Its first assertions found one more, a Critical in Story 9.7: the roster response is FLAT (`#[serde(flatten)]`) and the dashboard's type nested it, so the Cluster page rendered no node names on a real fleet while its unit tests passed against fixtures encoding the same fiction. **Epic close addendum:** the first version of this profile asserted only the SHAPE of Story 9.5's entitlement (its header claimed "the key reaches the node its route selects and no other" while no certificate was ever bound, so the assertion's own "nothing to distribute" branch fired unconditionally); the profile now includes the Pebble fixture the PRD named as its prerequisite, runs a real HTTP-01 order whose validation Pebble can only complete through the selected follower (the A record points at edge-a), binds the issued certificate to the selected route, and asserts the key install on edge-a and its absence on edge-b. It also exercises break-glass open/close on a follower and revocation of both followers, including `certificates_to_reissue` naming the key edge-a keeps. Still not covered, in priority order (backlog #77): a restart of any container (convergence-on-reconnect, the persisted generation and cursors are asserted nowhere), certificate renewal, `cluster leave`, drift/eviction/quarantine, and the ingest quota and storage watermark. Original entry: | Epic 9 debt inherited by Story 9.6, not created by it. `tests-e2e-docker/run.sh` lists fifteen profiles and none is `cluster`; the PRD designates one `cluster` profile (control plane plus two followers, both single-process and workers mode) to carry the Integration Verification for transport, enrollment, configuration replication, certificate distribution AND telemetry. Story 9.5's record states plainly that it "ships on unit tests only", and 9.2-9.6 are all in the same position: their IV has never run. Building it once covers five stories. Seven touchpoints per the PRD (compose service, runner service, two volumes, an entrypoint script, a Dockerfile COPY line, a runner script, and a run.sh phase plus the ALL_PROFILES entry whose omission causes the stale-volume failures run.sh documents). |
| 67 | A config apply cascade-deletes a follower's SLA history | Bug | Found during the Story 9.7 AC #5 arbitration. `sla_buckets.route_id` is `REFERENCES routes(id) ON DELETE CASCADE` (`003_sla_metrics.sql:15`), `PRAGMA foreign_keys=ON` is set on every connection (`store/mod.rs:975`), and `apply_replica_routes` calls the plain `DELETE FROM routes` in `delete_route` (`routes.rs:502`) for every local route absent from the blob or de-selected by `node_selector`. So a control-plane edit that narrows a selector silently destroys up to `sla_purge_retention_days` (default 90) of that node's SLA history, with no operator action on that node, outside the replica outcome counters, and re-selecting the node later does not restore it. On a standalone install the same cascade is an operator deleting their own route, which is defensible; driving it from another machine is not. `replica.rs`'s module comment claimed `sla_buckets` was "never touched" and has been corrected to say what actually holds. Decide between dropping the cascade (keep orphaned history, prune it on time only) and having the replica path preserve buckets across a route removal. `probe_results` has no foreign key and is genuinely unaffected. |
| 68 | Passive SLA is last-writer-wins across workers | Bug | Pre-existing, unrelated to Epic 9, and it affects the packaged default (`--workers auto`, `dist/lorica.service:35`). Each forked worker builds its own `SlaCollector` (`lorica/src/startup/worker.rs:437`) and its own 60 s flush task (`:453`) against the same `lorica.db`, and `insert_sla_bucket` is `INSERT OR REPLACE` on `UNIQUE(route_id, bucket_start, source)` (`store/sla.rs:122`, `003_sla_metrics.sql:27`). The persisted passive bucket is therefore ONE worker's slice of the minute, not the node's traffic, so on an N-core box the SLA page under-reports request volume by roughly N. Two adjacent defects in the same area: the active-probe writer (`lorica-bench/src/active_probes.rs:164-194`) writes the currently OPEN minute on every probe, so at the 5 s floor it overwrites the same row ~12 times and the stored bucket is the last single probe rather than an aggregate; and `passive_sla/collector.rs:73-75` replaces the map entry on a minute change without flushing, so a request arriving in M+1 before the free-running 60 s tick discards minute M. Fixing the first needs per-worker rows plus aggregation, or a single writer. |
| 69 | Decide the Viewer floor for fleet telemetry as one policy | Policy | **RESOLVED 2026-09-09 (Epic 9 close): every fleet read (`/cluster/nodes*`, `replication`, `drift`, `logs`, `waf-events`, `bans` GET) is Operator; `/cluster/status` stays Viewer; the dashboard hides the fleet navigation, the node filter and the fleet views from a Viewer.** Original entry: Story 9.7 QA (security-auditor, two Medium findings) plus the open #54. `GET /api/v1/cluster/nodes` sits at the Viewer floor and discloses `session_peer` (each follower's source address, 9.3) and `selected_for_hostnames` (which hostnames' private keys each node receives, 9.5); `/cluster/logs`, `/cluster/waf-events` and `/cluster/bans` sit there too and return every node's `client_ip`, `path`, `host`, `route_hostname` and `matched_value` (9.6). The floors match the single-node convention, so this is not drift, but the blast radius changed: a Viewer on the control plane used to see one node's traffic and now sees the whole fleet's through a UI built for it, which on a multi-tenant fleet is a cross-tenant aggregation point in the least-trusted role. Story 9.7 raised its OWN new field (`resources`) to Operator rather than inherit a convention that predates it, and left the rest alone because they are other stories' acceptance criteria. Decide the whole set at once, together with #54's sink topology, rather than one field per story. |
| 70 | Index the fan-in WAF `category` filter, after measuring | Perf | Story 9.7 QA (performance-auditor, measure-first). `fleet_waf_events` has indexes on `(node_id, id)`, `(node_id, timestamp)` and `(timestamp)` but none on `category`, and Story 9.7 added a `category` predicate to `query_waf`. With no other predicate, `ORDER BY id DESC LIMIT n` walks the rowid btree backwards and stops; adding an unindexed equality means every visited row is tested before it counts toward the limit, so a rare category degrades toward a full scan, and that scan holds the same `Mutex<Connection>` the ingest writer needs. That is the exact failure Story 9.6 designed the read paths against. Mitigated in practice by WAF events being orders of magnitude rarer than access rows and by the per-node row cap. Run `EXPLAIN QUERY PLAN` against a realistic category distribution before adding `idx_fleet_waf_category ON fleet_waf_events(node_id, category, id)`; the index is cheap but unjustified until the planner's actual behaviour on this shape is known. |
| 71 | The drift pill and `/cluster/drift` are two definitions of drift | Refactor | Story 9.7 QA (architect-reviewer, Medium). The server owns the predicate in `lorica-api/src/cluster/runtime.rs` (generation AND hash, evaluated against the LIVE session when the node is connected, falling back to the registry row otherwise). `Cluster.svelte` reimplements it as generation-only against the registry column, which `spawn_session_flush` persists on a 30 s timer. Two divergences follow: a node that converged by pull keeps a drift pill for up to 30 s after `/cluster/drift` calls it in sync, and a same-generation hash divergence never shows a pill at all. `NodeResponse` compounds it by flattening a registry row into the same object as live session facts with nothing marking the two clocks apart. Fix by carrying the verdict the server already computes on the roster row, or at minimum by exposing the live applied generation and hash so the comparison is like for like. One owner of the predicate, not one implementation per language. |
| 72 | A per-node budget for fanned-in audit rows | Perf | Story 9.9 QA (security and architecture auditors, High). Audit rows are exempt from the ingest quota's shedding verdict by design, and since the remediation they ARE charged to it, so a flooding node now gets a `retry_after_s`. What is still missing is a bound on what accumulates: fanned-in audit rows land in `access-log.db`, which the storage watermark does not measure (it stats `cluster-telemetry.db`), whose retention is time-based only, and which has no per-node row cap like the fan-in store's `DEFAULT_ROWS_PER_NODE`. `MAX_TELEMETRY_AUDIT` bounds one batch, not the rate and not the lifetime. **Corrected at the Epic 9 close (security audit): two mitigations this entry claimed did not exist.** The decode boundary never validated the timestamp beyond non-emptiness, so "stamp it in 2999" was open; it is now closed differently and structurally: fanned-in rows carry a `received_at` set by the control plane and retention ages them by ARRIVAL, so the origin's stamp cannot keep a row. And "its full permitted rate" was unbounded: `TelemetryPush` was the one expensive in-session method with no per-session cap (`CertPull` and `Renew` had theirs); it now has `MAX_TELEMETRY_PUSHES_PER_WINDOW` (240 per minute, twice a catching-up drain's legitimate peak), answered with a back-off acknowledgement rather than a dropped session. What remains open is the lifetime bound: a node pushing at the capped rate for the retention window still accumulates rate x window rows with no per-node ceiling. Add a per-node row cap enforced by the same hourly retention pass, so "never shed" cannot come to mean "never bounded". Note `lorica-cluster/src/telemetry.rs`'s justification for `DEFAULT_STORAGE_CAP_BYTES` mentions leaving volume "for the configuration store and the audit chain": that reasoning was written when the audit chain was local-only and is now stale. |
| 75 | Audit fan-in shares `access-log.db`'s one connection; measure the contention before redesigning | Perf | Epic 9 close (performance audit, High, measure-first). Story 9.6 kept telemetry OUT of `access-log.db` because that store is one connection behind one mutex shared by the local writer thread, every dashboard audit page, `verify` (a full ordered walk per chain) and the audit retention pass; Story 9.9 then put the audit share of the same fan-in traffic INTO it, on purpose, because verify walks one table. Each push with audit rows holds that mutex for one transaction of up to `MAX_TELEMETRY_AUDIT` rows; N followers on the same 10 s tick boundary each pay an acquisition, with no fairness against the control plane's OWN access-log writer, which drops rows on a full queue by design. The bounds added at the close (per-session push rate, arrival-time retention, quota charge) cap the rate, not the contention shape. Benchmark under a synthetic multi-follower audit-heavy load before choosing between a second connection on the same file, a separate audit fan-in store with a cross-store verify by `node_id`, or leaving it: the honest fix needs a design decision because verify's per-chain partitioning depends on one physical table today. |
| 76 | A partition longer than local retention loses fan-in rows with no signal | Observability | Epic 9 close (architecture review). `enforce_retention` deletes local access rows by id and `access_rows_after(cursor)` returns whatever is newer, so rows retention evicted below the drain cursor never reach the fleet view and nothing counts them; `docs/cluster.md` now states the loss. Add a `lorica_cluster_telemetry_lost_to_retention_total` style counter: retention knows the ids it deletes and the drain's cursor is in the config store, so the count is `deleted rows with id > cursor`, computed where the delete happens. Same for WAF events and audit rows. |
| 77 | `cluster` e2e: a restart phase, then renewal, leave, drift and the quota | Test | Epic 9 close (architecture review). The profile now covers enrolment, activation, replication, a real ACME issuance with challenge fan-out, need-to-know key distribution, telemetry and audit fan-in, break-glass and revocation. It exercises no restart of any container, so convergence-on-reconnect, the persisted generation and the persisted cursors are asserted nowhere; a `docker compose restart lorica-edge-a` between two smoke runs (run.sh owns the docker socket, the runner does not) plus assertions that the node reconnects, reports the same generation and resumes its cursors is the highest-value addition per unit of runner time. Then: certificate renewal (needs a short-lived node certificate or a clock lever), `cluster leave`, drift/eviction/quarantine, and the ingest quota and storage watermark (which #64's load phase would reach). |
| 78 | The drift alert takes a follower's break-glass claim at its word | Security | Epic 9 close (security audit, Low). `spawn_drift_watch` softens the alert text to "is in break-glass and diverges" on the peer-supplied `break_glass` bit, while the control plane holds no record that an operator ever opened a window (`cluster.break_glass.open` is a follower-side audit row that arrives only if the follower drains it). A compromised follower can keep itself out of commit rounds and have the resulting drift paged as the operator's doing. The honour-clock reset is now logged (WARN past half the cap), so cycling a claim is visible; the remaining step is for the drift alert to distinguish "the follower asserts break-glass" from "the control plane holds a corroborating `cluster.break_glass.open` row for this node". |
| 80 | The session cookie never reaches `/metrics` from a browser | Policy | Found by the cluster load phase at the Epic 9 close: the session cookie is `Path=/api`, so a cookie jar (and a browser) never sends it to `/metrics`, and the "session OR bearer" contract Story 8.8 documented holds only for a client that sends the cookie as an explicit header. The docs now say so. Decide whether to widen the cookie path to `/` (the dashboard's static assets would then receive it, which the `/api` scope was chosen to avoid) or to serve the operator's browser view of the metrics under `/api/v1/metrics` as well. Until then the bearer token is the scrape credential. |
| 79 | `refresh_control_plane` runs two full-table reads and a CRL mint under the global store lock | Perf | Epic 9 close (performance audit, Medium, no action at the documented scale). Every enrol, activate, revoke, retirement after renewal and cursor flush that pruned a serial calls it; each holds `Mutex<ConfigStore>` for `list_cluster_nodes` + `list_cluster_revoked_serials` plus the acceptor rebuild and the session reconciliation, so unrelated route/settings mutations queue behind it. Fine at five nodes; a bulk-enrolment burst or a mass revocation is where it would show. If either workflow is added, coalesce refreshes through a `watch` the way `spawn_replication_watch` coalesces reload signals, one refresh per burst rather than one per event. |
| 73 | `/api/v1/audit` inherited a single-node role floor for fleet-wide data | Policy | **RESOLVED 2026-09-09 (Epic 9 close), the narrow option: on a control plane, `node` absent or naming another node requires SuperAdmin, `node=` (empty) stays Operator, so the single-node behaviour is preserved exactly; a standalone or a follower is unchanged.** Original entry: Story 9.9 QA (security-auditor, Medium). The Operator floor is unchanged from Story 8.9, when the table held one node's rows; it now aggregates every follower's `operator_username`, `operator_role`, `ip` and `user_agent`. Backlog #69 was written for exactly this question but enumerates the Viewer-floor cluster endpoints and does not name this one, so the policy decision #69 defers does not currently cover it. On a delegated fleet any Operator on the control plane obtains the identity and source address of every operator on every node, including nodes they have no authority over. Decide it with #69 in one pass. The narrow interim option, if the whole question is deferred: `node` absent or naming a foreign node requires SuperAdmin while `node=""` stays Operator, which preserves the single-node behaviour exactly. |
| 74 | Two processes can fork the local audit chain | Bug | Story 9.9 QA (security-auditor, Low, pre-existing since Story 9.3). `lorica/src/cli_cluster.rs` opens its own `LogStore` on `access-log.db` and calls `insert_audit`, which reads the chain tail and inserts as two statements serialised only by the in-process `Mutex<Connection>`. With the daemon running, `lorica cluster leave` can read the same tail as a concurrent API-side write and produce two rows sharing a `prev_chain_hash`, forking the chain permanently and unrecoverably. The trigger is narrow and it predates Epic 9, but Story 9.9 raised the cost of a broken chain from a local annoyance to a fleet-wide verify failure. Wrap the tail read and the insert in an `IMMEDIATE` transaction so SQLite's write lock serialises across processes. |
