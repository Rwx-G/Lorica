schema: 1
story: "1.3.0-header-routing"
story_title: "v1.3.0 Phase 2.1 - Header-based routing"
gate: PASS
status_reason: "First-match-wins header rules (Exact/Prefix/Regex). Prometheus counter lorica_header_rule_match_total{route_id, rule_index} exposes rule-match activity with a \"default\" label for fallthrough. Dashboard surfaces disabled rules with a red badge when regex compilation fails at load time."
reviewer: "Romain G."
updated: "2026-04-12T00:00:00Z"
waiver: { active: false }
quality_score: 98
top_issues: []
evidence:
  tests_reviewed: 2
  risks_identified: 1
  trace:
    ac_covered: [1, 2, 3, 4]
    ac_gaps: []
nfr_validation:
  security: { status: PASS, notes: "regex crate = linear-time, ReDoS-safe" }
  performance: { status: PASS, notes: "Rules pre-compiled, first-match O(n)" }
  reliability: { status: PASS, notes: "Bad regex disables rule, not route; UI surfaces the disabled state" }
  maintainability: { status: PASS, notes: "Pure helper + pre-computed RouteEntry state" }
  observability: { status: PASS, notes: "lorica_header_rule_match_total{route_id, rule_index} counter + UI badge" }
risk_summary:
  totals: { critical: 0, high: 0, medium: 0, low: 0 }
  recommendations:
    must_fix: []
    monitor: []
