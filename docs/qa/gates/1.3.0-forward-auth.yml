schema: 1
story: "1.3.0-forward-auth"
story_title: "v1.3.0 Phase 3.1 - Forward authentication"
gate: PASS
status_reason: "Feature functionally complete with 7 e2e + 12 validator/unit tests covering all verdict paths (allow/deny/redirect/timeout/unreachable). http:// URLs warn-log when non-loopback (credential-forwarding risk). Optional per-route verdict cache (opt-in, TTL-capped at 60s, Cookie-keyed, honors upstream Cache-Control: no-store) with Prometheus hit/miss counter."
reviewer: "Romain G."
updated: "2026-04-12T00:00:00Z"
waiver: { active: false }
quality_score: 98
top_issues: []
evidence:
  tests_reviewed: 24
  risks_identified: 3
  trace:
    ac_covered: [1, 2, 3, 4, 5, 6, 7, 8]
    ac_gaps: []
nfr_validation:
  security: { status: PASS, notes: "http:// non-loopback warns; cache keyed on session cookie + route_id; Deny/FailClosed never cached; TTL capped at 60s" }
  performance: { status: PASS, notes: "Verdict cache (opt-in) removes auth round-trip on hot paths; off by default for strict zero-trust" }
  reliability: { status: PASS, notes: "Fail-closed on every error path (timeout/connect/unexpected status)" }
  maintainability: { status: PASS, notes: "Pure helpers + ForwardAuthOutcome enum + dedicated cache module" }
  observability: { status: PASS, notes: "Fail-closed warn; block_reason in access log; lorica_forward_auth_cache_total{route_id, outcome=hit|miss}" }
risk_summary:
  totals: { critical: 0, high: 0, medium: 0, low: 0 }
  recommendations:
    must_fix: []
    monitor: []
